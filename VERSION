version: 1.30.0
---

# 1.30.0

- 删除 idle timeout compact 功能：
  移除 `ModelConfig` 和 Conversation runtime config 中的 idle timeout compact 开关，删除 SessionActor 空闲超时自动压缩调度、Web / Telegram `/idle_compact` 与 `/idle_timeout_compact` 控制命令、Electron Conversation 属性面板中的空闲压缩设置和示例配置字段；手动 `/compact` 与 request-too-large 触发的 context compaction 保留。新增 config `0.13 -> 0.14` 升级，旧配置中的 `idle_timeout_compact_enabled` 会被忽略并在重写后消失；新增 workdir `0.25 -> 0.26` 升级，从 Conversation runtime config 文件移除旧字段。本次改变 `config` schema 至 `0.14`，改变 `workdir` schema 至 `0.26`。

# 1.29.6

- 修复 interrupt 后 provider forkserver worker 生命周期不一致：
  forkserver `StartWorker` 现在必须返回 worker 启动 ack 后父侧才缓存 `provider_worker_*` 并发送请求；pipe/fork 失败会返回真实错误，不再静默吞掉并导致后续 `StartOnWorker` 报 `unknown provider worker`。同时 provider worker 在 cancel、shutdown 和 forkserver 退出时会回收子进程，避免 Turn 中插入用户消息反复 kill worker 后积累 zombie/进程资源。本次不改变 `config` schema，不改变 `workdir` schema。

- 改用标准 Markdown artifact 渲染提示：
  foreground system prompt 不再要求 final assistant 消息使用旧 artifact tag，改为用 `![alt](relative/path.png)` 内联渲染图片、用 `[name](relative/path.ext)` 链接文件/目录/HTML；Electron chat 会把本地 Markdown 图片路径按 workspace artifact 解析并用现有附件预览渲染，文件链接继续打开右侧预览。本次不改变 `config` schema，不改变 `workdir` schema。

- 优化 StellaCodeX Electron streaming tool commit 范围：
  Chat render entry、assistant turn、tool round、tool segment 和 tool row 增加基于稳定消息/工具字段的 memo 比较，避免单个工具参数 delta 让所有可见历史工具组、Markdown 块和工具卡片重新 commit；仅当前变化的工具行继续刷新 shimmer / payload 展示。本次不改变 `config` schema，不改变 `workdir` schema。

- 降低 StellaCodeX Electron streaming tool 数据面开销：
  `stream_tool_call_delta` 不再每个 websocket delta 立即更新 React 消息状态，而是进入 RAF 队列并按帧合并当前积压的工具参数；工具参数流也不再额外维护一份不断增长的 stream buffer，只用本次合并 delta 更新 UI 和 activity 摘要，避免大工具参数流造成主线程字符串拷贝和 state update 风暴。本次不改变 `config` schema，不改变 `workdir` schema。

- 优化 StellaCodeX Electron 窗口焦点恢复路径：
  窗口 focus / visibility 恢复不再写入 App 顶层 React state，避免切回窗口立即触发 ConversationBar、ChatWorkspace 和右侧面板整树重渲染；前台已读标记改为 ref 驱动并延迟到焦点恢复后的空档执行，同时在 Chat 性能面板记录 `app.foreground_event` / `app.foreground_first_frame` 便于定位切回窗口首帧卡顿。本次不改变 `config` schema，不改变 `workdir` schema。

- 优化 StellaCodeX Electron Chat 性能诊断和消息流渲染边界：
  Chat commit 类性能指标改为性能面板打开时才采样，并按 RAF 聚合同一帧内多个 message / Markdown / tool 实例的 commit 记录，避免诊断面板本身放大卡顿；消息流主体抽出 memo 边界，输入框、命令弹窗和性能面板状态变化不再重新遍历可见消息列表；性能面板、虚拟窗口计算、composer 附件读取、live activity 计划面板以及工具卡合并/摘要逻辑拆到 chat 子模块，工具分类结果增加缓存，删除已经无调用点的旧 ActivityStatus 组件；消息时间格式化复用按系统时区缓存的 `Intl.DateTimeFormat`；第一层工具折叠增加展开/收起过渡动画，并统一工具展开指示箭头为折叠向右、展开向下。本次不改变 `config` schema，不改变 `workdir` schema。

- 继续拆分 StellaCodeX Electron App 顶层职责：
  Conversation websocket snapshot/update reducer、foreground session 本地状态合并、聊天协议诊断摘要、composer 控制命令解析/模式文案、本地文件/附件 target 解析、workspace tab snapshot、窗口 chrome metrics 和 App error boundary 分别迁入独立 lib / component 模块，`App.jsx` 不再混放这些纯数据函数和外围 UI 辅助；这为后续把 home/chat/workspace 流程拆成更细 hooks 留出边界。本次不改变 `config` schema，不改变 `workdir` schema。

- 改进 StellaCodeX Electron HTML 预览：
  右侧文件预览和 chat 内联 HTML artifact 默认以内嵌 iframe 渲染页面，并提供“预览 / 源码”两个 tab；有源码时用 `srcDoc` 渲染并保留源码查看，没有源码但有可访问 URL 时仍可渲染页面。本次不改变 `config` schema，不改变 `workdir` schema。

- 拆分 StellaCodeX Electron 高频 chat runtime 状态：
  `messages` / `messagesReady` / `sending` 从 `App` 顶层 React state 移入独立 runtime store，由 `ChatSessionPane` / `ChatWorkspace` 单独订阅；右上角 Raw Render 诊断改为打开 Raw 面板时才订阅消息并构建 snapshot，避免普通 stream delta 重渲染顶栏、Conversation Bar、文件预览和其它全局面板。本次不改变 `config` schema，不改变 `workdir` schema。

- 拆分 StellaCodeX Electron 窗口布局状态：
  sidebar、右侧面板、Terminal 开关和尺寸、拖拽 resize 逻辑迁入 `useAppLayout`，`App` 仅保留 session UI restore/save 的协调职责，减少顶层状态和布局事件处理代码堆叠。本次不改变 `config` schema，不改变 `workdir` schema。

- 拆分 StellaCodeX Electron workspace / preview 状态：
  workspace 文件树、展开/加载/错误状态、打开文件列表、当前文件路径、本地 workspace resource cache 和 cached file loader 迁入 `useWorkspaceState`，`App` 继续负责具体后端 API workflow 和面板协调，降低顶层 state/ref 数量并把文件预览 URL 清理收敛到 workspace 状态模块。本次不改变 `config` schema，不改变 `workdir` schema。

- 拆分 StellaCodeX Electron workspace workflow：
  workspace 路径加载、目录展开、打开文件/目录、PDF preview、文件刷新和 Markdown 本地图片解析迁入 `useWorkspaceWorkflow`；`App` 保留上传下载、消息附件入口和 session restore/save 协调，进一步降低顶层文件预览工作流复杂度。本次不改变 `config` schema，不改变 `workdir` schema。

- 拆分 StellaCodeX Electron workspace transfer workflow：
  上传/下载传输状态、上传压缩流程、下载保存流程和传输提示生命周期迁入 `useWorkspaceTransfers`，`App` 只传入当前 session、workspace cache invalidation 和刷新入口，继续减少顶层 transfer 状态与文件操作流程耦合。本次不改变 `config` schema，不改变 `workdir` schema。

- 优化 StellaCodeX Electron 空闲后台 CPU 占用：
  文件预览面板关闭时不再继续挂载 active file 的实际预览内容，避免 PDF/大文件预览的 worker、ResizeObserver、scroll listener 和渲染队列在不可见状态下继续运行；全局 LOG 关闭时也不再订阅普通协议事件或写入高频 stream 诊断，只保留 mismatch / gap / warning / error 等关键诊断。本次不改变 `config` schema，不改变 `workdir` schema。

- 优化 StellaCodeX Electron streaming tool 渲染性能：
  折叠的工具详情不再提前构建和解析大 payload；streaming tool 的展开详情避免对半截工具参数反复 JSON / diff 解析；历史消息的文本、结构化 item、token usage 和工具展示拆分结果增加对象级缓存，减少工具参数流式增长时的全量派生计算。本次不改变 `config` schema，不改变 `workdir` schema。

- 增加 StellaCodeX Electron Chat 性能诊断面板：
  Chat 左下角新增 `(i)` 性能面板，按需展示 render model、virtual window、tool group、tool card、Markdown commit、workspace commit 和 frame gap 等聚合耗时及慢事件；平时仅记录轻量 `performance.now()` 聚合，打开面板时才刷新 UI 和采样 frame gap，便于定位 streaming tool 卡顿热点。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 turn 中插入用户消息后 provider worker 复用已取消进程：
  forkserver 收到 provider request cancel 时会 kill 并移除正在执行的 worker；ProviderSession 现在会在 Abort 活跃请求后同步清空本地 cached worker binding，并在 forkserver 返回 `unknown provider worker` 时清空 stale binding、用同一 request_id 重建 worker 重试一次，避免下一条请求复用已经不存在的 `provider_worker_*` 导致插队消息后后端卡住。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 Web Terminal 响应路由和 Session 级前端布局状态：
  Terminal summary 的 `created_ms` / `updated_ms` 改为 JSON 可往返的 `u64` 毫秒，避免 TerminalService 已通过 ServiceChannel 返回 `terminal` / `terminals` payload 后，ChannelService 因 `serde_json` 不支持反序列化 `u128` 而记录 `bad_terminal_payload` 并让 Web 请求等到 30s 超时。ChannelService 向 TerminalService 转发请求时会携带 `request_id`，并优先按 Terminal `response_id` 精确匹配响应；Kernel 返回的 terminal 请求错误也会回到对应 Web Terminal 请求。StellaCodeX Electron 的右侧面板开关、面板宽度、打开文件和当前预览文件改为按 foreground session 持久化，避免同一 Conversation 下不同 Session 互相覆盖窗口状态。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 StellaCodeX Electron late stream delta 重新生成临时消息：
  如果 durable `message_appended` 或历史补拉已经把同一 `message_id` 固定为非 streaming 消息，后续迟到的 assistant / reasoning / tool-call stream delta 会被忽略，不再重新 append 一个 `_streaming` assistant 导致 final 后又出现挂起工具或重复文本；stream/durable commit 比较同时把 `item_id=fc_*` 与 `tool_call_id=call_*` 都纳入工具调用身份，并允许同工具名、同消息位置的参数前缀收敛，mismatch 诊断会输出 compare 版本和 canonical 对象。本次不改变 `config` schema，不改变 `workdir` schema。

- 放宽工具调用参数 commit 边界比较：
  StellaCodeX Electron 在比较 provisional stream 与 durable assistant message 时，工具调用参数允许 provisional 是 durable 的前缀；role、assistant 文本、工具 id/name、其它 item 仍严格比对。这样 durable commit 作为最终收敛边界到达时，不会因为前端渲染队列或事件处理尚未把最后几段 tool-call 参数落到 UI state 而误报 `chat.stream_commit_mismatch`，但参数内容发生分叉仍会报错。本次不改变 `config` schema，不改变 `workdir` schema。
- 修复工具调用流式参数在 commit 边界误报 mismatch：
  StellaCodeX Electron 不再把 `stream_tool_call_delta` 放入视觉 RAF 帧队列，而是作为协议状态立即合并到 provisional message；assistant/reasoning 文本仍保留帧队列。避免工具参数被 UI 动画节流后，durable `message_appended` 到达时前端拿半截工具参数和落盘消息比较，触发 `chat.stream_commit_mismatch` 误报。本次不改变 `config` schema，不改变 `workdir` schema。
- 修复 StellaCodeX Electron stream commit mismatch 误报：
  前端 durable/provisional 提交比较会优先使用工具调用的 provider item id，再退回 tool call id；调试摘要也统一通过 `messageItems()` 读取 `data` 中的落盘结构，避免 Codex durable 消息同时携带 `item_id=fc_*` 和 `tool_call_id=call_*` 时被误判为 stream/durable 不一致。本次不改变 `config` schema，不改变 `workdir` schema。
- 收敛 WebChannel stream 细粒度日志开关：
  WebChannel 默认只记录 turn start/done、stream error、stream index invalid 和 commit mismatch 等边界/异常日志；高频 assistant/tool/reasoning delta 只有设置 `STELLACLAW_WEB_STREAM_DEBUG=1` 时才写详细日志，便于复现 stream 协议问题时临时打开，平时避免刷日志。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.29.5

- 修复 Codex subscription tool call stream/durable 不一致：
  当 Codex websocket 只发送 `response.function_call_arguments.delta`，但 `response.completed.output` 未包含对应 `function_call` item 时，provider 会从已流出的 tool call delta 合成最终 `function_call` output item，再交给 SessionActor 落盘；避免前端 provisional assistant 已显示工具调用、durable `MessageAppended` 却只有文本，从而触发 `stream_commit_mismatch`。本次不改变 `config` schema，不改变 `workdir` schema。
- 补齐 StellaCodeX Electron stream 文本淡入：
  参考 Codex VSCode 扩展 markdown 渲染中的短 opacity fade，plain streaming 文本会把稳定前缀和最新追加 suffix 分开渲染，只对新增 suffix 做 180ms 淡入；不做位移、不重新解析 Markdown，降低 RAF 分帧后文本出现过硬的问题。本次不改变 `config` schema，不改变 `workdir` schema。
- 优化 StellaCodeX Electron stream 渲染性能：
  `chatStreamFrameQueue` 不再把同一帧收到的大块 delta 整块写入 UI，而是按 Codex VSCode 扩展的节奏以小块 `requestAnimationFrame` drain，durable 边界仍一次性 drain 保证一致性；高频 stream 文本帧只更新消息正文，activity / running activity 做低频节流；全局 LOG 关闭时不再记录普通 stream / UI append 事件到 localStorage 和本地日志，只保留 mismatch / gap / warning / error，避免诊断日志本身拖慢流式动画。本次不改变 `config` schema，不改变 `workdir` schema。
- 对齐 Codex VSCode 扩展的 stream 渲染节奏：
  StellaCodeX Electron 新增 `chatStreamFrameQueue`，assistant / reasoning / tool call delta 先在 `requestAnimationFrame` 队列中按 stream key 合并，再落到当前消息状态；收到 durable message、snapshot/history refresh、tool result、turn done 或 stream error 前会强制 drain 队列，避免调试窗口改变渲染时序时才不缺字。本次不改变 `config` schema，不改变 `workdir` schema。
- 简化 StellaCodeX Electron stream 文本渲染：
  前端不再对 streaming assistant 文本和工具 round 中间文本做额外打字机缓冲；每个 websocket delta 更新当前累积文本后会直接渲染，只保留消息块首次出现时的轻量淡入效果，避免协议流式效果和本地二次动画叠加导致重复打字机观感。本次不改变 `config` schema，不改变 `workdir` schema。
- 修复前端 provisional stream 重复投影：
  StellaCodeX Electron 在处理 assistant / reasoning / tool call stream patch 和通用消息合并时只按稳定 `message_id` 收敛同一条 provisional assistant，缺少 message id 的老事件才退回 `turn_id`；tool result 只挂到含有匹配 tool call 的 provisional message，找不到精确 id 时才挂到同 turn 最新工具消息；落盘历史中的 `context` 文本项会先规范成和 stream 相同的 `text` 项再进入工具 round 分组，避免同一 turn 内多条 assistant provisional 被错误合并，或同一段文本在 stream / commit 附近走两套渲染路径导致显示两份、出现中间文本顺序跳变、commit 后才收敛。本次不改变 `config` schema，不改变 `workdir` schema。
- 增加 StellaCodeX Electron 全局协议日志面板：
  右上角新增 `LOG` popover，记录完整落盘消息到达和 stream 增量应用两类主线事件，并保留 commit mismatch / stream index gap 等异常事件；日志内容压缩为消息 id、index、delta 长度、文本摘要和前端动作说明，支持按 `Stream`、`Append Stream to UI`、`Replace UI element` 三类筛选、复制和清空，方便定位前端 stream 渲染异常。本次不改变 `config` schema，不改变 `workdir` schema。
- 修复 streaming assistant Markdown 中间态渲染抖动：
  `_streaming` assistant、live reasoning 和 active tool note 的临时文本改为 plain text 渲染，等 durable/final 消息落盘后再走 Markdown/GFM/highlight 渲染，避免 Markdown 增量解析过程中 AST 边界变化导致 UI 看起来重复、缺字或稍后才收敛。本次不改变 `config` schema，不改变 `workdir` schema。
- 收紧前端 stream 顺序校验：
  `in_message_index` 的第一条可渲染事件必须从 0 开始，除非本地已经有同一 `message_id` 的 provisional 消息并记录了上一个 index；否则前端会撤销当前临时消息并记录 `chat.stream_index_gap`，避免 socket 重连、snapshot 缺少 live base 或 tracker 被异常 reset 后从中间 delta 开始拼接，造成稳定缺字、重复显示并在 durable commit 后才收敛。本次不改变 `config` schema，不改变 `workdir` schema。
- 修复 live tool round 中 streaming assistant 文本投影：
  工具执行中的 `_streaming` assistant message 如果同时有顶层累积文本和 `items[]` 内的 text/tool/reasoning 项，前端渲染时以顶层文本作为唯一 assistant text 来源，`items[]` 只用于工具和推理结构；避免 `items[]` 内旧 text item 或多 text item 分片导致工具调用前的 assistant 文本稳定缺字或重复。本次不改变 `config` schema，不改变 `workdir` schema。
- 增加 Raw Render 诊断面板：
  StellaCodeX Electron 的 LOG popover 新增 `Raw Render` tab，同步显示当前 raw `messages`、`displayMessages` 和最终 `renderEntries` 三层渲染投影摘要，并支持复制当前面板 JSON，用于定位 stream 文本是在数据面、display 投影还是最终 render entry 中出现缺字/重复。本次不改变 `config` schema，不改变 `workdir` schema。
- 抽离 Chat 渲染投影模型：
  将 `messages -> displayMessages -> renderEntries`、entry key、最新 assistant turn 和 pending assistant 判断收敛到 `components/chat/renderModel.js`，`ChatWorkspace` 和 Raw Render 诊断面板共用同一套纯函数，避免真实 UI 与诊断面板各自维护分组逻辑导致排查 stream 文本缺字/重复时出现分叉。本次不改变 `config` schema，不改变 `workdir` schema。
- 简化 Raw Render 诊断展示：
  `Raw Render` tab 默认先显示三层渲染管线的 compact overview，每行只保留索引、角色、stream/final/exec 标记、文本长度、item/tool 计数和文本摘要；完整 raw/display/render JSON 改为次级折叠项且只展示尾部若干条，复制按钮仍复制完整 snapshot，便于直接看出哪一层开始缺字或重复。本次不改变 `config` schema，不改变 `workdir` schema。
- 修复工具 round 内 assistant note 重复渲染：
  `ToolProcessGroup` 对同一条 structured assistant message 不再同时渲染 `textMessage` 和 `segments.notes` 两份文本；当 split 后已经有 segment note 时，只渲染 segment note，避免同一个 `assistantTurn` 内出现 render entry 唯一但画面文本重复。本次不改变 `config` schema，不改变 `workdir` schema。
- 修复消息正文误用 preview：
  前端 `messageText()` 和 streaming assistant 顶层正文选择顺序改为优先使用 `text_with_attachment_markers`、`rendered_text`、`text`、`content`，最后才 fallback 到 `preview`；`preview` 仅作为无正文时的兜底摘要，避免后端携带短 preview 时聊天正文显示缺字或被摘要替代。本次不改变 `config` schema，不改变 `workdir` schema。
- 稳定 assistant stream 文本累积：
  `stream_assistant_message_delta` 和 reasoning/tool delta 一样使用独立 stream buffer 累积完整正文，再写回 `_streaming` assistant message；不再依赖当前 React message 对象里的 `text` / `preview` 作为下一段 delta 的基准，避免快速 tool/assistant 事件交错或额外渲染时序差异导致正文回退成短文本。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.29.4

- 修复 Codex websocket 工具调用 ID 别名提交校验：
  WebChannel 不再依赖额外空参数增量把 `fc_*` 对齐到 `call_*`；`ToolCallItem` 新增可选 `item_id` 字段，durable assistant tool call 会同时保存 provider output item id 和 tool call id，stream/durable commit 校验按 `item_id` 对齐，工具执行和 ToolResult 继续按 `tool_call_id` 对齐。本次不改变 `config` schema，改变 `workdir` schema 至 `0.25`。

# 1.29.3

- 修复 Codex websocket 在 `response.completed` 才返回工具调用参数时仍触发 `stream_commit_mismatch`：
  Codex subscription provider 会在最终 response 落盘前扫描完整 `output`，对没有通过 `response.function_call_arguments.delta` 或 `response.output_item.done` 流出的 `function_call.arguments` 补发一次 `ToolCallInputDelta`，避免 `apply_patch` 等工具调用只出现在 durable assistant message 中、但 provisional stream message 缺少 tool call。本次不改变 `config` schema，不改变 `workdir` schema。
- 修复 Codex websocket 工具调用参数重复补发：
  Codex 的 `response.function_call_arguments.delta` 可能只携带 `item_id`，而最终 `response.completed.output` 同一 `function_call` 同时携带 `item_id` 和 `call_id`；provider 现在会把两者作为同一工具输入流的别名记录，避免 completed fallback 把已经流过的参数再次补发，从而造成同一 streaming assistant message 内出现两份 tool call input 并触发 `stream_commit_mismatch`。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.29.2

- 增加 WebChannel stream 协议诊断日志：
  WebChannelMain 会在 chat websocket snapshot 订阅、stream event 接收/记录、stream index invalid 和 stream commit mismatch 时记录当前 live state、message id、turn id、in-message index、provisional 文本长度和 committed 文本摘要，便于定位前端看到半截 stream / `stream_commit_mismatch` 时后端是否存在生命周期或顺序状态异常。本次不改变 `config` schema，不改变 `workdir` schema。
- 修复 Codex websocket tool call stream 缺失：
  当 Codex Responses websocket 没有发送 `response.function_call_arguments.delta`，但在 `response.output_item.done` 中返回完整 `function_call.arguments` 时，provider 会补发一次 `ToolCallInputDelta`，保证 WebChannel provisional assistant message 能包含完整 tool call，避免带工具调用的 assistant message 落盘时触发 `stream_commit_mismatch`。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.29.1

- 修复 Web message history/detail 查询读取半写 JSONL 的竞态：
  `AgentSessionService` 不再直接读取 `all_messages.jsonl` / `messages_index.json` 响应 Web history、summary 和 message detail 查询；相关请求会转发到 `agent_server` 内的 `SessionActor` control path，由负责维护和落盘 session history 的 actor 基于内存中的 `all_messages` 返回 typed response，避免查询路径读到正在写入的半截 JSONL 并触发 conversation kernel 重启。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.29.0

- 收敛 StellaCodeX Electron 前端数据面边界：
  将 chat stream 协议解析、顺序追踪、临时 assistant/tool/reasoning 消息投影和 stream error 撤销逻辑从 `App.jsx` 拆入独立 `chatStreamDataPlane` 模块，并继续把各类 stream event 到消息 patch / running activity patch 的映射收敛到该模块；将 chat websocket 连接/重连/陈旧连接防护拆入 `chatSocketClient`，将 durable message 合并、snapshot live projection 和 ack history reconcile 拆入 `chatMessagePlane`，将历史分页参数、snapshot state 和 plan/progress 归一化拆入 `chatSessionState`，并将消息本地缓存和通用 localStorage cache 拆入独立模块，避免实时 websocket、历史分页和 workspace 资源缓存继续共享散落在顶层组件里的隐式状态。本次不改变 `config` schema，不改变 `workdir` schema。
  如果同一 chat scope 在已有不同 `activeTurnId` running 时再次收到 `stream_turn_start`，前端会记录 protocol warning，避免该异常被 turn_start 的权威覆盖行为静默掩盖。
  Electron 新增 chat protocol diagnostics，本地记录 socket payload 摘要、stream commit mismatch、stream index gap、turn overlap 和重复 streaming assistant 到 renderer ring buffer 以及 `<userData>/logs/chat-protocol.jsonl`，用于区分后端重复事件和前端临时消息投影错误。

- 重构 SessionActor context compaction 的 Provider 边界：
  `ChatRole` 新增 `Compaction`，压缩结果不再伪装成普通 assistant 消息；`ChatMessageItem` 新增 `CompactionItem`，区分可跨 Provider 复用的 `generic_summary` 和 Provider 专用的 `provider_builtin` opaque payload。Provider trait 新增 compaction mode 和 builtin compact 入口，非 Codex provider 默认把通用 summary materialize 成 user context、过滤不匹配的 Provider 专用压缩块，避免把 Codex compact state 计入上下文或发送给不支持的模型。Codex subscription provider 新增 builtin `/responses/compact` 调用路径，按当前 Responses input 翻译、system instructions、工具 schema、reasoning、service tier 和 prompt cache key 构造 compact 请求，HTTP header 补齐 installation id，并把 user-agent 改为 `stellaclaw`；返回的 `compaction` / `context_compaction` item 会 materialize 为 `ChatRole::Compaction` + Codex `provider_builtin` compaction item。通用 summary compact 仍保留为 fallback，但移除 cybersecurity policy retry/reduction fallback。本次不改变 `config` schema，改变 `workdir` schema 至 `0.24`。

- 新增 Conversation 级 idle timeout compact 覆盖开关：
  `ModelConfig` 新增 `idle_timeout_compact_enabled` 作为每个模型的系统默认值，默认 `true`；Conversation runtime config 新增可选 `idle_timeout_compact_enabled`，`null` 表示继承当前模型默认值，`true` / `false` 表示覆盖。AgentSession 启动时会把最终生效值写入 `SessionInitial`，SessionActor 的 idle timeout compaction 会按该值决定是否运行；Web / Telegram 控制命令新增 `/idle_compact` / `/idle_timeout_compact`，支持 `on`、`off`、`default`，Web conversation summary 同步返回 override 和 effective 状态。本次改变 `config` schema 至 `0.13`，改变 `workdir` schema 至 `0.23`。

- 修复 AgentSession bridge 工具控制路径潜在阻塞：
  `AgentSessionService` 向 `agent_server` 投递普通 session request 时不再同步等待 JSON-RPC ack，而是通过专用 writer thread 发送 notification，避免 background agent / subagent 的 host coordination resolve、child session event、cancel 等事件在 service 主 `select!` 循环里被 30s request timeout 阻塞；AgentSessionService 转发 memory / skill / tool_binary / cron / kernel 请求时改用内部 service request id 做 pending correlation；`subagent_join` 的 cancel 路径在 cancel ack 后不再阻塞等待原 join response，未即时返回时直接生成 interrupted 工具结果；background agent / subagent 的生命周期仍由父 AgentSessionService 管理，父 session 在 child completed / failed / crashed 终态保存结果并向 child service 发送 shutdown，`subagent_kill` 对已终态 child 直接返回 failure 且不会重复 shutdown；background `terminate` 会先通知父 session 标记 killed，再关闭自身 runner；Kernel service runner 会在 service run 返回错误时投影 `ServiceOutput::Failed`，且 `StopService` 对不存在服务返回 `service_not_found`。本次不改变 `config` schema，不改变 `workdir` schema。

- 升级本地 `web_fetch` 工具：
  响应体读取改为先按 `max_bytes` 硬上限截断，再做文本/HTML/二进制分类，避免大响应在 `max_chars` 截断前被完整读入内存；HTML auto 模式会提取 title、description、链接和更接近 Markdown 的可读正文，二进制响应默认返回 metadata 而不是乱码正文；结果新增 content length、redirect、截断原因、选定响应头和 bytes read 等结构化字段，同时保留旧的 `body` / `truncated` 字段。本次不改变 `config` schema，不改变 `workdir` schema。

- 补充 SessionActor 工具结果落盘链路诊断日志：
  在工具 batch 完成到 durable `MessageAppended` 之间增加 `mark_loaded_skills`、`append_history_message`、session state persist 和 emit 的分段耗时日志；同时在 append 内部记录 system prompt 构造、would-compress 判断和 compression append 边界，便于定位工具结果已完成但前端仍停在工具执行中的卡点。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复最终 assistant 消息落盘后 live turn 未结束：
  SessionActor 在无工具调用的最终 assistant 消息 durable append 后会先发出 `TurnCompleted`，再清理当前 plan；plan 清理或其持久化失败只记录 `clear_current_plan_after_turn_completed_failed`，不再阻断完成事件，避免 WebChannel snapshot 在重新进入 Session 时仍保留上一轮 `running_tool_results`。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 Web stream tool call 一致性误报和超上下文恢复路径：
  `stream_tool_call_delta` 补充 `tool_name` 并由 Codex Responses output item 元数据填充，WebChannel 累积 provisional tool call 时不再把工具名临时写成 `tool`，避免中间 assistant+tool_call 消息落盘时误报 `stream_commit_mismatch`。WebChannel 对 assistant/tool/reasoning delta 改为按协议顺序直接追加，不再做前端式重叠去重，避免重复字符片段被误删。Provider preflight 发现请求超过模型上下文时改为先做 context compaction 并重试，不再直接执行带数据丢失的 history prune；WebSocket 单帧上限从 128 KiB 提高到 16 MiB，避免较大的 tool call/result 事件导致前端反复断连。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 Web stream 重复渲染和 provider request-too-large 结果兜底：
  StellaCodeX Electron 会按 `message_id + in_message_index` 追踪当前流式消息，重复到达的 stream event 会被忽略，非连续到达会撤销当前临时消息并提示错误；前端 tool call provisional 也会使用后端下发的 `tool_name`，避免本地 commit 校验仍把 `exec_command` 误判成 `tool`。SessionActor 在 provider 已经发出请求后收到 request-too-large 错误时也改为执行 context compaction 后重试，并发出 `CompactCompleted` 事件，不再走旧的 history prune 兜底路径。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 core 测试环境隔离：
  Codex subscription models / token refresh 相关单测会串行化修改全局认证环境变量，shell `~` workdir 单测会确保测试 HOME 目录存在，避免并发测试时临时 HOME 和 mock models URL 互相污染导致假失败。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.28.3

- 修复 SessionActor 工具结果落盘被同步压缩阻塞：
  `tool_result` / `tool_result_repair` 追加到 durable history 时不再在同一同步路径触发 context compression，避免工具已经完成但工具结果尚未 `MessageAppended` 时，压缩请求、memory context lookup 或 provider summary 阻塞整轮状态推进，表现为前端一直停在某个工具执行中。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 StellaCodeX Electron final 消息图片附件渲染：
  Durable `FileItem` 中的 `file://` / 本地路径图片不再直接作为 `<img src>` 使用；Electron 会按当前 Conversation workspace root 映射成 workspace 相对路径，通过现有 workspace file API 拉取图片内容并缓存为 data URL 后展示，避免历史 final assistant 消息里的图片附件显示为破图。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 StellaCodeX Electron 右键菜单重命名入口：
  Conversation 重命名改为应用内 Dialog，不再依赖右键菜单选中事件里触发原生 `window.prompt`，避免 ContextMenu 关闭 / 焦点恢复和浏览器用户激活策略导致点击“重命名 Conversation”没有任何输入框；Session 右键菜单中的 prompt / confirm 类操作也会在菜单关闭后的下一轮事件循环触发。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 StellaCodeX Electron 用户消息重复和附件源 Conversation 解析：
  `chat.user_message_queued` / `chat.user_message_started` 不再把同一条用户消息再次插入消息列表，只更新本地 pending / queued 状态，落盘后的 durable 用户消息会替换临时消息；消息合并逻辑会按 durable index、client message id 和临时状态清理用户消息回声，避免发送后出现两份或多份用户气泡。chat snapshot 里的 provisional assistant message 和后续 stream delta 会按 turn id / message id 合并到同一条流式消息，避免切换 / 重连后同一段 assistant 文本临时显示两份。历史 final 消息附件若携带跨 Conversation 的绝对 `file://` 路径，预览、打开和下载都会解析路径中的源 Conversation id 再调用 workspace file API，避免使用当前 Conversation id 访问不存在的文件。本次不改变 `config` schema，不改变 `workdir` schema。

- 优化 StellaCodeX Electron 发送和流式回复显示：
  用户消息刚发送时会保留 pending/queued 可见状态和轻量高光，等待响应状态不再被隐藏；assistant streaming 文本和工具 round 中间文本会在前端按当前 delta 缓冲做平滑打字机显示，后端仍按原始 `chat.stream_assistant_message_delta` 逐事件转发，不改变 stream 协议。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.28.2

- 适配 StellaCodeX Electron 到新的 WebChannel 协议：
  Conversation / foreground session 初始化改为通过 `home.snapshot` websocket 获取，不再依赖已删除的 conversation list、foreground session list 和 status REST 读接口；前端会归一化 `conversation_name`、`session_name`、`last_committed_message_*` 和 foreground session state 字段，并处理 `home.foreground_session_*` 增量事件。Chat websocket 新增处理 `chat.user_message_started` / `chat.user_message_committed`，stream tool result 会优先合并回当前 provisional assistant 消息，避免工具结果单独渲染成空消息。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.28.1

- 删除 Web debug 读接口和旧 StatusService 链路：
  移除 `/api/debug/conversations`、`/api/debug/conversations/{conversation_id}/foreground_sessions` 和 `/api/debug/conversations/{conversation_id}/status`，Web 读取初始化只保留 home / chat websocket snapshot 与 message history/detail REST 边界；同时删除 `ChannelIngress::Status` / `StatusSnapshot`、StatusService 和 status service protocol，不再保留旧兼容壳。新增 workdir `0.21 -> 0.22` upgrade，从旧 service manifest 中移除 status service entry 和 `local__status` 存储目录。本次不改变 `config` schema，改变 `workdir` schema 至 `0.22`。

# 1.28.0

- 新增 Web stream 协议重写骨架和工具结果流式事件：
  WebChannel 目录拆分为 `channels/web/{channel,main,protocol,history,ids,control}.rs`，删除旧单文件实现并以新的 HTTP/WebSocket channel 入口承接 conversation list、message append、session stream 和 conversation stream；旧 `.stellaclaw/channels/<channel>/web_state.json` 已读状态迁移到 `.stellaclaw/web/<channel>/seen_state.json`。WebChannel 连接订阅、home 序号、chat live state、seen state 和 processing state 改由串行 `WebChannelMain` actor 统一维护，chat stream 对外投影为扁平的 `chat.*` typed events，不再向前端暴露嵌套 `event` payload；临时 `TODO_NewWeb.md` 和未接入的 Web state 草稿模块已删除，避免协议文档与实现继续分叉。Provider message 内的 stream assistant/tool-call/reasoning/error 事件现在从 SessionActor 起携带单调 `in_message_index`，Web 投影同时带 `next_message_id`，前端可以按 message id + index 检查连续性；`chat.message_appended` 明确带 `committed: true`，作为 durable timeline 边界。Web message history paging / detail REST 入口不再由 WebChannel 直接读取 `all_messages.jsonl`，改为通过 `ChannelIngress::QueryMessageHistory` / `QueryMessageDetail` 转给 ChannelService，再向对应 AgentSessionService 查询当前 foreground session 的持久化历史；home / chat snapshot 和 conversation / foreground session summary 的 message count / last committed message 也改为同一路径查询 SessionService，不再由 WebChannel 直接读取消息日志。同时新增 home / chat / terminal websocket snapshot、30s heartbeat 和 live foreground session projection 的 typed protocol/state 基础；core `LocalToolBatchExecutor` 在每个独立工具完成时发送 `ToolBatchProgress`，`SessionActor` 会先投影 `StreamToolResultDone` live 事件，再等待完整 tool batch 落盘为 durable `MessageAppended`，该事件已贯通到 AgentSession / Channel stream，并补充顺序单测。本次不改变 `config` schema，改变 `workdir` schema 至 `0.21`。

- 清理旧 Channel 层状态 / 进度事件死代码：
  删除不再由新 `conversation_new` service-tree 生产的旧 `ChannelEvent::ProgressFeedback`、`ConversationUpdated` 和 `Status` 事件，以及对应的 Web / Telegram 进度消息缓存、状态文本渲染和旧 token usage 汇总结构；Web 状态查询继续走 `ChannelIngress::QueryForegroundStatus` / StatusService，前台实时渲染继续走 session stream typed events，避免旧兼容分支和新消息 API 并存。本次不改变 `config` schema，不改变 `workdir` schema。

- 清理旧 Channel delivery 壳和冗余 warning：
  删除旧 `OutgoingDelivery` / `OutgoingAttachment` 输出结构、通用 `Channel::send_delivery` trait 方法和已无生产方的 `DeleteConversation` ingress variant；Telegram 改为直接从 durable `message_appended` 渲染 assistant 消息，Web 继续按 typed message / session / home events 投影。同时删除旧 delivery 清理后暴露出的 dead code warning，包括未使用的 caption 常量、`OutgoingOptions.prompt`、`is_available_agent_model`、`ConversationIdManager::remove_mapping` 和 `WorkdirLayout::runtime_shared_root`。本次不改变 `config` schema，不改变 `workdir` schema。

- 收口 Web stream / home 事件协议：
  WebChannel 的 home / chat snapshot、conversation/session mutation、message append、stream error 和 session stream 投影开始统一通过 `channels/web/protocol.rs` 的固定 builder 生成，避免事件名和字段形态散落在多个 `json!` 调用中；foreground session 创建、重命名、删除分别投影为 `home.foreground_session_upserted`、`home.foreground_session_updated` 和 `home.foreground_session_deleted`，不再用整包 conversation upsert 伪装 session mutation。WebChannelMain 会按 `message_id + in_message_index` 校验 stream 连续性，发现缺口时立即撤销当前 provisional message 并发送 `chat.stream_error`；最终 durable assistant message 到达时会对比已累积的 stream 文本 / tool call 参数 / reasoning summary，不一致时先报告 `stream_commit_mismatch`，再让 durable `chat.message_appended` 替换页面内容。SessionActor 对新用户消息抢占 active provider stream 增加 200ms 活跃保护，最近仍有 provider stream delta 到达时不会立刻 abort，只有超过 grace window 后才用 `superseded_by_user_message` 取消。本次不改变 `config` schema，不改变 `workdir` schema。

- 收敛 Web 读取接口到 websocket snapshot：
  Conversation list、foreground session list 和 conversation status snapshot 不再保留在正式 REST 路径；首页和 session 列表初始化应通过 `GET /api/ws/home` 的 `home.snapshot`，后续通过 `home.*` 增量事件更新。为了调试保留只读入口，但统一移动到 `/api/debug/conversations`、`/api/debug/conversations/{conversation_id}/foreground_sessions` 和 `/api/debug/conversations/{conversation_id}/status`；create / rename / delete 等 mutation REST 继续保留。本次不改变 `config` schema，不改变 `workdir` schema。

- 收敛用户消息生命周期和旧 delivery/progress 投影：
  ChannelService 收到平台输入后会先发 `UserMessageQueued`，AgentSessionService 在开始处理用户消息和用户消息落盘后分别投影 `UserMessageStarted` / `UserMessageCommitted`，Web chat stream 对应新增 `chat.user_message_queued`、`chat.user_message_started` 和 `chat.user_message_committed` typed events；同时删除旧 Channel `Deliver` request / `Delivery` event、`chat.delivery` 投影和 AgentSession 层 `Progress` 事件，计划更新只走 `PlanUpdated`，普通进度文本不再伪装成协议消息。本次不改变 `config` schema，不改变 `workdir` schema。

- 收敛 Web metadata / runtime config 查询边界：
  Kernel 协议新增只读 `QueryRuntimeConfig`，ChannelService 新增对应 `ChannelIngress::QueryRuntimeConfig` 和 typed `KernelRuntimeConfig` event；WebChannel 的 conversation list、conversation rename、foreground session list/create/rename/delete、message append summary 和 status summary 不再直接读取或修改 conversation metadata / runtime config 文件，而是通过 `WebChannel -> ChannelService -> Kernel` 查询或更新。新建 conversation 的初始 metadata 写入和删除 conversation 的目录移除仍保留在 Host/Web REST 生命周期边界。本次不改变 `config` schema，不改变 `workdir` schema。

- 补齐 home websocket 的 metadata / runtime config 动态事件：
  通用 ChannelEvent 新增 Web 专用 home event 投影；Kernel metadata 更新会投影为 `home.conversation_updated`，携带 conversation name、foreground session id、model selection pending 和 session nickname patch；runtime config 更新会同步投影为 `home.conversation_updated`，携带 model、reasoning、sandbox、remote 和精简 runtime_config patch，避免前端必须靠 REST 回读才能看到模型 / 推理强度 / remote / sandbox 变化。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 FixedSSH workspace remote helper 启动：
  WorkspaceService 不再把整段 Python helper 作为裸 `ssh host python3 -c <script>` 参数发送给远端 shell；现在会把 helper 脚本 base64 编码后作为 shell-safe 参数传给远端 `python3 -c` loader，JSON request 仍通过 stdin 传输，避免 zsh 等远端登录 shell 把 Python 函数体当 shell 语法解析并报 `parse error near ')'`。远端 listing 也改为用 `lstat` 和 symlink 优先识别，避免断开的远端 symlink 让 workspace list 失败。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 Web workspace / file API 的 WorkspaceResponse 分发：
  ChannelService 发起 WorkspaceService 请求时会把平台 workspace request id 写入 `ServiceCall.request_id`，WorkspaceService 回包后通过 `response_id` 找回对应 pending 请求，不再依赖 FIFO；同时 workspace response 会优先按 `response_id` / workspace 来源走 workspace decoder，避免 `listing` / `file` 响应被当作普通 Channel payload 报 `bad_channel_payload`。`WorkspaceEntry.modified_ms` / file `modified_ms` 从 `u128` 收敛为 JSON 可稳定反序列化的 `u64` 毫秒时间戳，修复线上 workspace listing decode 失败。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 Web workspace / file API 在 Channel 重启后的响应订阅：
  WebChannel 发起 workspace list/read/write/upload/download 请求时仍按 `WebChannel -> ChannelService -> WorkspaceService` 路径走，但现在由 HostRuntime 在发送 Channel ingress 的同时建立事件订阅；如果发现 main Channel ingress 已关闭，会重启 conversation、重新订阅新 Channel 的事件后再重试请求，避免请求已经发到新 Channel、HTTP 却仍等待旧订阅而超时。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 AgentSession bridge 响应分发串服务：
  `ServiceCall` 新增可选 `request_id` / `response_id` correlation 字段；AgentSessionService 发起 memory / skill / tool_binary / cron / kernel bridge 子请求时写入 `request_id` 并记录 pending 请求类型，各服务回复时回填 `response_id`，AgentSessionService 再按 `response_id` 找回原始 pending 请求和协议类型，避免 `{"type":"accepted"}` 这类通用响应被 memory response 误吞，导致 `cron_task_create` 已创建任务但前台 turn 永久卡在 open tool call。本次不改变 `config` schema，不改变 `workdir` schema。

- 补齐 Web / StellaCodeX 多 foreground session 会话支持：
  `ChannelIngress` 的前台消息、状态查询、cancel / continue / compact、host coordination resolve 和删除前台 session 等操作都带上可选 `foreground_session_id`，Web API 新增按 Conversation 管理、列出、创建、重命名、删除 foreground session 以及按 session 拉取消息 / 建立 websocket 的接口；Web seen 状态改为按 foreground session 记录，conversation stream 会返回每个 foreground session 的摘要。StellaCodeX Electron 的 Conversation Bar 改成文件夹 + 子对话结构，发送消息、实时 websocket、消息分页、已读和标题状态都按选中的 foreground session 走，保留 workspace / terminal 的 Conversation 级共享语义。本次不改变 `config` schema，不改变 `workdir` schema。

- 收敛 WebChannel 的 Conversation metadata 读取边界：
  WebChannel 列表、按 platform chat 查找和 `load_web_state` 不再直接读取 `ConversationMetadataStore` 中的 JSON 内容，而是通过 `ChannelIngress::QueryKernelMetadata` 交给 ChannelService 向 Kernel 查询当前 `ConversationMetadata`；磁盘 store 只保留为创建 / 删除和已知 conversation id 的持久化边界。ConversationKernel 挂载任意 AgentSession 时会补齐 `session_nicknames` 默认值，确保每个 session 的名字由 Kernel metadata 维护。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 StellaCodeX Electron 多 Session 侧栏交互：
  Session 行右键菜单改为 Session 级操作，支持重命名 Session，文件夹行右键菜单保留 Conversation 级重命名 / 隐藏 / 删除；Conversation 文件夹展开状态改为只由显式点击切换控制，避免选中项或少量子会话强制展开导致折叠失效；新建 Session 入口移到文件夹行右侧，并仅在悬浮 / 聚焦时显示，同时移除文件夹行上的 session 数字。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 Web Conversation ingress stale sender：
  当某个 ConversationKernel 或 ChannelService 已经退出但 HostRuntime 仍保留该 conversation 的 main channel ingress sender 时，Web / Electron 再次发送消息会收到 `channel ingress is closed`；HostRuntime 现在会在发送发现 ingress 关闭时记录 warning、移除旧 kernel、重新启动该 conversation 并重试一次请求，避免用户必须重启整个 stellaclaw 服务。本次不改变 `config` schema，不改变 `workdir` schema。

- 补齐 workdir 级错误和警告日志：
  Host logger 现在除继续写 `.stellaclaw/host.log` 外，会把 `warn` 和 `error` 级别日志分别镜像到 `<workdir>/logs/warn.log` 和 `<workdir>/logs/error.log`；ConversationKernel 主循环遇到会导致 kernel 退出的 fatal output / dispatch 错误时，会在返回前写入 `conversation_kernel_failed` 到 `<workdir>/logs/error.log`，包含 conversation id 和完整错误，避免后台线程失败原因丢失。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 ChannelService bad payload 杀死 kernel：
  ChannelService 收到无法识别的 inbox payload 时不再发 `ServiceOutput::Failed` 并导致 ConversationKernel 退出，而是投影 `channel.bad_payload` 错误事件和 `bad_channel_payload` 状态，记录来源 service 与原始 payload，保证错误 payload 不会中断当前 conversation 的消息流 / websocket 事件链路。本次不改变 `config` schema，不改变 `workdir` schema。

- 升级 Codex reasoning summary 持久化和 provider stream 事件：
  `ReasoningItem.codex_summary` 从单个字符串升级为有序 summary part 数组，保留 Codex reasoning summary 的分段顺序，并新增 workdir `0.19 -> 0.20` upgrade 迁移旧 session history；`ChatMessage` 新增稳定 `message_id`，SessionActor 写入 `all_messages.jsonl` 时同步生成 `messages_index.json`，WebChannel 的消息详情、summary 和 seen 合并开始基于 `message_id` / index 文件定位消息，不再把裸数字行号当作消息 ID；provider stream delta 不再写入历史，最终仍以 completed / done 后生成的完整 `ChatMessage` 为准，同时 core SessionEvent / conversation_new AgentSessionEvent 新增 `Stream*` 流式事件用于 assistant 文本、工具调用参数、reasoning summary delta、reasoning summary part added 和带 `message_id` 的 stream error 实时投影。本次不改变 `config` schema，改变 `workdir` schema 至 `0.20`。

- 新增 provider native system prompt 查询接口：
  Provider / ProviderSession 可以基于 `ModelConfig` 返回 provider 原生 system prompt；SessionActor 构造 system prompt 时会优先把 provider 原生 prompt 作为 common section，只有 provider 没有返回 prompt 时才使用 StellaClaw 默认 `common_prompt()`，后续工具协议、session kind、remote/runtime metadata 和 tool efficiency sections 仍会继续追加。Codex subscription 会在缺少内存/磁盘缓存时请求 Codex `/models` endpoint，读取匹配模型的 `model_messages.instructions_template`，并把完整 models payload 缓存到 `.stellaclaw/codex/models.json`，后续会优先复用进程内缓存和磁盘缓存；如果需要在线请求且请求失败，则直接返回 provider error。本次不改变 `config` schema，workdir schema 仍为 `0.20`。

- 注入 Codex Pragmatic personality：
  Codex subscription provider 在渲染 `/models` 返回的 `model_messages.instructions_template` 时会替换 `{{ personality }}`，优先使用同一 payload 中的 `personality_pragmatic`，缺失时使用 Codex Pragmatic personality 文本兜底，避免 provider 原生 prompt 缺少 Codex 风格协作约束。本次不改变 `config` schema，不改变 `workdir` schema。

- 收敛工具 prompt 协议：
  删除按 enabled tools 动态注入的 `PromptProtocol` 系统；`file.discovery`、`file.editing`、`host.update_plan`、`process.shell` 和 `host.memory` 的稳定指导进入 common system prompt，单工具依赖的 `shell_make_visible`、`attachment_make_visible` 和 `subagent_start` 指导合并进各自工具 description。本次不改变 `config` schema，不改变 `workdir` schema。

- 收敛 Fixed SSH 远端 workspace 指令快照：
  Fixed SSH 模式启动时读取到的远端 workspace `AGENTS.md` 不再作为 `SessionInitial.remote_workspace_instructions` 直接拼进 system prompt，而是进入 `RuntimeMetadataState` 的 `remote_workspace` prompt component，并由 `[Remote Workspace Snapshot]` 渲染；压缩后会和 identity、user metadata、memory、skills、remote aliases 一样作为 canonical snapshot 保留。本次不改变 `config` schema，不改变 `workdir` schema。

- 收敛工具效率提示词：
  原本独立追加在 session kind / runtime snapshot 后面的 `Tool Efficiency Prompt` 合并进 common system prompt，system prompt 构造链路不再有独立工具效率 section。本次不改变 `config` schema，不改变 `workdir` schema。

- 调整工具 batch 中断语义：
  `LocalToolBatchExecutor` 不再由 batch worker 轮询共享 cancel token 后放弃等待 operation 线程，而是由 SessionActor 通过 batch interrupt channel 通知 worker，worker 再向当前运行的 operation fan-out 独立 interrupt channel；已收集的工具结果会保留，正在运行的工具需要返回 normal / interrupted / partial result，尚未启动的工具由 worker 合成 interrupted result，并且中断后不再启动后续工具。`subagent_join` 被中断时不再由 AgentSessionService 偷看用户消息或 CancelTurn 清理 pending join，而是由 core 发送显式 `subagent_join_cancel` bridge request，service 收到后 resolve 原始 join 为 interrupted；普通 ConversationBridge 工具在没有专用 cancel 协议时仍会快速返回结构化 interrupted 结果，避免前台 turn 被 host coordination 阻塞。本次不改变 `config` schema，不改变 `workdir` schema。

- 改造 shell 工具输出等待为事件驱动：
  `shell_exec` / `shell_write_stdin` 的输出收集不再使用固定 sleep 轮询；ShellSession 现在通过 bounded wake channel 在 reader 写入输出、waiter 标记退出时唤醒 collector，collector 在输出事件、工具中断和 yield deadline 之间 select，并在进程退出后保留短暂 trailing output grace window。本次不改变 `config` schema，不改变 `workdir` schema。

- 引入 BaseTool / ExtTool / ProviderNativeTool 工具注册抽象：
  ToolCatalog 开始保存 enabled tool entry，新增 `BaseTool`、`ExtTool`、`ProviderNativeTool` 和 `ToolSet` 抽象；默认 `BuiltinToolSet` 兼容现有工具定义路径，Provider / ProviderSession 可以可选返回自己的 ToolSet 来替换默认工具注册。LocalToolBatchExecutor 现在持有 enabled ToolCatalog，operator 可以通过 `RegisteredTool` 直接调用 enabled Base/Ext tool 的 `call(ctx,args)`；普通本地 BaseTool 和 Provider ExtTool 已接入这条执行路径，ExtTool 可以在转换 provider-specific schema 后复用 `BuiltinBaseTool::call_local`，并会继承其 `base_tool_id()` 对应 base tool 的启用条件；Codex subscription 已用 provider ToolSet 显式注册自身可见工具集合，把 shell 暴露为 Codex 风格的 `exec_command` / `write_stdin` facade，把 `apply_patch` 暴露为更接近 Codex freeform patch 语义的简化 schema，执行时默认补 `format=freeform` 后复用 base `apply_patch`，同时保留当前 session 启用的 host、memory、cron、skill、media、web 和 Fixed SSH visibility 工具；`update_plan` 不再由 core SessionActor immediate 特判维护 plan，而是作为 ConversationBridge 工具交给 AgentSessionService 更新内存态 `current_plan` 并投影 `PlanUpdated` 事件，plan 不写入 service state；`skill_load`、`web_search`、provider-backed、其它 bridge 和 provider-native 工具仍保留各自需要 actor/provider/service 上下文的专用路径。本次不改变 `config` schema，不改变 `workdir` schema。

- 继续收敛 Codex subscription 工具 facade：
  Codex provider ToolSet 不再直接暴露底层 `shell_stop` 和 `shell_make_visible`，改为暴露 `exec_stop` 和 Fixed SSH 下的 `exec_make_visible`，参数命名与 `exec_command` / `write_stdin` 的 `session_id` 语义保持一致，内部仍复用对应 base tool。本次不改变 `config` schema，不改变 `workdir` schema。

- 优化 `web_fetch` 工具：
  `web_fetch` 现在只要求 `url` 参数，`timeout_seconds`、`max_chars`、`method` 和 `format` 都有默认值；工具会校验 HTTP/HTTPS URL，默认设置 User-Agent，支持 GET/HEAD、raw/text/auto 格式，并在 auto 模式下把 HTML 去除 script/style/noscript 后转成可读文本。返回结果新增 `kind`、`ok`、`body_format`、`max_chars` 等结构化字段。本次不改变 `config` schema，不改变 `workdir` schema。

- 重命名原生媒体查看工具：
  模型可见的 `image_load`、`pdf_load`、`audio_load` 分别改名为 `image_view`、`pdf_view`、`audio_view`，使工具名更贴近其“把媒体放入下一次模型请求供检查”的语义；README、核心文档和相关测试同步更新。本次不改变 `config` schema，不改变 `workdir` schema。

- 新增 `conversation_new` service-state workdir 升级：
  新增 workdir `0.18 -> 0.19` upgrade，把旧 `conversations/<id>/conversation.json` 中的 conversation 运行配置、foreground session 轻量状态和 managed agent 记录拆到 workdir 级 `services/<conversation_id>/` 下的 `manifest.json`、`runtime_config.json` 和 `local__agent__foreground__main/service_state.json`；旧 foreground SessionActor 日志目录会迁移到新 foreground service session id，避免历史消息丢失；旧全局 `.stellaclaw/cron_tasks.json` 会按 conversation 拆到各自 `local__cron/tasks.json`，并把任务归属映射到默认 foreground/channel。legacy script cron task 暂时迁移为 disabled prompt task 并保留错误说明，避免在新 CronService 尚未支持 script 模式时静默运行不等价逻辑。本次不改变 `config` schema，改变 `workdir` schema 至 `0.19`。

- 新增实验性 `conversation_new` service-tree 骨架：
  新实现先不接入现有运行链路，新增 `ConversationKernel`、具体 `ServiceAddr` 动态路由表、线程式 `ConversationService` 生命周期、保留 `kernel` 目标、固定 `channel` 服务边界，以及 `service_protos/*` typed payload，作为后续重写 conversation / service 的工作基础；骨架同时记录 `services/<conversation_id>/manifest.json`，支持按 manifest 恢复已挂载的 skeleton service，并为 `cron`、`memory`、`skill`、`tool_binary`、`workspace`、`status` 建立独立 service/proto 文件和标准挂载入口，`control` proto/service 骨架暂保留但不再作为标准服务挂载；Kernel 现在也有正式主循环，可在线程内同时接收 shutdown 输入和各 service 的 `ServiceOutput`；`ChannelService` 自己拥有平台 ingress receiver，并负责把平台输入转换成 service call，`ChannelIngress` 现在只接受 normalized `ChatMessage` 用户内容，并覆盖 foreground context/status、foreground 创建/删除、cancel/continue/compact、host coordination resolve、conversation-wide runtime config update 和 workspace 请求，同时可向自身拥有的平台 adapter 队列输出 typed `ChannelEvent`，避免 Web/Telegram 反向解析 generic status；前台 AgentSession 地址改为 `agent/foreground/<id>`，默认 `main`，同一个 conversation 可以挂载多个 foreground session，Channel 地址也改为 `channel/<id>` 并与同 id foreground 一对一绑定，`channel/<id>` 会拒绝来自其它 foreground id 的 session event；AgentSession 输出路由改为 Kernel 创建时写入的显式 binding metadata，binding 收敛为单一 `event_sink` 和可选 `parent_addr`，foreground sink 指向同 id channel，Cron-owned background sink 指向 cron，subagent 继承父 session sink；`AgentSessionService` 协议补齐 enqueue message、cancel/continue/compact、host coordination resolve、context query、status、shutdown 和 session event，并开始把轻量 service lifecycle/context state 持久化到自身 storage 的 `service_state.json`，完整 session history 仍由 core SessionActor 自己持久化；`AgentSessionService` 也新增 service protocol 到 core `SessionRequest` / `SessionEvent` 的转换边界，Kernel 负责持有 AgentSession 启动所需的运行配置视图并在 mount 时注入快照（conversation root、agent server path、session profile、models、session defaults、memory、remote mode、sandbox、reasoning effort），`workspace_root` 只作为 core launch 参数由 Kernel 根据 conversation root 和 remote mode 派生，不作为独立运行配置字段，也不额外引入独立 `session_root` 概念；当 launch config 具备 agent server path 和 chat-capable model 时，`AgentSessionService` 会启动真实 `agent_server`、初始化 core `SessionActor`，把 service request 转成 core `SessionRequest` 并把 core `SessionEvent` 投影回 binding 的 event sink；未提供 agent server path 时仍保留 skeleton runner 方便新协议单测；`ChannelService` 协议收敛为 delivery、session event projection、status/error、foreground context query 转发、runtime config update ingress 和 workspace ingress；Kernel 新增单一 `UpdateRuntimeConfig` 请求，负责修改并持久化 conversation-wide runtime config 到 `services/<conversation_id>/runtime_config.json`，模型/profile、reasoning、remote mode、sandbox、memory 等影响后续 AgentSession 创建的配置不再经过独立 ControlService；WorkspaceService 由 Kernel 创建时注入 runtime config snapshot，并在 runtime config 更新时和 AgentSession 一起接收广播，并新增 OverlayFS 风格的 workspace list/read/write/delete/move 协议，Selectable 下 `auto` 访问本地 conversation workspace，FixedSSH 下 `auto` 会把 `.stellaclaw/**` 路由到本地 overlay、其它路径路由到 remote workspace，跨边界 move 会被拒绝；ChannelService 现在会把平台 workspace 请求转发给 WorkspaceService，并把 typed workspace response 投影为 `ChannelEvent::Workspace`；带 `data:` FileItem 的 incoming message 会先交给 WorkspaceService 落盘到 `.stellaclaw/attachments/incoming` 并转换成稳定 `file://` 后再投递给 foreground AgentSession；MemoryService 不再是 skeleton，新增 typed search/write/update/delete/prompt-context/maintain 协议，并复用现有 Memory v1 backend 和 workdir memory manager，conversation scope 继续写 `.stellaclaw/memory_v1/conversation`，user/public scope 继续写 `rundir/memory_v1/{user,public}`；AgentSession 会拦截 core `memory_search` / `memory_write` / `memory_update` / `memory_delete` host bridge request，转发给 MemoryService，并把响应转换回 core `ConversationBridgeResponse`，保持旧 memory tool JSON result 形态；SkillService 不再是 skeleton，启动时会把现有 `rundir/.stellaclaw/skill` runtime skill store 同步到当前 conversation workspace 的 `.stellaclaw/skill`，并提供 typed reconcile、create/update/delete persist、list 和 load 协议；AgentSession 会拦截 core `skill_create` / `skill_update` / `skill_delete` host bridge request，转发给 SkillService，并保持旧 skill tool JSON result 形态；`CronService` 注册任务时记录 `registered_by`、`channel_addr`、可选 foreground session、schedule 和 payload，避免多 foreground 下丢失任务归属，并会在持久化前拒绝空 `task_id`、非法 interval、foreground/channel id 不一致等无效注册；手动触发 cron 的协议命名为 `TriggerTaskNow`，触发后 CronService 会请求 Kernel 创建带 binding 的 background AgentSession，并以 system-origin 消息投递 task payload，同一 task 已有 pending 或 active background run 时会拒绝手动触发，interval 到点则跳过该次触发；CronService 自己也开始维护 schedule timer，当前支持 typed `CronSchedule::Manual` 和 `CronSchedule::IntervalSeconds` 两种注册；Cron-owned background session 会把 session event 回传给 CronService，Cron 记录 run completed / failed、last error 和 consecutive failure count，Kernel 拒绝 background AgentSession 创建时也会把对应 pending run 标记为 failed，并把成功结果按 `forward_result_to_foreground` 输出策略包装成 user-role actor message 投递给对应 foreground session，`store_only` 策略则只记录结果不转发；Cron task prompt payload 也从裸 JSON 收敛为 typed `CronTaskPayload`；CronService 现在把 task registry/status 持久化到自身 storage 的 `tasks.json`，并新增 `GetTaskStatus` 协议返回 active run、next due、last result/error 和 failure count。本次不改变 `config` schema，不改变 `workdir` schema。

- 移除单轮 model/tool loop 步数上限：
  `SessionActor` 不再用固定 200 步限制截断同一个 turn 的模型/工具循环，长链路任务会继续运行直到模型自然完成、外部取消、provider/tool/context 等真实错误发生，避免达到内部兜底上限后需要 `/continue` 手动续跑。本次不改变 `config` schema，不改变 `workdir` schema。

- 补齐实验性 `conversation_new` ToolBinaryService：
  新 ToolBinaryService 提供 typed ensure 协议并复用现有全局 ToolBinaryManager；AgentSession 会拦截 core `tool_binary_ensure` host bridge request，转发给 ToolBinaryService，并保持 core `ToolBinaryEnsureResponse` JSON 结果形态。ToolBinaryService 不订阅 runtime sandbox update，managed tool 目录视为稳定挂载边界。本次不改变 `config` schema，不改变 `workdir` schema。

- 补齐实验性 `conversation_new` subagent bridge：
  AgentSessionService 现在拦截 core `subagent_start` / `subagent_kill` / `subagent_join` host bridge request；`subagent_start` 通过 Kernel 动态创建 Subagent AgentSession，并把子 session event sink 指回父 AgentSession，由父 AgentSession 维护子任务状态、投递初始任务消息并按旧 subagent tool JSON 形态返回 start/join/kill 结果；pending subagent join 会在新用户消息或显式 cancel 到达父 AgentSession 时解析为 interrupted，避免前台交互被过期 join 卡住。本次不改变 `config` schema，不改变 `workdir` schema。

- 补齐实验性 `conversation_new` background_agent_start bridge：
  AgentSessionService 现在拦截 core `background_agent_start` host bridge request，通过 Kernel 动态创建 parent-bound Background AgentSession，并把初始 task 投递给 background；background 成功完成后，父 AgentSession 会把结果发给对应 channel，并作为 actor message 重新注入父 session context。Cron-owned background 仍保持 event sink 指向 CronService 的独立路径。本次不改变 `config` schema，不改变 `workdir` schema。

- 强化实验性 `conversation_new` AgentSession 查询状态：
  AgentSessionService 现在在内存中维护当前 task plan，`QueryStatus` 返回的 `AgentSessionStatus` 会带上 `current_plan`；turn completed、turn failed 和 runtime crashed 等终态事件会清空当前 plan。该状态不写入 `service_state.json`，避免把运行中 UI 状态持久化为恢复状态。本次不改变 `config` schema，不改变 `workdir` schema。

- 补齐实验性 `conversation_new` cron 和 managed agent host bridge：
  AgentSessionService 现在拦截 `cron_tasks_list` / `cron_task_get` / `cron_task_create` / `cron_task_update` / `cron_task_remove`，并以当前 AgentSession 地址作为 owner 边界转发给 CronService；CronService 新增 owner-scoped list/get/update/remove、enabled/name/description/schedule/payload patch、持久化 cron expression payload，以及 `DisableTasksForOwner`，确保每个 session 只能看到和修改自己创建的任务；Kernel 删除 AgentSession 时会先通知 CronService 禁用该 session 创建的 cron 任务。AgentSessionService 同时新增 `background_agents_list`，从自身维护的 parent-bound background/subagent 状态返回当前任务、状态、最近消息和错误。core host tool catalog 删除 `user_tell` 和冗余的单 agent 详情工具，system prompt 改为让工具调用前的 preamble / progress 消息承担面向用户的工作中回复；core 测试里也移除旧的 `snapshot_save` bridge 工具占位。本次不改变 `config` schema，不改变 `workdir` schema。

- 收敛实验性 `conversation_new` CronService 调度语义：
  CronService 不再把每个 task 的 `next_due` 当作运行队列状态维护；服务每轮根据当前时间扫描 enabled task，动态计算最近未来 wakeup。Interval task 从服务启动、注册、更新或上次触发时的内存 anchor 往后计算，持久化恢复后从当前时间重新开始，不补跑停机期间错过的 interval；cron-expression task 复用现有 `cron` + `chrono-tz` 依赖按 `expression` 和 `timezone` 计算下一次未来墙钟时间，重启后同样只调度未来触发点，不执行错过的 cron。本次不改变 `config` schema，不改变 `workdir` schema。

- 补齐实验性 `conversation_new` TerminalService：
  新 TerminalService 提供 list/get/create/terminate/input/resize/replay/attach/detach typed 协议，复用现有 PTY terminal manager，并接收 Kernel runtime config 广播以在 Selectable / FixedSSH 模式变化时清理不匹配的 terminal；attach 后的 PTY 输出会作为 typed terminal output response 推回调用方，ChannelService 也新增 terminal ingress 转发和 typed terminal event 投影，后续 WebChannel 可以直接把 HTTP/WebSocket terminal 操作投影到 channel service 请求，不再持有 terminal manager。本次不改变 `config` schema，不改变 `workdir` schema。

- 调整实验性 `conversation_new` host 启动模式：
  新增 ConversationHostRuntime，服务启动时先启动全局 ToolBinary manager，再枚举已有 conversation 并 eager restore/spawn 对应 ConversationKernel；标准 bootstrap 现在包含默认 `agent/foreground/main`，确保 ChannelService、CronService 和 foreground AgentSession 都在 Channel ingress 开始前拥有 live runtime owner，避免按消息懒加载导致 cron 或 session 生命周期缺席。HostRuntime 现在也保存每个 conversation 的 `channel/main` ingress sender 和 typed event receiver；WebChannel 新增 session-scoped `POST /conversations/{id}/foreground_sessions/main/messages` 入口，直接把 normalized `ChatMessage` 投递给 ChannelService，并把 ChannelService typed events 桥接回 Web websocket subscriber。本次不改变 `config` schema，不改变 `workdir` schema。

- 新增 `stellaclaw setup <config> <workdir> [--systemd [--user]]` 配置向导：
  向导按模型、Tooling、Memory System、Channel、确认创建五个阶段生成当前 `config` schema 的配置文件，并在 TTY 中使用轻量 ANSI 颜色区分阶段、菜单、提示、警告和成功状态；非终端、`NO_COLOR` 或 `TERM=dumb` 时自动退回纯文本。向导会在开始时检查 config 目录、workdir 和可选 systemd unit 目录权限，避免最后保存时才失败；`--dry-run` 会完整跑向导并打印 config / systemd 预览，但不写文件、不创建 workdir、不安装服务。模型阶段支持添加多个 chat model，并按 provider 只暴露对应参数；Codex subscription 会复用 Codex auth.json / `CHATGPT_ACCESS_TOKEN` 约定，并可辅助运行 `codex login --device-auth`。Tooling 阶段解释并支持 `alias:self` 工具模型目标，Brave Search 和 OpenAI image helper 会生成独立工具模型；compression 配置在向导中标为高级 override，默认显示按当前默认模型 context window 90% 计算的压缩阈值和 10% retain 预算，但不写入固定 override。Memory System 阶段说明长期记忆用途，并提示 dedupe / user compaction 应优先选择便宜可靠的 chat 模型，也允许保持本地 fallback。Channel 阶段支持 Web / Telegram 多实例。`--systemd --user` 会写入 user service 并 enable/start，`--systemd` 会写入 system service。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复结构化 tool result 文件渲染：
  `ToolResultContent.files[]` 不再在已有结构化文本时重复混入通用 `tool_result_text`，避免 token estimator 和 context compression 把文件 URI 当作普通文本；压缩请求会继续为 tool result 附件输出明确的文件省略占位符，纯文件 tool result 仍会回退渲染 URI 作为文本协议兼容。本次不改变 `config` schema，不改变 `workdir` schema。

- 移除 `grep` 文件工具：
  模型可见工具目录不再暴露独立 `grep` 工具，也删除对应本地扫描器和 fixed SSH remote Python handler；代码/路径发现统一回到 Codex 风格的 shell `rg` 路线，由 `shell_exec` 每次执行前通过 ToolBinaryManager 确保 managed ripgrep 并把其目录前置到 PATH，不再依赖命令字符串启发式判断；文件内容读取继续使用 `file_read`。文件发现协议同步改为提示窄范围 `rg -n` / `rg --files` 命令。本次不改变 `config` schema，不改变 `workdir` schema。

- 强化 `shell_exec` 输出结构和截断策略：
  `TokenEstimator` 新增 raw text / string stream 估算接口，`agent_server` 会把当前 `ModelConfig` 对应的 estimator 注入本地工具执行器；`shell_exec` / `shell_write_stdin` 新增 `max_output_tokens` 并移除模型可见的 `max_output_chars` 参数，优先按当前模型估算 token 预算做中间截断，内部只保留字符安全上限。非 TTY shell 现在分离保留 `stdout` / `stderr`，结果按 Codex 风格渲染为简洁的 LLM-readable 文本，避免把 shell 结果作为 pretty JSON 暴露给模型。`shell_exec` 的默认 `yield_time_ms` 对齐 Codex unified exec 为 10 秒，空输入 `shell_write_stdin` poll 至少等待 5 秒，避免短窗口导致命令频繁提前返回。本次不改变 `config` schema，不改变 `workdir` schema。

- 将 shell 工具结果改为结构化持久化、按目标协议渲染：
  `ToolResultContent` 新增可选 `structured` 字段，`shell_exec` / `shell_write_stdin` 现在保存 provider-neutral 的 shell 状态、输出流、截断元数据和 process id，而不是把预渲染文本塞进 `context.text`；provider、token estimator、context compression 和 Web channel 统一通过 `tool_result_text` 在发送或展示前渲染。模型可见文本进一步对齐 Codex unified exec，只保留状态、wall time、必要的 session id、输出和截断信息，避免重复 command/cwd/shell/remote 等已经存在于 tool call 参数或本地 UI 的信息。本次不改变 `config` schema，不改变 `workdir` schema。

- 收敛所有工具结果到结构化 payload：
  新工具结果统一写入 `ToolResultContent.structured`，普通文本包装为 `text_result`，普通 JSON 包装为 `json_result`，工具产物文件统一写入 `ToolResultContent.files[]`；`context.text` 和单文件 `file` 只保留为旧历史兼容输入。Web channel 会把 raw structured payload 一并下发给前端，Electron 工具卡片优先展示结构化 payload，并支持同一个 tool result 关联多个文件附件。新增 workdir `0.17 -> 0.18` upgrade，会迁移各 conversation `.stellaclaw/log/*/session.json`、`all_messages.jsonl` 和 `current_messages.jsonl` 中旧的 tool result `context.text` 到 `structured.text_result`，并把旧 `file` 合并进 `files[]`。本次不改变 `config` schema，改变 `workdir` schema 至 `0.18`。

- 收紧 `.stellaclaw/` 工具协议边界并修复 Fixed SSH 路径判定：
  common prompt 现在只保留 `.stellaclaw/` 应走文档化工具工作流的通用边界；文件工具协议明确 `.stellaclaw/` 下的文档化路径优先由 file tools 访问，且 Fixed SSH 模式下，shell 需要读取 `.stellaclaw/...` 时必须先用 `shell_make_visible` 把本地 overlay 路径复制到远端 workspace，并且只读使用，不应从 shell 修改 `.stellaclaw/`。模型也不应在文档化工具工作流范围之外自行创建 `.stellaclaw/` 路径。media load/analysis 工具现在使用 path-aware execution target，Fixed SSH 下普通 workspace 路径继续走远端，`.stellaclaw/**` 继续走本地 overlay；`apply_patch` 在 Fixed SSH 下会把远端绝对路径中的 `.stellaclaw/...` 识别为本地 overlay patch，并在本地执行前重写成 workspace-relative `.stellaclaw/...`，避免误改远端 `.stellaclaw/`；`stellaclaw-fs-tool` 的 `file_read` / `file_write` 会拒绝绝对路径或 `..` 逃逸 workspace，避免文件工具绕过 workspace 边界。本次不改变 `config` schema，不改变 `workdir` schema。

- 优化 StellaCodeX Electron PDF 预览和选区引擎：
  EmbedPDF 作为 `3rd_party/embed-pdf-viewer` submodule 接入并固定到 StellaCodeX 优化分支，Electron 开发和构建流程会自动准备本地 EmbedPDF dist 产物；PDFium worker/raw render 路径增加 ImageBitmap 传输和 worker 初始化错误透传，减少渲染结果从 worker 回主线程的拷贝成本。PDF 预览保留优化后的 Stella PDFium Glyph 引擎，增加渲染耗时统计面板和 Debug 叠层；选区引擎迁入 Electron 源码目录，新增 glyph/line/segment/lane/block 调试信息，并迭代修复跨行吸附、表格误判、段落块过度合并、复制空格/换行丢失等问题。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.26.2

- 新增 host 侧统一 ToolBinaryManager：
  `core` 只保留 managed tool binary 的请求/响应 DTO 和 conversation bridge 调用，实际下载、校验、缓存、SSH 平台探测和远端上传由 `stellaclaw` host 中的全局单例服务处理，避免每个 conversation 重复下载/上传同一工具。managed tool cache 统一位于 `$STELLACLAW_SOFTWARE_DIR/stellaclaw/tools`；未配置时软件根目录默认是 `~/.cache`，因此默认 cache 仍是 `~/.cache/stellaclaw/tools`，bubblewrap 下则通过软件根目录挂载让 agent_server 能看到同一路径结构。`stellaclaw-fs-tool` 的 local/remote 执行改为优先经该服务保证二进制存在；`shell_exec` 在检测到命令会使用 `rg` 时会通过同一服务保证官方 ripgrep 15.1.0 可用，并把对应目录注入本地或远端 PATH。远端 cache 准备阶段会先解析绝对 `$HOME` 路径并注入保底 PATH，避免 zsh 非交互环境里基础命令不可见。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复并发文件工具触发 managed binary ensure 时的 bridge 冲突：
  `tool_binary_ensure` 的 conversation bridge request id 现在每次调用都会生成唯一值，避免多个并发 `file_read` / `file_write` 同时确保 `stellaclaw-fs-tool` 时复用同一个 request id，导致 SessionRpcThread pending response 被覆盖，进而出现 `conversation bridge failed: session rpc thread stopped`。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.26.1

- 修复 `shell_exec.workdir` 显式传 `~` 或 `~/...` 时的路径展开：
  本地 shell 会把 `~` 展开到 `$HOME`，不再误当作 workspace 下的普通目录；fixed/selectable SSH remote shell 会把默认 `~` 和 `~/...` 转成 `${HOME}` 形式再 `cd`，避免 shell quote 后变成 `cd '~'` 导致远端不展开。本次不改变 `config` schema，不改变 `workdir` schema。

- 补齐 StellaCodeX iOS 选区引用和附件预览链路：
  iOS conversation model 新增 `SelectionReference` payload，composer 支持显示和移除待发送选区引用，workspace 文件预览可以把当前文件内容作为引用加入 composer；聊天附件支持跳转到 workspace 文件预览，文件预览/QuickLook 路径支持更完整的引用、分享和打开体验。本次不改变服务端 `config` schema，不改变 `workdir` schema。

# 1.26.0

- 修复命令超时后 shell 子进程残留的问题：
  `run_command_with_timeout` 在 Unix 下会把子进程放入独立 process group，超时时 kill 整个 group，避免只终止 shell 但 `sleep` / 远端命令子进程继续持有 stdout/stderr pipe，导致工具调用和 CI 测试等待到子进程自然退出。本次不改变 `config` schema，不改变 `workdir` schema。

- 新增 StellaCodeX Electron 选区引用协议：
  `ChatMessageItem` 新增 provider-neutral 的 `SelectionReference`，Web channel 入站消息支持结构化 `selection_references`，provider 发送时会渲染成 selected content block。StellaCodeX 文件预览支持从 Markdown、HTML 源码、PDF、docx/Word 版式预览和源码/纯文本选区生成引用 chip，发送时只带选区文本、文件路径、定位信息和少量上下文，不全量复制文件内容；选区引用改为右键菜单显式触发，支持引用到对话和复制选中内容。PDF 预览从 PDF.js 自绘 canvas/text layer 切换为 EmbedPDF PDFium WASM direct engine 渲染，避免公式、嵌入字体和复杂版式依赖 PDF.js text layer 时出现乱码和字体退化；PDF 选区引用后续会基于 PDFium 文本几何单独补齐。docx 预览改用 docx-preview 保留 Word 样式、分页、字体、段距、页眉页脚和图片，Mammoth 只保留为选区引用的纯文本提取 fallback；docx 页面会根据预览面板可用宽度等比缩放，避免右侧面板打开时页面被横向裁切。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 StellaCodeX Electron 演示文稿预览卡死：
  工作区文件类型识别新增 PPT/PPTX/PPS/POT 演示文稿类型，打开这类文件时不再读取二进制内容进预览 tab，也不会落到源码高亮路径；预览面板改为显示安全的下载占位。其他没有专用预览器且 `encoding` 非 `utf8` 的文件也会跳过代码渲染，避免 base64/zip 二进制内容进入 highlight.js 后卡住主线程。本次不改变 `config` schema，不改变 `workdir` schema。

- 调整 StellaCodeX Electron PDF 预览加载方式：
  PDF 文件恢复和打开时会直接加载 PDFium 预览，不再需要点击延迟加载按钮；PDF 预览工具栏新增刷新按钮，可重新从 workspace 拉取当前 PDF 并重建预览，刷新时尽量按原滚动比例恢复到刷新前的阅读位置。本次不改变 `config` schema，不改变 `workdir` schema。

- 优化 StellaCodeX Electron PDF 文本选区对齐：
  PDFium 图片预览上方的透明 text layer 现在使用 PDFium 返回的文本 bbox 高度作为字号和行高，并通过 canvas 实测文本宽度后横向缩放到 bbox 宽度；PDF 选区不再依赖浏览器原生 DOM selection，而是参考 Zotero/PDF.js 的阅读序列思路，根据页内行片段和稳定栏间空白推导 column-aware reading order，再把 pointer 起止位置映射到 PDFium text run 范围并自绘高亮 overlay。PDF 自定义选区支持系统复制快捷键；拖拽选择时会缓存 text run 几何并按 animation frame 合并更新，调整预览面板宽度时也会 debounce PDFium 重渲染，避免拖动过程卡顿。本次不改变 `config` schema，不改变 `workdir` schema。

- 强化 common system prompt 的工具调用 preamble 规则：
  common prompt 现在改为 Codex 风格的 preamble 节奏：工具工作开始时先简短确认任务并给出一到两句计划；长任务中每 1-3 个执行步骤更新一次，硬下限为 6 个步骤或 10 次工具调用内至少更新；每次更新聚焦当前结果/影响和下一步，避免标题、状态标签、重复口癖和逐条命令日志。本次不改变 `config` schema，不改变 `workdir` schema。

- 强化 system prompt 的并行工具调用偏好：
  工具效率提示现在要求在仓库探索或其他 read-heavy 工作前先整体判断所需文件、搜索、列表和命令输出，再发起一个有边界的独立工具批次；该规则覆盖 read/list/search/git inspection 和类似 shell 命令，并明确只有后续路径真正依赖前序结果时才逐个读取。本次不改变 `config` schema，不改变 `workdir` schema。

- 调整 StellaCodeX Electron URL 打开方式：
  聊天消息和文件预览 Markdown 中的 `http` / `https` / `mailto` 链接点击后默认通过系统浏览器打开；Electron main process 同时拦截 `target=_blank` / `window.open` 作为兜底，避免外部 URL 在客户端内部新开窗口或导航当前页面。本次不改变 `config` schema，不改变 `workdir` schema。

- 增强 StellaCodeX Electron 文件预览 Markdown 渲染：
  文件预览里的 Markdown 现在支持 raw HTML 标签渲染，并会按当前 Markdown 文件路径解析相对图片资源，从 workspace API 拉取后渲染为 data URL；SVG 图片也会在 utf8 内容返回时转为 `image/svg+xml` data URL 展示。本次不改变 `config` schema，不改变 `workdir` schema。

- 调整 StellaCodeX Electron 顶部渐变覆盖下的聊天布局：
  聊天内容区现在显式让出标题栏渐变安全区，短 conversation 内容不足一屏时自动底部对齐；右上角运行计划面板下移到标题栏下方，避免被渐变标题栏和顶部按钮遮挡。本次不改变 `config` schema，不改变 `workdir` schema。

- 新增 StellaCodeX Electron 本地隐藏 Conversation：
  Conversation Bar 的会话右键菜单新增“隐藏 / 取消隐藏”，隐藏状态按 server 保存到 Electron 本地 settings；被隐藏的 conversation 会从普通列表移入可折叠的“已隐藏”文件夹，隐藏当前会话时自动切到下一条可见会话。该功能只影响客户端本地展示，不删除服务端 conversation。本次不改变 `config` schema，不改变 `workdir` schema。

- 更新 README / README_zh 和 ROAD_MAP 的 StellaClaw 对外描述：
  README 与 README_zh 改用 SVG 架构图替代 mermaid，并优化图中信息层次：把 `stellaclaw` Host、`agent_server + core` 和服务器实体合并展示，把 Local / SSH workspace 拆成多个可连接的目标服务器，并把 StellaCodeX 与 Telegram/Chat/Web 等 Channel 以及 macOS / Windows / Linux 全平台同步入口并列展示。文档补齐 StellaCodeX/Web 当前已落地的 workspace、terminal、attachment preview、HTML sandbox preview、Git diff tool detail、plan/usage panel 等高级能力，并把 Dynamic ToolCatalog、Prompt Protocol、Memory v1、FileItem 附件/多模态链路、Codex subscription reasoning summary 和三跳 Remote 工作路径作为核心产品能力说明；同时补充 paper wiki / 大规模论文库整理、远程服务器实验、benchmark 驱动代码优化、长时间后台任务、论文和 artifact 开发等典型场景。ROAD_MAP 同步更新 Web channel 当前状态和对外描述时应强调的高级能力，并明确 memory 存储/检索/写入策略暂不作为产品 roadmap 展开。本次不改变 `config` schema，不改变 `workdir` schema。

- 补齐 Codex subscription reasoning summary 持久化：
  Codex websocket provider 现在会处理 `response.output_item.added`、`response.reasoning_summary_text.delta` 和 `response.reasoning_summary_part.added` 流事件，按 `summary_index` 聚合 reasoning summary，并在 `response.completed` 合并到最终 reasoning output item；如果 completed output 省略 reasoning item，也会生成 summary-only reasoning item。`ChatMessage::Reasoning` 对 summary-only Codex reasoning 会写入 `codex_summary`，密文 reasoning 仍只用于 replay，展示层继续不暴露 `codex_encrypted_content`。本次不改变 `config` schema，不改变 `workdir` schema。

- 切换 stellaclaw-apply-patch Linux release 为 musl static：
  `stellaclaw-apply-patch` 子包版本提升到 `0.1.2`，CD 中 `linux-x64` / `linux-arm64` 资产改用 `x86_64-unknown-linux-musl` / `aarch64-unknown-linux-musl` 构建但保留原 platform 名；`linux-arm64` 交叉编译显式使用 Rust toolchain 自带的 `rust-lld`，避免 GitHub Actions 用宿主 `/usr/bin/ld` 链接 AArch64 对象；remote `apply_patch` runtime 期望的 release manifest 同步切到 `stellaclaw-apply-patch-v0.1.2`，避免 Ubuntu 20.04 这类 glibc 2.31 机器命中旧动态链接二进制后无法执行。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 remote apply_patch bootstrap 依赖远端 PATH 的问题：
  fixed/selectable SSH remote 下准备 `stellaclaw-apply-patch` cache、检测远端平台、安装二进制和执行二进制前都会先注入保底 `PATH=/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin...`；cache/bootstrap 阶段的 `mkdir`、`rm`、`chmod`、`mv`、`uname` 进一步改用绝对路径或绝对路径探测，避免某些 zsh 非交互环境里基础命令因 PATH 缺失而 `command not found`。本次不改变 `config` schema，不改变 `workdir` schema。

- 新增 StellaCodeX Electron Codex 风格可配置主题：
  Electron 默认浅色/深色配色改为 Codex 风格的蓝色强调色、白色浅色背景和 #181818 深色背景；设置页外观中新增主题配色面板，可分别调整浅色和深色主题的强调色、背景、前景和对比度，并支持导入、复制和重置主题 JSON。设置持久化新增 `themeColors` 字段，但不改变服务端 `config` schema，不改变 `workdir` schema。

- 新增独立 stellaclaw-apply-patch 子软件包：
  在 `tools/stellaclaw-apply-patch` 下新增独立版本的跨平台补丁应用 CLI，支持 Codex patch 格式和 unified diff 格式，输出稳定 JSON；新增独立 CD workflow，可按 `stellaclaw-apply-patch-v*` tag 或手动触发构建 linux-x64、linux-arm64、macos-x64、macos-arm64、windows-x64 产物并生成 hash-pinned manifest，供后续 remote tool bootstrap 安装到受控 cache。本次不改变 `config` schema，不改变 `workdir` schema。

- 接入 remote apply_patch 的独立执行器：
  fixed/selectable SSH remote 下 `apply_patch` 会按远端 `uname` 选择 `stellaclaw-apply-patch` release 资产，从 GitHub release manifest 下载并校验 SHA256 后缓存到本地 `STELLACLAW_SOFTWARE_DIR` 或 `~/.cache/stellaclaw/tools`，再通过 `scp` 安装到远端 `~/.cache/stellaclaw/tools` 执行；Codex patch 格式因此不再只支持本地 workspace。本次不改变 `config` schema，不改变 `workdir` schema。

- 更新 ROAD_MAP 当前实现快照和工具边界：
  ROAD_MAP 现在补齐已经落地的 Prompt Protocol、provider visibility 过滤、Memory v1/user memory snapshot、fresh-process shell、文件工具 surface 收敛、Host coordination tools、StellaCodeX Electron 附件/预览/diff UI、skill canonical 路径和 tool schema 动态裁剪规则；同时移除旧 `glob` / `ls` / `edit`、持久 shell、`STELLACLAW.md` system snapshot 等过期描述。本次不改变 `config` schema，不改变 `workdir` schema。

- 优化 memory 工具 prompt 分层和写入触发：
  common prompt 不再用容易抑制工具调用的“answer from memory”表述，改为区分模型内部记忆和可验证的长期记忆/仓库检查；`host.memory` protocol 现在把 `memory_write` 作为 turn 结束前的持久状态维护动作，明确用户 facts、conversation facts、项目实现要求、关键文件/模块知识、长任务恢复句柄等都应在可复用时写入。`memory_search` / `memory_write` / `memory_update` / `memory_delete` 的工具 description 收敛为接口契约和参数限制，具体触发场景、scope 选择、禁止写入项和 update/delete 流程集中在 tool protocol，避免重复说明互相稀释。本次不改变 `config` schema，不改变 `workdir` schema。

- 清理工具 description 与 Prompt Protocol 重复说明：
  `user_tell` 和 `update_plan` 的工具 description 只保留直接作用，使用偏好继续放在对应 `host.*` protocol；`grep` 和 `apply_patch` description 保留搜索/补丁格式、路径和参数硬约束，编辑偏好继续放在 `file.*` protocol；`process.shell` protocol 修正旧的 `session_id` / `cmd` 噪声，改为当前 `shell_exec.command`、`shell_write_stdin.process_id` 和 `shell_stop.process_id` 语义。本次不改变 `config` schema，不改变 `workdir` schema。

- 优化 update_plan 工具说明：
  `update_plan` 的工具 description 和 `host.update_plan` prompt protocol 现在明确建议：如果首个计划步骤可以立即开始，应把 `update_plan` 和下一批相互独立的工具调用放在同一个 tool-call batch 中返回，而不是单独消耗一轮只更新计划；SessionActor 会立即应用计划并继续执行剩余工具。本次不改变 `config` schema，不改变 `workdir` schema。

- 优化 apply_patch unified diff 绝对路径处理：
  `apply_patch` 工具 description 和文件编辑协议现在明确 patch 路径必须是 workspace-relative，remote 执行时必须是 remote cwd-relative；unified diff 在执行前会扫描 `---` / `+++` / `diff --git` 文件头，若绝对路径位于当前本地 workspace 或 remote cwd 下，会自动归一化成相对路径再交给 `git apply`，若绝对路径位于工作区外或 remote 没有 cwd，则提前返回明确参数错误，避免模型先触发 `git apply` 的 `invalid path` 再重复尝试一次几乎相同的 patch。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 StellaCodeX Electron split diff 滚动条闪烁的问题：
  Git diff split 视图不再用 `ResizeObserver` 动态检测横向溢出并切换 `x-overflow` class，避免滚动条出现/消失反复触发布局测量；左右 pane 改为稳定的 CSS overflow 布局，行内容用 `width: max-content` 与 `min-width: 100%` 自然决定是否需要横向滚动，并为 pane 增加稳定 scrollbar gutter。同步纵向滚动时也会跳过小于 1px 的无意义回写，减少程序化 scroll 事件抖动。本次不改变 `config` schema，不改变 `workdir` schema。

- 统一工具 provider 可见性过滤：
  `ToolDefinition` 新增 provider visibility 元数据，工具可以在定义处声明不支持的 provider；`user_tell` 现在由自身声明对 Codex subscription 不可见，`ToolCatalog` 构建和 `SessionActor::new` 会按 `ModelConfig` 统一过滤。删除 Codex provider 侧的 `filter_tools_for_provider` 特例后，system prompt 协议注入、provider request tools 和本地执行白名单都依赖同一个已过滤 catalog，避免 provider 与工具目录之间继续耦合。本次不改变 `config` schema，不改变 `workdir` schema。

- 移除 ls 文件工具：
  删除模型可见 `ls` 工具、本地执行入口和 fixed SSH remote handler，减少文件发现语义重叠；后续目录/路径发现统一学习 Codex 使用窄范围、有限制的 `rg --files` / ripgrep shell 命令，内容搜索继续使用 `grep`，文件内容读取继续使用 `file_read`。system prompt 和 README 同步移除 `ls` 工具说明；`.stellaclaw/` broad listing 隐藏但精确路径仍可用于文件工具的规则并入文件发现协议。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 common prompt 泄漏 user_tell 工具名的问题：
  Codex subscription 的 SessionActor tool catalog 现在从源头不再加入 `user_tell`，而不是只在 provider request tools 阶段过滤；`<attachment>...</attachment>` 的通用输出协议也不再提到 `user_tell`，只说明 final answer 的附件标签格式和路径规则；`user_tell` 文本可带附件标签的说明改到 `host.user_tell` 工具协议中，并且只有 `user_tell` 工具实际启用且 provider 可见时才注入。Codex subscription 下 system prompt、工具协议、执行白名单和最终 request tools 都不应再暴露该工具名。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复工具调用历史可能缺失 ToolResult 的问题：
  `update_plan` 被 SessionActor 接管后仍会为原始 tool call 写入即时 ToolResult，恢复前端“工具结果”展示并保持 provider tool protocol 闭合；工具 batch 启动失败或 completion 返回错误时，也会为 batch 中每个未完成操作生成结构化 error ToolResult，再继续后续模型循环。对于已经写入历史的旧半开 tool call，SessionActor 会在下一次 provider request 前自动追加 synthetic error ToolResult 修复协议，避免 Codex subscription 报 `No tool output found for function call`。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 Codex cyber policy 错误阻塞上下文压缩的问题：
  provider error 分类新增 `cyber_policy`，Codex subscription websocket 的 `response.completed` / `response.failed` 错误会优先保留结构化失败类型；上下文压缩遇到 cyber policy 拒绝时，会学习 Codex hooks feedback-to-model 的思路，追加一条压缩重试反馈并自动重试一次，明确这是 benign context-compaction、禁止新增安全操作细节、只保留续写所需状态。如果仍失败，压缩器只针对 `cyber_policy` 继续做两级降噪重试：先移除待压缩历史中的全部 tool result content，再移除全部 user-role 历史消息；三层降噪后仍被拒绝时，不再把原始旧上下文继续追加进 current context，而是丢弃待总结旧区间，仅保留 recent high-fidelity 区域并视为完成压缩。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 StellaCodeX Electron 消息附件卡片不可操作的问题：
  assistant / user 消息中的 Web attachment 现在复用 workspace 文件预览和下载链路，附件卡片按文件实体样式展示，右侧提供“打开”菜单，可直接预览或下载对应文件；图片附件也可点击预览并通过同一菜单下载。本次不改变 `config` schema，不改变 `workdir` schema。

- 增强 StellaCodeX Electron HTML 文件预览：
  workspace/附件打开 `.html` / `.htm` 文件时不再只显示源码，文件预览面板默认渲染 HTML，并提供带图标的“预览 / 源码”切换；渲染预览使用 sandbox iframe 禁止脚本执行，报告类产物可以直接查看也可以切回源码排查。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 StellaCodeX Electron light theme 下发送按钮过黑的问题：
  composer 发送按钮改为使用主题 accent 色，并补齐 hover / disabled 状态，避免浅色主题里显示成黑色圆形按钮。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 StellaCodeX Electron markdown 表格列对齐不一致的问题：
  GFM table 的列对齐属性不再覆盖聊天和文件预览里的表格样式，markdown 表格单元格统一左对齐，避免文档类表格出现部分列右对齐、部分列左对齐的阅读割裂。本次不改变 `config` schema，不改变 `workdir` schema。

- 优化 StellaCodeX Electron edit/apply_patch 工具详情展示：
  编辑类工具参数中包含 `patch` / `diff` 时，不再以普通 key-value 方式展示大段补丁，而是渲染成 Git diff 风格视图，包含文件头、hunk、行号 gutter、红/绿增删行和新增/删除统计；窄宽度自动使用 unified diff，宽屏自动切换为左右 split diff。split diff 左右栏各自横向滚动，并同步纵向滚动位置，避免宽行对照时两栏被同一个横向滚动条一起带走。没有 diff 内容的工具结果继续按普通详情展示。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 Fixed SSH Remote Mode 下 Web workspace 的 `.stellaclaw` 本地 overlay：
  远程 workspace 根目录会合并显示本地 conversation workspace 的 `.stellaclaw/`，并且 `.stellaclaw/**` 的列表、读取、上传、删除、同 overlay 内移动和下载均走本地 conversation workspace；其他路径继续走远程 SSH。跨本地 overlay 和远程 workspace 的移动、混合下载会明确拒绝，避免把本地运行产物误当远端文件处理。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 Codex provider 下已过滤工具仍可能被执行的问题：
  provider 可见工具集合现在不仅用于 system prompt 和 request tools，也会在本地构造 tool batch 时作为执行白名单；如果 Codex subscription 的旧服务端会话状态或历史残留返回了 `user_tell` 这类当前 provider 已过滤工具，本地不再按全局 catalog 执行，而是返回明确的 unsupported tool 结果。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 Codex subscription streaming 文本可能不渲染的问题：
  Codex websocket 流中的 `response.output_text.delta` 现在只要最终 `response.completed.output` 没有 assistant 文本项，就会合成为 assistant `message/output_text`，即使 completed output 里已经包含 reasoning 或 tool call 也不会丢弃；如果 completed output 已经带有文本，则不会重复追加。前端因此能收到并渲染 Codex SDK 层对应的 agent message 文本。本次不改变 `config` schema，不改变 `workdir` schema。

- 引入 Prompt Protocol 层：
  system prompt 不再把文件搜索、文件编辑、shell、`user_tell`、`update_plan` 和 memory 的工具操作细节全部塞进 `common_prompt`；新增 `PromptProtocol { id, priority, required_tools, body }`，由对应工具模块声明协议内容，并按 provider 实际可见工具集合筛选后以自然语言拼接进 system prompt。协议不渲染 title 或总标题，避免额外 token；`required_tools` 保证工具未启用或被 provider 过滤时不会注入相关 prompt。同步检查并对齐第一批协议和工具 description，避免搜索、编辑、shell 与 memory 规则互相冲突。本次不改变 `config` schema，不改变 `workdir` schema。

- 调整附件输出 prompt 分层：
  `<attachment>relative/path/from/workspace_root</attachment>` 是 assistant 输出协议而不是文件写入工具协议，现已移到始终注入的 common system prompt，并明确附件路径必须是 workspace-relative、不能是绝对路径或 `file://` URI、发送时必须对 conversation workspace 可见；common prompt 同时保留 `.stellaclaw/shared/` 跨 conversation 共享目录和 `STELLACLAW_SOFTWARE_DIR` 共享软件目录说明。删除 `file_write` 相关附件协议中的 `.stellaclaw/output/` 写入建议，`.stellaclaw/` broad listing 隐藏但精确路径可用的说明改由文件发现协议注入，`attachment_make_visible` 和 `user_tell` 描述也改为只保留必要语义。本次不改变 `config` schema，不改变 `workdir` schema。

- 强化 apply_patch 后的 system prompt 行为约束：
  system prompt 学习 Codex 的 patch-first 使用方式，明确 `apply_patch` 用于定点修改，且成功后不要为了确认补丁是否应用而无意义重读已修改文件；工具未应用会报告失败，只有需要新上下文、后续命令或 formatter 可能改写文件、或验证失败需要排查时才重新读取。同时检查 system prompt 中已删除工具残留，当前未发现 `edit` / `old_text` / `replace_all` / `create_if_missing` 之类旧工具噪声。本次不改变 `config` schema，不改变 `workdir` schema。

- 移除 edit 文件编辑工具：
  删除 `edit` 字符串替换工具的模型可见定义、本地实现和 fixed SSH remote handler，避免同时暴露多套文件编辑语义；后续定点/多文件修改统一走 `apply_patch`，新建或完整写入仍保留 `file_write`。这使核心编辑路径更接近 Codex 的 patch-first 设计。本次不改变 `config` schema，不改变 `workdir` schema。

- 调整 file_read 行范围参数：
  `file_read` 的模型可见参数从 `offset` / `limit` 改为更贴近 Gemini CLI、Dirac、Cline 的 `start_line` / `end_line`，其中行号从 1 开始且 `end_line` 为 inclusive；省略 `end_line` 时默认从 `start_line` 读取最多 200 行。内部继续兼容旧的 `offset` / `limit` 调用，避免既有历史或客户端立即失效。本次不改变 `config` schema，不改变 `workdir` schema。

- 强化 grep 工具能力和搜索 prompt：
  `grep` 工具学习 Gemini CLI / Dirac / Codex 的可用设计，除匹配文件列表外新增返回 line-level `matches`，包含 `file`、`line`、`column`、匹配行文本以及可选 `before` / `after` 上下文；`context_lines` 支持 Dirac 风格的 0 到 10 行，新增 `exclude`、`max_matches_per_file`、`total_max_matches` 和 `names_only`，本地与 fixed SSH remote 文件工具保持同形输出。system prompt 现在优先引导模型使用 `grep` / `file_read`，并学习 Codex：路径模式发现不再暴露单独 `glob` 工具，而是明确允许使用窄范围、有限制的 `rg --files -g <pattern>` / ripgrep 命令；如果 `rg` 不可用，则使用有边界的替代方案。本次不改变 `config` schema，不改变 `workdir` schema。

- 移除 glob 文件发现工具：
  删除模型可见 `glob` 工具、本地执行入口和 fixed SSH remote handler，减少文件发现工具语义重叠；后续按路径 pattern 找文件统一学习 Codex 使用 `rg --files`，按内容搜索继续使用 `grep`。保留内部 glob matcher 仅用于 `grep.include` / `grep.exclude` 文件过滤。本次不改变 `config` schema，不改变 `workdir` schema。

- 强化 SessionActor 上下文压缩 prompt：
  压缩 provider request 不再使用独立 compression system prompt，改为沿用当前 session 的原始 system prompt 以保持 provider prefix/cache 一致性；压缩请求仍在 user instruction 中要求严格 JSON。prompt 吸收 Dirac 式高保真续写要求，明确压缩结果必须覆盖用户意图、需求约束、技术发现、架构决策、代码模式、涉及文件、当前状态、阻塞项、验证状态和精确下一步，同时避免复制不必要的原始历史。压缩 JSON 新增 `active_tasks`，用于结构化保留仍在运行或等待中的 `process_id`、`download_id`、`subagent` / background agent id、DSL job id 等恢复句柄及其目的、状态和下一步观察/继续/停止方式；`preserved_tool_call_ids` 保留为可选 raw tool pair 回填机制，仅在工具参数和结果无法安全摘要时使用。本次不改变 `config` schema，不改变 `workdir` schema。

- 调整 SessionActor idle 压缩触发比例：
  idle compaction 在模型输出后等待 cache 过期前做维护性压缩时，token 触发上限从当前模型上下文窗口的 40% 降到 30%；如果显式配置了更低的 `compression_threshold_tokens`，仍按更低配置提前压缩。压缩后的 recent high-fidelity 保留默认继续是压缩阈值的 10%，显式配置 `compression_retain_recent_tokens` 时仍优先生效。本次不改变 `config` schema，不改变 `workdir` schema。

- 增加 StellaCodeX Electron 多模态发送能力：
  Electron 聊天输入框支持直接 `Ctrl+V` / `Cmd+V` 粘贴剪贴板图片作为待发送附件，也支持点击左下角 `+` 打开系统文件选择器选择多个文件；待发送附件会在输入框顶部以 chip 列表展示，图片显示缩略图，点击 chip 可移除。发送时 Electron 使用 Web Channel `files` payload 传输 `data:` 文件，服务端继续 materialize 到 conversation `.stellaclaw/attachments/incoming` 后再写入标准 `FileItem`。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 StellaCodeX Electron 用户消息带图片附件时气泡过宽的问题：
  用户消息改为聊天软件常见的整行右对齐、内部气泡包内容布局，用户名在气泡外侧弱化显示；正文和图片/文件附件在同一个收缩边界内靠右排列，图片附件继续按真实尺寸和最大预览尺寸裁剪，避免文字在右侧、附件贴左侧并撑出大块空白。Token usage 也改成右侧紧凑椭圆 meta pill，cache read 主导时显示绿色点，否则显示红色点，整个 pill 都可点击展开明细，并和下一条用户气泡保持更清晰的底部间距；token pill 字号和状态点进一步收小。本次不改变 `config` schema，不改变 `workdir` schema。

- 调整 StellaCodeX Electron 运行计划展示：
  带计划的运行状态不再把进度面板压在输入框上方，当前“思考中 / 运行中”状态改回聊天流内显示；计划详情作为右侧悬浮面板显示，宽屏时放在主聊天列右边的空白区，空间不足时收进右侧并只保留透明 hover 热区，鼠标移入或聚焦时滑出；计划面板本身不再提供折叠控件。聊天流底部和输入框之间增加固定留白，避免最后一条状态/消息贴住输入框。输入框会随内容自动增高，到达最大高度后再显示内部滚动条；输入框增高时会同步刷新聊天流底部占位并保持贴底内容向上移动。本次不改变 `config` schema，不改变 `workdir` schema。

- 清理客户端目录布局：
  Electron 客户端从 `apps/stellacode2` 迁移到 `apps/stellacodeX/electron`，发布 workflow、文档和 SSH 更新固定路径同步切换；原 `apps/stellacodeX/apple` 迁移为 `apps/stellacodeX/ios`，作为后续 iOS 原生客户端入口。Electron 与 iOS app icon / launch logo 同步改用 `apps/stellacodeX/shared/assets/icons/stellacodex/` 下的新 Logo。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 StellaCodeX macOS 空 conversation 状态下展开右侧概览可能闪退的问题：
  Web API client 的 `invalidateTransport()` 不再永久 invalidate 底层 `URLSession`，改为取消当前 session tasks 并关闭 SSH tunnel，避免后续 SwiftUI 视图重建触发 `loadInitialData` 时复用已经 invalidated 的 session。`AppViewModel.loadInitialData` 增加运行中和已完成 guard，设置页保存 profile 后使用强制 reload；profile 切换时只关闭旧 client transport，不会误关新 client。本次不改变 `config` schema，不改变 `workdir` schema。

- 重构 shell 工具默认执行语义：
  `shell_exec` 现在每次启动 fresh process，默认 `tty=false`、stdin 关闭、不复用隐藏 shell，并以 child process exit 作为完成信号，不再注入 sentinel；运行超过 `yield_time_ms` 时返回 `process_id`，后续通过 `shell_write_stdin(chars="")` 观察输出或 `shell_stop` 停止。`tty=true` 时才分配 PTY 并允许非空 `shell_write_stdin` 写入；`login=true` 使用 shell `-lc` 加载用户 profile 环境。移除模型可见 `shell_observe` / `shell_close`，新增 `shell_stop`。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.25.8

- 增加 Memory v1 独立 Token Usage 统计：
  MemoryService 会把 dedupe provider 调用和 user memory provider compaction 调用记录到对应 scope 的 `usage.jsonl`；Conversation status 用量汇总新增 `memory`、`user_memory_compaction` 和按天聚合的 `user_memory_compaction_daily`，Web `/api/conversations/{id}/status`、Channel status 事件和 Telegram `/status` 都能看到当前 conversation 触发的 memory API 用量以及全局 user memory 压缩每日用量。workdir schema 升级到 `0.17`，为 `rundir/memory_v1/user|public/usage.jsonl` 和 `conversations/<id>/.stellaclaw/memory_v1/conversation/usage.jsonl` 初始化 usage log。本次不改变 `config` schema，改变 `workdir` schema。

# 1.25.7

- 实现 Memory v1 第一批能力：
  新增 `memory_search` / `memory_write` / `memory_update` / `memory_delete` host bridge tools，并接入 Conversation host service registry。Host 侧新增文件型 `MemoryService`，按 `rundir/memory_v1/user`、`rundir/memory_v1/public` 和 `conversations/<id>/.stellaclaw/memory_v1/conversation` 保存 entries；entry 使用短 id（`u_N` / `p_N` / `c_N`），`MemoryBackend` trait 统一 `write` / `update` / `delete` / `search` / `prompt_context` 接口，当前文件型 `MemoryService` 已实现该接口。新增 workdir 级共享 `WorkdirMemoryManager` 线程和轻量 `MemoryClient`，Conversation 持有 clone，并把 `user` / `public` scope 的写入、更新、删除和 public search 投递给该线程；`conversation` scope 继续使用本地 backend，Conversation 合并 conversation/public 两路 search 结果。`memory_search` 返回预算内的 `id` / `scope` / `subject` / `text` / `tags` / `updated_at` / `score`，并支持按 `conversation` / `public` scopes 过滤。搜索默认返回 5 条、最多 20 条，会优先当前 conversation、裁剪单条和总文本预算、去掉明显重复文本，并更新返回条目的 `last_accessed_at`。`memory_search` 的本地检索已从简单 overlap 升级为 `jieba-rs` 中英文分词、BM25Okapi lexical rank、本地 hashed dense feature vector cosine rank、RRF 多路融合和 subject/alias/tag/conversation_id metadata boost；内部 searchable document 现在包含 `id` / `scope` / `subject` / `aliases` / `text` / `tags` / `conversation_id` / `updated_at`，`public/subjects.json` 也改为记录 subject、aliases、entry ids、last_seen_at 和短摘要。`memory_write` 对外只返回成功或失败；内部 consistency judge 现在固定输出 `decision` + `actions`，actions 只允许 `touch` / `update` / `delete` / `insert`，会先校验候选 id 和大小限制再事务式应用并写 audit log。配置 `memory.dedupe_model_alias` 时，写入判重会复用现有 Provider request/retry 链并要求小模型返回严格 JSON；Provider 调用失败、输出非法或 action 不合法时，本次写入失败且不改变 memory。未配置 dedupe model 时使用本地一致性判定器消融同 subject 或高相关旧条目。MemoryService 现在限制单条 entry、候选数、搜索返回预算、每个 scope 的 active entry 数和 `entries.jsonl` 原始大小；写入候选数和搜索返回总字节预算接入 `memory.write_candidate_limit` / `memory.tool_result_max_bytes` 配置；`rundir/memory_v1/user/compaction.json` 会初始化 user memory 后台压缩状态，并按 rendered user memory bytes 标记 `idle` / `dirty` / `hard_pending`，`manifest.json` 同时记录 raw size 和 rendered size；user memory 压缩状态机现在支持失败后记录 `last_error`、递增 `attempts`、设置 `next_retry_at`，每日 soft compaction 记录 `last_soft_compaction_at`，以及在两次 hard compaction 输出未变短后写入 `threshold_override_bytes`；hard threshold 当前会立即运行 Provider compaction 或本地重复过滤 fallback，本地 fallback 只删除完全重复的 active user entries，无法变短时按 no-shrink 路径提高本次阈值；Provider compaction 调用链和输出应用层已落地：配置了 `memory.user_compaction_model_alias` 时，hard pending 和 daily soft dirty 会用该 model 走现有 Provider request/retry 链，输出条目列表会保留已有 `u_N` id、为新增条目分配短 id，并在非法 id / 重复 id / 超长条目时拒绝整次应用；Provider hard 失败会进入 `retry_waiting`，到期后由 WorkdirMemoryManager 周期维护重试，Provider soft 失败会保持 `dirty` 并等下一天重试，未配置 compaction model 时 hard 使用本地安全 fallback。删除会从 active records 中移除条目并保留 audit，避免长期膨胀。system prompt 新增 Memory Instructions，明确何时写 long memory 以及 `user` / `conversation` / `public` scope 的边界；启用 memory 时，`rundir/memory_v1/user/entries.jsonl` 的 active user memory 会渲染成紧凑 `User Memory Snapshot`，作为 current system prompt snapshot 的稳定组成部分；user memory snapshot 变化时，下一次用户消息前的 runtime notice 使用类似 git diff 的紧凑增量，不再全文重复刷新。`.stellaclaw/STELLACLAW.md` 不再作为 memory snapshot 读入 system prompt；`USER.md` 也不再全文常驻注入，只注入路径、bytes、hash 等 meta snapshot，变化通知只包含 meta；需要具体 profile 内容时由模型按需读取 `.stellaclaw/USER.md`。普通 provider request 不自动注入 conversation/public memory；模型需要时显式调用 `memory_search`。触发上下文压缩时，SessionActor 会各取 20 条 conversation/public memory 候选，按相关性分数和更新时间排序，并用模型 context window 的 3% 预算裁剪成 compaction-only 背景，不写入 `all_messages.jsonl` / `current_messages.jsonl`。压缩 prompt 现在要求严格 JSON，compaction provider request 使用专用短 system prompt 而不复用聊天完整 system prompt，系统把 `summary` / `current_state` / `plan` 渲染成 compacted provider history block，并按 `preserved_tool_call_ids` 回填仍需保留的 tool call 参数和 tool result；当前 `update_plan` 不再额外拼接成一条 provider context 消息，压缩 JSON 的 `plan` 字段仍由模型描述需要延续的下一步。压缩模型返回非 JSON 或压缩失败时会发出 `CompactFailed` 事件，由 Conversation 转成 foreground Channel 可见错误。config schema 升级到 `0.12`，新增默认关闭的 `memory.enabled=false`，并校验 `memory.user_compaction_model_alias` / `memory.dedupe_model_alias` 必须指向 chat-capable model；关闭时 memory tools 返回 `memory_disabled`，且不启动 WorkdirMemoryManager、不观察 user memory snapshot、不做压缩前 memory recall。system prompt 明确 `.stellaclaw/output/`、`.stellaclaw/attachments/`、`.stellaclaw/shared/` 是可直接使用的特殊路径，并说明 `.stellaclaw/` 普通 `ls` 隐藏但精确路径对文件工具有效；新 workspace seed 会预创建 `.stellaclaw/attachments/` 和 `.stellaclaw/output/`，`.stellaclaw/shared/` 继续指向 workdir 级 `rundir/shared`。workdir schema 升级到 `0.16`，新增 memory_v1 目录初始化；本次改变 `config` schema 和 `workdir` schema。

# 1.25.6

- 更新 Memory v1 TODO 设计：
  memory scope 命名统一为 `user` / `public` / `conversation`，`memory_search` 默认搜索当前 conversation 与 public，user memory 作为 system prompt snapshot 中的长期记忆由后台压缩维护，并在两次 compact 之间保持稳定；`memory_write` 写入时由工具端自动做同 scope / 同 subject 候选检索，并交给轻量判重模型或本地小模型输出 `decision + actions`，用 `touch` / `update` / `delete` / `insert` 消融旧条目的重复、冲突和过期事实。`memory_write` 对外只返回 `status="success"` 或 `status="failure"` 以及失败原因，内部候选、模型输出和 actions 只写 audit log；system prompt 需要强化只有稳定、未来会复用、turn 结束后仍有价值的信息才写入，并按 user / conversation / public scope 保存对应类型的知识。核心抽象改成 `MemoryService` / `MemoryClient` / `WorkdirMemoryManager` / `MemoryBackend` 分层，和 host service registry 架构对齐。TODO 已按长期记忆和短期记忆两条线重排：长期线负责 memory tools、文件布局、search 和 user compaction，并只列出 memory 直接相关路径；短期线继续使用现有 `update_plan`、provider current context compression，并明确达到阈值时在待压缩 provider context 末尾追加一条 `role="user"` 的压缩请求消息，让模型返回结构化 JSON compaction result，其中工具结果保留改为 `preserved_tool_call_ids`，并由系统回填对应 tool call 参数和 tool result，不在 compacted block 中渲染工具结果列表，也不再从压缩流程输出长期记忆候选。正常 provider context 由当前 system prompt snapshot、按预算检索出的 conversation/public memory、compacted provider history block、当前 plan 和当前 user message 拼接。TODO 同时标记当前已落地的 HostServiceRegistry 骨架、SessionStateStore 历史文件、USER.md runtime metadata 读取、`update_plan` 内存态、provider context compression，以及现有 compaction request 的尾部 `role="user"` 请求形态。本次不改变 `config` schema，不改变 `workdir` schema。

- 调整默认上下文压缩高保真保留比例：
  未显式配置 `compression_retain_recent_tokens` 时，默认 recent high-fidelity context 保留预算从压缩阈值的 18% 调整为 10%；PartyClaw 兼容配置迁移在缺少 `recent_fidelity_target_ratio` 时也按 10% 生成 retain recent。示例配置同步把 `compression_retain_recent_tokens` 调整为阈值的 10%。显式配置值继续按用户配置生效。本次不改变 `config` schema，不改变 `workdir` schema。

- 引入 Conversation host service 路由骨架：
  Conversation 的 host bridge action 处理拆成 `HostServiceRegistry` 路由和 Core / ManagedSession / Skill / Cron service 边界，先保持现有工具行为不变，为后续 MemoryService、WorkdirMemoryManager 和 ask conversation 等 host service 接入留出解耦入口。本次不改变 `config` schema，不改变 `workdir` schema。

- 调整 `update_plan` 的所有权到 SessionActor：
  `update_plan` 仍作为 host tool 暴露，但 plan payload 现在由 SessionActor 解析、校验和持有，并通过 SessionEvent 向 Conversation 推送 plan/progress snapshot；Conversation 不再维护 `session_plans` 数据源，只把事件投影到 channel progress。plan 不写入独立持久状态，正常 turn 完成会清空，用户中断不会主动清空；active context compression 时会临时把当前 plan 拼入压缩后的 provider context，turn 完成清空时移除。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 Remote Mode 远程文件能力可能无限等待的问题：
  fixed SSH Remote Mode 下，模型可见的远程 `ls` / `glob` / `grep` / `file_read` / `file_write` 等 JSON 文件工具现在有 60 秒 SSH 总超时，远程 `apply_patch` 这类 stdin SSH 命令有 300 秒总超时；远端 Python 文件脚本内部也增加操作级 timeout，且只读操作会做有限 retry，写入/删除/移动类副作用操作只超时不重试。Web workspace 的远程 list/read/upload/delete/move/download 路径也增加 60 秒 SSH 总超时和对应的 Python 内部 timeout，避免远端网络挂载目录在 `stat` / `readdir` 卡住时让一次工具调用或文件浏览请求无限挂起。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复已升级到 `0.14` workdir 的旧 special link：
  workdir schema 升级到 `0.15` 时会修复已经写入 `0.14` 的运行目录，把仍留在 `rundir/.skill/` 和 `rundir/skill_memory/` 的 runtime skill 数据迁移到 `rundir/.stellaclaw/skill/` 与 `rundir/.stellaclaw/skill_memory/`，并把 conversation 下指向旧 `rundir/skill_memory/` 的 stale `.stellaclaw/skill_memory` symlink 改到新的 canonical runtime target。旧路径兼容逻辑保留在 upgrade 内，运行时附件解析只接受新的 `.stellaclaw/...` canonical 路径。本次不改变 `config` schema，改变 `workdir` schema。

- 统一 conversation workspace 的特殊本地路径到 `.stellaclaw/`：
  workdir schema 升级到 `0.14` 时会把旧根路径 `.output/`、`attachments/`、`shared/`、`.skill/`、`.skill_memory/`、`skill_memory/` 和 `STELLACLAW.md` 迁移到 `.stellaclaw/output/`、`.stellaclaw/attachments/`、`.stellaclaw/shared/`、`.stellaclaw/skill/`、`.stellaclaw/skill_memory/` 和 `.stellaclaw/STELLACLAW.md`，并兼容 `.stellaclaw/stellaclaw_shared/` 到 `.stellaclaw/shared/`；runtime backing 的 `rundir/.skill/` 和 `rundir/skill_memory/` 也迁移到 `rundir/.stellaclaw/skill/` 和 `rundir/.stellaclaw/skill_memory/`。迁移会合并目录并保留冲突副本，避免覆盖已有本地数据；新上传附件、shared 入口、system prompt、runtime skill store 和 runtime memory 读取均改用新的 canonical 路径。本次不改变 `config` schema，改变 `workdir` schema。

- 重构 fixed SSH Remote Mode 的 workspace 可见性：
  fixed SSH Remote Mode 不再依赖 `sshfs` 挂载普通工作区；文件、搜索、补丁、下载和 shell 工具默认作用于远程 workspace，本地 conversation workspace 只保留附件、channel 可下载产物、runtime 元数据和特殊本地文件。新增 `shell_make_visible` / `attachment_make_visible` 用于按同一个 workspace-relative 路径在本地与远程之间同步可见性；升级到 workdir schema `0.13` 时只会 materialize fixed SSH Remote Mode 下必须留在本地的关键路径（`.stellaclaw/`、`.output/`、`.skill/`、`.skill_memory/`、`attachments/`、`shared/`、`skill_memory/`、`STELLACLAW.md`），普通 workspace 文件继续留在远端。本次不改变 `config` schema，改变 `workdir` schema。

- 修复 Telegram foreground final reply 附件标签未发送的问题：
  foreground turn 完成时，如果 assistant 最终消息里包含 `<attachment>...</attachment>`，Conversation 会先按 workspace / shared 路径解析成 `OutgoingAttachment` 再交给 Telegram 发送，避免只把标签当普通文本展示。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复了一个shell工具在RemoteSSH模式下的BUG。

- 修复 StellaCodeX Apple turn 内工具结果与失败展示问题：
  工具过程组现在使用稳定 identity，新的 tool result 到达时不会重置用户已展开的中间结果；timeline 布局变化只在用户本来位于底部附近时才自动贴底，避免运行中折叠/展开把滚动位置拉飞；Apple WebSocket realtime error 解码兼容 Web channel 的结构化 `error` payload，并在 provider / turn 失败时显示可见系统错误。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 StellaCodeX iOS 模型切换后的导航与状态展示：
  在 Actions 里的 Model 页面选择模型后会直接返回 Chat，并主动重建当前 conversation 的 foreground realtime 连接；连接层的 transient `Stream error` 不再作为标题下状态或聊天系统错误残留，provider / turn 失败仍会显示为可见错误。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 StellaCodeX Apple timeline 手动展开工具列表时的滚动问题：
  用户展开/折叠 tool call / tool result 列表不再触发自动贴底滚动，避免 macOS 执行过程中 `LazyVStack` 动态高度变化把 scrollbar 推到空白渲染区域；新消息到达时的靠近底部自动跟随逻辑保持不变。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.25.0

- 初始化 StellaCodeX Apple 原生客户端：
  新增 `apps/stellacodeX/apple` SwiftUI/Xcode 工程，支持 macOS 和 iOS 共享核心协议与平台化 UI，覆盖会话列表、聊天流、Markdown/工具/附件展示、SSH Proxy、模型与设置、文件浏览/预览、终端会话、图片/文件附件发送、主题/语言配置、启动图和应用图标等 Apple 客户端基础能力。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 Web channel 消息投递失败不可见的问题：
  `POST /api/conversations/{id}/messages` 成功进入队列后，如果 dispatcher 后续未能把消息投递到对应 conversation runtime，现在会通过现有 WebSocket `error` 事件回传到该 conversation；Apple 客户端收到 realtime error 时会在聊天流中展示系统错误，避免只出现本地乐观消息却看不到失败原因。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.24.2

- 调整 Stellacode 2 SSH 更新通道为解压覆盖安装：
  SSH test/stable 固定路径现在拉取平台压缩包而不是安装器，Windows/macOS 使用 `.zip`，Linux 使用 `.tar.gz`；客户端下载后会在退出当前进程后解压到临时目录、覆盖当前安装目录并重新启动，临时目录正常完成后自动删除。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.24.1

- 修复 Stellacode 2 GitHub 自动更新安装包资产路径：
  Electron builder 现在固定生成与 `latest.yml` 一致的安装包文件名 `Stellacode-Setup-<version>.<ext>`，并将桌面客户端发布配置指向当前 `JeremyGuo/StellaClaw` 仓库，避免 Windows 自动更新请求 `Stellacode-Setup-<version>.exe` 时命中 404。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.24.0

- 发布 Stellacode 2 桌面客户端显示与更新体验改进：
  Settings 新增显示字号和整体 UI 缩放配置，终端字号跟随显示设置，高 DPI / resize 场景会刷新窗口 chrome metrics；更新页保留 GitHub Release 自动更新通道，并新增可选 SSH 固定路径通道，可通过当前 SSH profile 拉取 test 或 stable 安装包。本次不改变 `config` schema，不改变 `workdir` schema。

- 发布 StellacodeX Android 聊天与会话列表体验改进：
  优化聊天页滚动、输入和会话列表展示细节，补齐聊天 ViewModel 状态处理。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.23.5

- 修复 Web workspace 图片预览被截断的问题：
  `workspace/file` 现在支持图片文件通过 `full=true` 读取完整原图，不再受普通文件 25 MiB 预览上限影响；非图片文件仍走 bounded preview，由客户端负责在超大文件时提示用户改用下载/打开。本次不改变 `config` schema，不改变 `workdir` schema。

- 增加 Web workspace 删除和重命名/移动接口：
  Web channel 现在支持 `DELETE /api/conversations/{id}/workspace?path=...` 和 `PATCH /api/conversations/{id}/workspace`，用于原生客户端文件管理。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.23.4

- 修复 Codex subscription 原生图片生成工具未挂入请求的问题：
  当当前 Codex subscription 模型支持 `image_out` 且图片生成工具目标为当前模型时，工具 catalog 现在会注册 provider-native `image_generation`，并在 Responses payload 中发送 `{ "type": "image_generation", "output_format": "png" }`；该路径不引入本地 image generation function tool，也不暴露 `image_generation_stop`。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.23.3

- 发布 StellacodeX Android `0.1.24` 稳定版：
  将 Android 客户端从 `0.1.24-rc.13` 提升为稳定版本，包含近期聊天体验修复：消息可选择复制、本地时间显示、隐藏 runtime metadata、工具消息分组折叠、分页和滚动锚点修正、按真实聊天项加载历史、用户名显示/发送配置，以及用户消息右侧气泡样式。本次不改变 `config` schema，不改变 `workdir` schema。

- 发布 Stellacode 2 桌面客户端更新：
  Server settings 新增可配置 `Username`，默认 `workspace-user`；普通聊天消息的 `user_name` 使用该用户身份，不再硬编码客户端名 `Stellacode`，为后续多用户共享 conversation 做准备。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.23.2

- 增加 StellacodeX Android 应用内 APK 更新入口：
  Settings 中新增 App updates，可通过当前 SSH profile 从服务器固定路径下载并安装 `test` 或 `stable` APK。测试版固定路径为 `/home/liuhao/ClawParty/apps/stellacodeX/android/dist/stellacodex-android-test.apk`，稳定版固定路径为 `/home/liuhao/ClawParty/apps/stellacodeX/android/dist/stellacodex-android-stable.apk`；下载和安装流程会写入 Logs，APK 通过 FileProvider 交给系统安装器。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.23.1

- 增加 StellacodeX Android 调试日志界面：
  会话列表新增 `Logs` 入口，日志文本可选择、可一键复制、可清空，并持久保留最近 600 行。Android realtime WebSocket 连接、订阅 ack、消息帧、close/failure、重连调度和异常帧现在都会写入本地日志，便于定位打开会话后持续 reconnecting 但界面看不到错误的问题。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.23.0

- 增加 tool batch 内的并行/串行调度：
  工具定义现在带有执行并发属性，并会在模型可见 description 中明确标注 parallel 或 serial；批处理执行器会并发运行连续的 parallel 工具，遇到 serial 工具时按原始顺序独占执行。文件写入、文本编辑、apply_patch、全部 shell 工具、下载启动/取消、媒体 stop、Host bridge 状态变更和 skill 持久化等工具标为 serial，读文件、搜索、列表、web/media 查询等保持 parallel。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 batch 内工具失败互相影响的问题：
  单个工具返回 `LocalToolError` 或 panic 时只生成该工具自己的 error result，不再跳过同一 batch 后续工具；interrupt 语义仍会把未完成工具标为 interrupted。本次不改变 `config` schema，不改变 `workdir` schema。

- 增加并维护 Codex 工具差异设计文档：
  新增 [CODEX_TOOL_DIFF.md](CODEX_TOOL_DIFF.md)，记录当前 shell / 文件编辑工具与 Codex unified exec / apply_patch 的实现差异；已落地且不再构成差异的旧方案说明会从该文档中移除，只保留当前实现状态和未实现清单。本次不改变 `config` schema，不改变 `workdir` schema。

- 优化 `apply_patch` patch 格式：
  `apply_patch` 现在支持 `format=auto|codex|unified`；本地 workspace 可直接使用 Codex 风格的 `*** Begin Patch` / `*** End Patch` envelope 表达 add/delete/update/move 文件操作，并在写入前做路径和上下文校验。统一 diff 路径继续走 `git apply`，SSH remote 暂保持 unified diff。本次不改变 `config` schema，不改变 `workdir` schema。

- 修正 Shell 工具优化方案文档：
  [CODEX_TOOL_DIFF.md](CODEX_TOOL_DIFF.md) 中的 shell 路线改为 `shell_exec` 主入口和长期 shell handle 优先，明确 `export`、`cd`、venv activate 等状态应保存在可复用 shell 进程里；`shell_open` 收敛为内部能力，不暴露 mode 参数，默认使用长期 shell。同时补充 Codex unified exec 的等待、deadline、退出 token、post-exit 收尾、空输入观察、`yield_time_ms` 范围、输出截断、`shell_exec` 前清空 pending buffer、截断后中间内容获取边界、per process 的 PTY snapshot 判定，以及 shell_id 对 remote 的绑定规则。本次不改变 `config` schema，不改变 `workdir` schema。

- 重构模型可见 Shell 工具：
  移除旧 `shell` 工具，新增 `shell_exec`、`shell_observe`、`shell_write_stdin` 和 `shell_close`。`shell_exec` 默认创建或复用长期 PTY shell，保留 `export`、`cd`、venv activate 等 shell 状态；新命令写入前会清空 pending output buffer，并通过 sentinel 判断命令完成和 exit code。`remote` 只在创建/选择 shell 时参与绑定，后续带 `shell_id` 的调用自动复用同一执行位置；输出按 `max_output_chars` 做 Codex 风格中间截断。Shell runtime 现在维护 terminal screen state，解析 SGR 颜色、光标移动、清屏、回车覆盖和 alternate screen：普通 plain text / 彩色日志继续返回 stripped `output`，屏幕型 TUI 输出返回 `terminal_snapshot.visible_text`。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.22.8

- 增强 StellacodeX Android realtime 长连接保活与诊断：
  OkHttp WebSocket 增加 15 秒主动 ping；SSH Proxy tunnel 增加 JSch server alive keepalive；WebSocket close/failure 状态显示 close code、异常类型、异常消息和 HTTP 状态，便于定位一直 reconnecting 的原因。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.22.7

- 显示 StellacodeX Android tool call explanation：
  Android 聊天消息现在会从 tool call payload 中提取模型返回的 `explanation`，并在 Assistant 消息正文区域显示；tool call / tool result 详细 payload 仍默认折叠，点击后展开。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.22.6

- 优化 StellacodeX Android 聊天阅读体验：
  Assistant tool call / tool result 卡片默认折叠，点击后再展开查看 payload；进入会话后初始消息列表直接定位到底部，不再播放从顶部滚到底部的大量滚动动画，后续新增消息仍保持自动滚动。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.22.5

- 增强 StellacodeX Android 会话内实时同步：
  Android WebSocket URL 现在和 Stellacode 2 一样附带 `token` query 作为认证兜底，同时保留 Authorization header；聊天页恢复可见 realtime/progress 状态，并增加轻量 backfill 同步兜底，按最后已同步 message id 拉取缺失消息，避免 WebSocket push 丢失时只能手动刷新。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.22.4

- 稳定 StellacodeX Android 安装包签名与命名：
  Android debug / release variant 现在使用仓库内固定的 StellacodeX dev release signing key，GitHub Release 改为构建 signed release APK，并上传 `stellacodex-android-v<versionName>.apk`，文件名不再带 `debug`。已安装旧 debug keystore 包仍需卸载一次，后续同包名升级可直接覆盖安装。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.22.3

- 将 StellacodeX Android APK 纳入 GitHub Release 自动发布：
  Release workflow 现在会构建 `apps/stellacodeX/android` debug APK，并把 `stellacodex-android-debug-v<versionName>.apk` 作为 release asset 上传；同时继续忽略本地 Android 构建产物和 `restart.log`。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.22.2

- 修复 Stellacode 2 workspace 下载较大文件失败的问题：
  Web workspace download 的压缩归档上限从复用 10 MiB 上传限制改为独立 50 MiB 下载限制，上传限制仍保持 10 MiB；避免下载 Android debug APK 等较大二进制产物时报 `download archive exceeds 10485760 byte limit`。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.22.1

- 初始化 StellacodeX Android 原生客户端：
  `apps/stellacodeX/android` 现在包含原生 Android Gradle/Compose 客户端，支持连接配置、会话列表、创建会话、聊天收发、WebSocket 实时同步、最新 30 条历史加载、轻量 Markdown/代码块、附件预览和工具调用/结果展示；构建产物不纳入 git 跟踪。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.22.0

- 收敛 provider transient retry 策略：
  provider 内部不再各自按 `retry_mode` 重试聊天请求，统一由 shared retry helper 按 `ModelConfig.retry_mode` 处理 `Request`、WebSocket、subprocess、429、5xx、rate limited、provider unavailable 等 transient error；`SessionActor` 会记录 `provider_request_retrying` 日志，Codex subscription 在重试前仅负责清理 cached socket。`retry_mode` 省略时默认使用 `random_interval { max_interval_secs: 1, max_retries: 5 }`，示例配置同步更新。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 Stellacode 2 长对话输入卡顿、打字机回归和 IME Enter 行为：
  Composer draft 下放到聊天组件本地状态，历史消息和工具组渲染增加 memo / offscreen skip，避免每次键入都重渲染整页 Markdown；打字机新消息判断不再被本地 optimistic message 的非数字 id 阻断；IME 只屏蔽同一次 composition Enter 产生的后续 keydown，并阻止它插入换行，快速二次按 Enter 仍会发送。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.21.12

- 执行并固化 `ChatMessage` / `FileItem` 标准：
  Web REST message 入口现在会把 incoming `data:` file item materialize 到 conversation `attachments/incoming` 后再以 `file://` 写入用户 `ChatMessage`，并为文件路径补充可推断的 `media_type`。`AGENTS.md` 新增 ChatMessage / FileItem 约定，要求后续相关实现遵守 `chat_message.md` 与 `file_item.md`。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.21.10

- 增加 `FileItem` payload 标准文档：
  `core/src/session_actor/file_item.md` 现在规定 `ChatMessageItem::File` / `ToolResultContent.file` 的 URI、MIME、模态、materialize、request-time normalization、crashed state 等约定，并在 `chat_message.md` 中引用。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.21.9

- 澄清 Codex subscription 文档里的用户图片输入形态：
  `codex_subscription.md` 现在明确真实用户上传图片通常先 materialize 成本地文件，并以 `file://` `FileItem` 持久化；Codex 请求中的 `data:` URL 是 request-time media normalization 的传输形态。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.21.8

- 重写 Codex subscription 消息翻译文档：
  `codex_subscription.md` 现在先完整说明 `ChatMessage` 格式，再用多组 user / assistant / tool call / tool result / assistant image / reasoning 示例展示如何转换成 Codex Responses `input` 消息。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.21.7

- 扩充 `ChatMessage` 文档的期望转换规则：
  `chat_message.md` 现在补充说明用户输入、assistant 文本、provider tool call、runtime tool result、assistant 生成文件和 provider reasoning 应如何转换成正确的 `ChatMessage`，尤其明确 `ToolResultContent.context` 与 `ToolResultContent.file` 的使用边界。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.21.6

- 增加 `ChatMessage` 数据结构文档：
  在 `core/src/session_actor/chat_message.md` 中说明 `ChatMessage`、各类 `ChatMessageItem`、token usage、file state 和 tool result 字段的语义及序列化形态。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.21.5

- 增加 provider 消息翻译文档：
  在 `core/src/providers/` 下为各 provider 补充 `<provider>.md`，说明不同 `ChatMessage` item、角色和模型 capability 下会如何转换成对应 provider 请求消息。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.21.4

- 收敛 Codex subscription assistant 图片上下文回放：
  assistant 顶层图片不再额外构造 top-level `image_generation_call`；模型支持 `image_in` 时只追加一条临时 `role=user` 图片上下文消息，使用 `[Generated by Assistant]` 作为短文本标记，并把图片作为 `input_image` 回放给 Codex。这样保留已验证有效的视觉上下文路径，并避免为生成 `image_generation_call.result` 再读取一次图片文件。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.21.3

- 修复 fixed SSH workspace 在 sshfs 刚断开时会让 conversation 线程启动失败的问题：
  启动阶段如果挂载探活后紧接着创建 workspace seed 遇到 `Transport endpoint is not connected`，现在会 lazy unmount、重新挂载并重试一次，避免前端消息停在“等待模型开始处理”。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.21.2

- 修复 Codex subscription 回放 assistant 图片输出后仍无法视觉理解的问题：
  assistant 顶层图片现在仍会回放为 top-level `image_generation_call` 保留生成历史，同时紧跟一条临时 `role=user` 图片上下文消息，让 Codex 以 `input_image` 形式真正看到图片像素。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.21.1

- 修复工具结果图片和 Codex 原生图片生成的上下文回放：
  各聊天 provider 在发送请求时，如果 `ToolResult` 携带图片，会按当前模型能力在该 tool result 后追加一条临时 `role=user` 图片消息；模型不支持 `image_in` 时改为追加文本引用。Codex subscription 在回放 assistant 顶层图片输出时，不再降级成 `output_text` 路径，而是在模型支持图片输入时回放为 top-level `image_generation_call`。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.21.0

- 收紧 system prompt 中 `shared/` 目录的使用边界：
  system prompt 现在明确要求只有在用户要求，或用户明确说明产物需要共享给其他 agent / conversation 时，才使用 workspace 下的 `shared/` 目录；普通会话产物继续留在当前 workspace。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.20.0

- 增加 conversation 级 reasoning effort 控制命令：
  Web 和 Telegram channel 支持 `/reasoning` 查看当前配置，支持 `/reasoning low|medium|high|xhigh|default` 调整当前 conversation 的 reasoning effort；切换后会重启 foreground session，使后续模型请求使用新的 conversation override。Stellacode 2 命令面板同步加入 reasoning effort 命令。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 Stellacode 2 原生图片生成结果重复展示：
  Web REST message 中结构化 `FileItem` 已经通过 message item 渲染为附件卡片，前端不再把同一个 attachment 追加渲染为尾部附件，避免单个 assistant file item 在聊天区显示成两张图片。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 fixed SSH workspace 下媒体加载卡死：
  `image_load`、`pdf_load`、`audio_load` 在 fixed SSH 模式下不再通过 sshfs mount 读取媒体文件，而是直接通过 SSH 从远端路径拷贝到本地 session 输出目录后再加载；sshfs watchdog 的探活命令也不再依赖外部 `timeout` 等待不可中断的 FUSE `stat`。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.19.0

- 增加 Codex subscription 自身图片生成能力：
  当会话的 `image_generation_tool_model` 选择当前 Codex subscription 模型且模型具备 `image_out` capability 时，工具目录会暴露 Responses 原生 `image_generation` builtin，而不是本地 function tool；Codex 返回的 `image_generation_call` 会 materialize 为 assistant `FileItem`，并补齐裸 base64 图片落盘。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 Web Channel foreground WebSocket 重复展示最终 assistant 消息：
  Foreground session 的持久化消息已经通过 `message_appended` 事件推给 Web 客户端；WebChannel 不再同时把携带结构化 `ChatMessage` 的 `OutgoingDelivery` 再投影为一条 `messages` 增量，避免图片生成等带附件回复在 Stellacode 2 中显示两份。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.18.12

- 修复 Stellacode 2 工具执行过程聊天区闪烁：
  工具组的完成态和自动折叠条件分离，收到 tool result 后仍会展示完成状态，但不会在最终 assistant 回复出现前反复折叠/展开，避免连续工具调用过程中聊天内容像被清空后重渲染。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.18.11

- 修复 Stellacode 2 工具调用过程不进入聊天流：
  Core session 现在会在 `model_response` 和 `tool_result` 写入历史后发出结构化 `message_appended` 事件，Web foreground stream 会把工具开始和工具结束都作为 `messages` 增量推给前端；Stellacode 2 工具组也会在收到对应 tool result 后切换到完成态，不再必须等最终 assistant 回复出现才结束 running 展示。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.18.10

- 清理 Web Terminal 测试专用路径的 release 编译 warning：
  Web terminal 旧 REST 数据面移除后，`TerminalManager::output/input` 和相关 output limit 类型只在测试中使用；现在这些测试专用项用 `#[cfg(test)]` 限定，避免 release build 继续产生死代码 warning。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 Stellacode 2 侧栏二次拖拽回跳：
  Conversation 级 layout 不再覆盖全局 sidebar 宽度，左侧 Conversation Bar 的宽度始终从全局 `settings.layout.sidebar` 读取，避免选中 conversation 时第二次拖拽先回到默认宽度再开始调整。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 Runtime Skill Updates 辅助消息展示：
  Stellacode 2 现在会把 `[Runtime Skill Updates]` 和 `[Runtime Prompt Updates]` 一样识别为辅助 user message，并折叠到下一条用户消息上的小圆点中，避免运行时技能元数据刷新内容直接占据聊天流。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 Stellacode 2 发送消息乱序和思考中状态不可见：
  Core session 在用户输入和运行时辅助消息写入历史后会发出结构化 `message_appended` 事件，Web Channel 将 foreground session 的 append 事件投影成 foreground WebSocket `messages` 增量，前端用服务端 echo 替换乐观消息，避免 assistant 消息先于用户消息显示。Stellacode 2 发送成功后也会按 offset/limit 补拉新增消息作为兜底，并渲染没有 plan 的运行态 activity，确保“等待响应 / 思考中”能及时显示。本次不改变 `config` schema，不改变 `workdir` schema。

- 优化 Stellacode 2 macOS 系统通知文案：
  Conversation 完成后的 macOS 通知现在使用 conversation 名称作为通知标题、正文显示“新回复已完成”，避免系统通知里重复显示应用名并把 conversation 名塞进正文导致提示语不自然。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.18.9

- 实现 Web Channel 服务端删除 conversation：
  后端新增 `DELETE /api/conversations/{conversation_id}`，会先通过 host dispatcher 停止活跃 conversation runtime，再删除本地 conversation 目录、消息日志、session 状态、local workspace、Web 已读状态和关联 cron 任务，并广播 `conversation_deleted` 列表流事件；fixed SSH remote 只卸载并清理本地 sshfs mountpoint，不删除远端目录。Stellacode 2 删除操作现在等待服务端成功后再更新列表，不再用本地 `hiddenConversations` 伪装服务端删除。本次不改变 `config` schema，不改变 `workdir` schema。

- 收敛 Stellacode 2 状态事实源：
  前端不再用 foreground session WebSocket 的 `processing` 事件直接修改左侧 conversation 列表运行态；侧栏只消费 `/api/conversations/stream` 的 summary / processing / completion / seen / deleted 事件，当前聊天区详情继续消费 `/status` 和 `/api/ws` foreground 事件，避免列表状态被不同来源反复覆盖。本次不改变 `config` schema，不改变 `workdir` schema。

- 收敛 Web Channel 消息分页和进度事件：
  Web Channel 删除 `messages/after/{id}` 和 `messages/range` 路由，只保留 `GET /api/conversations/{conversation_id}/messages?offset=&limit=`；Stellacode 2 根据 WebSocket ack 的 `current_message_id` 和本地已持有消息 id 自行计算 offset，不再通过多套分页接口补齐窗口。Foreground WebSocket 也只保留结构化 `turn_progress` 事件，不再发送 Web Channel 渲染过的 `progress_feedback` 文案事件，前端直接根据结构化 `TurnProgress` 决定展示。本次不改变 `config` schema，不改变 `workdir` schema。

- 收敛 foreground WebSocket 事件边界：
  `OutgoingDelivery` 现在可以携带结构化 `ChatMessage` 和 foreground `session_id`；TelegramChannel 自行渲染消息文本，WebChannel 直接把结构化 delivery 投影成 foreground `messages` 事件，不再通过轮询 `all_messages.jsonl` 发现新增消息。Foreground WebSocket 不再发送 `processing` 和 `status` 事件；conversation 级 running/status summary 统一走 `/api/conversations/stream`，状态详情仍可按需通过 REST 读取。本次不改变 `config` schema，不改变 `workdir` schema。

- 通过 conversation stream 同步 conversation 级变化：
  Conversation 持久化模型、remote、sandbox、managed session 等状态变化后会发送 `ConversationUpdated` channel event，WebChannel 投影成 `/api/conversations/stream` 的 `conversation_upserted`；Stellacode 2 发送 `/model` 等控制命令后不再轮询 `/status` 等待变化，当前模型显示优先使用 conversation stream summary。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 Web 附件路径在 macOS 临时目录下回退为绝对路径：
  Web message attachment 渲染在计算 workspace/shared 相对路径时会同时尝试 canonical path，避免 `/var` 与 `/private/var` 这类系统路径别名导致 attachment `path` 和下载 URL 变成绝对本地路径。本次不改变 `config` schema，不改变 `workdir` schema。

- 删除旧版 Stellacode Electron 客户端：
  移除已被 Stellacode 2 替代的 `apps/stellacode` 目录，避免旧客户端继续引用已收敛/删除的 Web Channel API。本次不改变 `config` schema，不改变 `workdir` schema。

- 继续清理 Web Channel 冗余接口：
  移除 terminal REST output/input/resize 数据面，只保留 terminal 生命周期 REST 接口，实时 PTY output/input/resize 统一走 terminal WebSocket；移除读取全量已读状态的 `GET /api/conversations/seen`，已读读取统一来自 conversation summary 和 `/api/conversations/stream`，Stellacode 2 也不再在 Electron settings 中保留 `conversationNames`、`hiddenConversations`、`conversationRead` 这类 conversation 事实源。本次不改变 `config` schema，不改变 `workdir` schema。

- 统一 WebSocket 路径语义：
  Foreground WebSocket 从通用 `/api/ws` + 首帧 `subscribe_foreground` 改为 `/api/conversations/{conversation_id}/foreground/ws` 直接连接；conversation stream、foreground stream 和 terminal stream 的事件 payload 不再携带冗余 `subscription` 字段。Foreground `subscription_ack` 现在除了当前/下一个 message id，也会带上当前活跃 turn 的最近一次结构化 `turn_progress`，方便 Stellacode 2 重连后立即恢复进度展示。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.18.8

- 增加 Web Channel / Stellacode 2 API 静态文档页：
  在 `docs/web-channel-api.html` 中整理 REST、WebSocket、workspace、terminal、Electron 转发与 GitHub Pages 部署方式，并记录当前交互设计中的冗余/歧义点（例如前端本地隐藏 conversation 与未实现的服务端 DELETE、列表状态与 foreground 状态事实源边界、terminal REST 与 WebSocket 数据面分工）。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 Stellacode 2 输入法回车误发送：
  Chat composer 现在显式跟踪 IME composition 状态，并在 composition end 后短时间屏蔽 Enter，兼容 Windows 和部分输入法在 Enter keydown 时 `isComposing` 已经变回 false 的情况；发送失败时会把原 draft 恢复到输入框，避免误发送后内容状态不一致。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.18.7

- 修复 Windows 标题栏系统按钮不可见：
  Stellacode 2 在 Windows/Linux 上启用 Electron title bar overlay，并把自定义右侧工具栏整体左移避开系统最小化、最大化和关闭按钮区域，避免隐藏标题栏后看不到窗口关闭按钮。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.18.6

- 按需读取 Web conversation 消息日志：
  Web channel 的消息列表、range、detail、foreground WebSocket 增量推送和 conversation summary 不再把 `all_messages.jsonl` 完整读入内存；列表/range/detail 只解析需要返回的消息范围，WebSocket 在文件长度未变化时不扫描日志，conversation summary 改为流式扫描最后一条消息。Status usage 统计也改为流式读取 JSONL。本次不改变 `config` schema，不改变 `workdir` schema。

- 修正自动已读的前台条件：
  Stellacode 2 只有在窗口可见且处于 focus 状态时，才会自动把当前 conversation 标记为已读；窗口在后台时即使当前选中了该 conversation，也不会消费新消息，并且当前 conversation 在后台完成一轮时仍会按未读消息触发系统通知。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.18.5

- 收敛 Stellacode 2 系统通知触发源：
  系统通知现在和左侧未读红点使用同一个 `conversation_turn_completed` 事实源，只在非当前 conversation 一轮完成且 `last_message_id > last_seen_message_id` 时触发；移除前端基于 running -> idle 状态变化推断完成的通知逻辑，避免运行态变化和新消息语义不一致。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 Web conversation 完成事件缺失：
  Web conversation 的消息 API 使用消息数组下标作为 `message_id`，但列表摘要曾错误地尝试从 `all_messages.jsonl` 的消息 JSON 中读取不存在的 `id/message_id` 字段，导致 `conversation_turn_completed` 找不到 `last_message_id` 后提前返回。现在摘要直接使用最后一条消息的数组下标作为 `last_message_id`，保证一轮完成后能稳定推送 completion 事件。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.18.4

- 收敛 Stellacode 2 conversation stream 状态机：
  前端现在通过统一的 stream event reducer 处理 `conversation_snapshot`、`conversation_upserted`、`conversation_processing`、`conversation_turn_completed` 和 `conversation_seen`，并在同一入口保证 `last_seen_message_id` 单调递增、最新消息摘要不被旧事件回退，避免未读红点逻辑继续分散在多个 handler 中。本次不改变 `config` schema，不改变 `workdir` schema。

- 持久化 Web channel 已读状态：
  Web channel 现在把 conversation seen 状态保存到 `.log/stellaclaw/channels/<channel_id>/web_state.json`，服务重启后仍能恢复已读位置；`POST /seen` 在后端也会按最大消息 id 合并，防止较旧的已读上报覆盖较新的已读状态。本次不改变 `config` schema，`workdir` schema 升级到 `0.11`。

# 1.18.3

- 抽象 Stellacode 2 标题栏系统控制区布局：
  Electron preload 现在向前端暴露平台相关的 chrome metrics，前端通过 CSS 变量决定左侧 toolbar、drag safe area 和标题区域的 inset。macOS 继续避让 traffic lights，Windows/Linux 左侧 toolbar 默认贴近窗口左边缘，后续其它平台只需调整 metrics，不需要改 React 布局。本次不改变 `config` schema，不改变 `workdir` schema。

- 简化 Stellacode 2 标题栏：
  移除标题右侧未使用的“更多”三点按钮，减少标题区域视觉噪音。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 Stellacode 2 设置页按钮换行：
  设置页 section header 的操作按钮现在保持不换行且不被说明文字挤压，避免“添加”等短按钮被压成两行。本次不改变 `config` schema，不改变 `workdir` schema。

- 修正 Stellacode 2 输入框执行位置标识：
  发送按钮旁边的“本地 / 远程”标识现在只根据当前 conversation 的 remote mode 派生：fixed SSH remote 显示为远程，其它情况显示为本地。SSH Proxy 或非 loopback Direct URL 只代表客户端连接 Stellaclaw 的方式，不再影响单个 conversation 的执行位置标识。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 Stellacode 2 Markdown 文件预览崩溃：
  文件预览面板现在补齐 Markdown 渲染组件，并给预览区域增加局部错误边界；点击 `.stellaclaw/USER.md` 等 Markdown 文件时不再因为未定义组件导致整个 renderer 黑屏，单个文件预览异常也会限制在预览面板内。本次不改变 `config` schema，不改变 `workdir` schema。

- 接入 Stellacode 2 低延迟 Terminal Dock：
  前端现在使用 `@xterm/xterm` 渲染终端，并通过后端 terminal WebSocket stream 的二进制帧直接传输 PTY 输入输出；Terminal Dock 支持按 conversation 自动创建终端、列出并切换多个 terminal 会话、关闭当前会话、实时 resize，并保留 REST 生命周期接口作为管理通道。Terminal Dock 高度和左侧会话列表宽度都可拖拽调整并保存到本地布局设置。本次不改变 `config` schema，不改变 `workdir` schema。

- 增加 Stellacode 2 Conversation 级 UI 状态恢复：
  Electron 本地 settings 现在记录每个 conversation 的 UI 状态，包括 Overview / Workspace / Preview / Terminal 面板开关、各面板尺寸、Terminal 尺寸和左侧列表宽度，以及 Preview 中打开的文件和当前 active tab。切换 conversation 时会自动恢复对应布局，并按路径重新读取已打开文件内容。本次不改变 `config` schema，不改变 `workdir` schema。

- 增加 Stellacode 2 Conversation 新消息提示：
  Web channel 会在 conversation 列表摘要中带上前台 processing 状态和最新消息 id；左侧 Conversation 列表用右上角红点提示有未读新消息，当前打开的 conversation 会自动标记为已读。前端仍会检测可见 conversation 从运行态变为 idle，并通过 Electron 触发系统通知。本次不改变 `config` schema，不改变 `workdir` schema。

- 将 Stellacode 2 Conversation 已读和列表更新移到 Web channel：
  Web channel 现在提供 conversation seen API，并在 conversation summary 中返回 `last_seen_message_id`；Stellacode 2 前端不再把已读位置写入 Electron 本地 settings，而是向后端标记已读。新增 `/api/conversations/stream` WebSocket：连接时只推一次 `conversation_snapshot` 初始快照，后续使用轻量事件 `conversation_processing`、`conversation_turn_completed`、`conversation_seen` 和 `conversation_upserted` 同步运行态、新消息、已读和摘要变化，左侧列表不再依赖客户端定时轮询。本次 seen 状态保存在 WebChannel 进程内，暂不改变 `config` schema，不改变 `workdir` schema。

- 修复 1.18.x CI 格式化失败：
  对 Web channel conversation stream 相关 Rust 代码执行 `cargo fmt --all`，确保 GitHub CI 的格式检查通过。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 Stellacode 2 未读红点反复出现：
  Conversation 列表合并服务端 `conversation_snapshot`、`conversation_upserted` 和 `conversation_turn_completed` 事件时会保留本地已消费到的最大 `last_seen_message_id`，避免刚打开的 conversation 被稍早的服务端旧 snapshot 覆盖后在切换走时重新亮红点；已读上报也改为直接传入 server id 和 conversation id，不再从组合 key 中反解析，避免服务器 ID 含冒号时上报失败。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.18.0

- 增加 Web channel terminal WebSocket 实时流：
  保留现有 terminal REST 生命周期和 fallback output 接口，新增 `GET /api/conversations/{conversation_id}/terminals/{terminal_id}/stream` WebSocket attach。终端输出继续按 raw bytes 写入 ring buffer，WebSocket Binary frame 直接传 PTY stdout/stderr bytes，client Binary frame 直接写入 stdin；Text frame 只处理 `attach`、`resize` 和 `ping` 控制 JSON。支持按 offset replay/resume，offset 太旧时返回 `dropped` 控制事件并从当前 buffer 起点继续；慢客户端会被 detach，避免阻塞 PTY reader。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.17.3

- 恢复 Stellacode 2 SSH Proxy 服务器模式：
  设置页重新提供 Direct / SSH Proxy 连接模式。SSH Proxy 会先通过 `ssh -L` 连接到配置的 SSH Host，再从目标机器网络访问 Target URL，并把本地转发地址供 Stellacode 使用；切换或删除服务器配置时会关闭不再匹配的 SSH tunnel。本次不改变 `config` schema，不改变 `workdir` schema。

- 增加 Stellacode 2 手动检查更新入口：
  设置页“更新”现在提供“检查更新”按钮，并展示当前 updater 状态；打包版本仍会自动后台检查和下载，开发环境继续禁用检查。下载完成后可以从设置页“重启并安装”，也可以使用标题栏 `Update` 按钮。本次不改变 `config` schema，不改变 `workdir` schema。

- 调整 Stellacode 2 应用图标与 SSH Proxy 文案：
  macOS 打包图标现在保留透明安全边距，避免 Dock 中视觉尺寸偏大；SSH Proxy 设置明确支持填写 `~/.ssh/config` 中的 Host alias，例如 `remote_server`。SSH tunnel 启动时不再因为 alias 里无关的 forward 失败而提前退出，并会等待本地转发端口可连接后再返回，最长等待 10 秒以兼容较慢的 SSH 登录。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.17.2

- 修复 Stellacode 2 Windows 打包启动黑屏：
  Vite 构建现在使用相对资源路径，避免 Electron 通过 `file://` 加载 `dist/index.html` 时把 `/assets/...` 解析成文件系统根目录下的资源，导致 Windows packaged app 无法加载 renderer bundle。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.17.1

- 适配结构化 turn progress 的前端计划展示：
  Stellacode Web 前端现在同时监听新的 `turn_progress` 事件和兼容的 `progress_feedback` 事件，计划面板读取 `TurnProgressPlan.items` 结构化字段，并继续兼容旧的 `plan` 字段与文本计划回退。底部计划面板保持当前回合计划状态，不再被普通 progress 刷掉；工具流仍在聊天区 compact row 中展示，新增工具行、进度行和计划行使用淡入动画。本次不改变 `config` schema，不改变 `workdir` schema。

- 接入 Stellacode 2 自动更新：
  React/Electron 版 Stellacode 现在通过 `electron-updater` 在后台自动检查并下载 GitHub Release 更新，下载完成后在标题栏右侧按钮组左边显示蓝色 `Update` 按钮，点击后重启并安装。Release workflow 现在构建 `apps/stellacode2` 并上传安装包、`latest*.yml` 和 blockmap，供客户端自动更新读取。本次不改变 `config` schema，不改变 `workdir` schema。

- 简化 Stellacode 2 服务器设置：
  设置页服务器配置现在只展示名称、Base URL 和 Token；SSH 场景改为用户自行建立隧道后填写本机可访问的转发地址，不再在客户端内维护 SSH Proxy / Target URL 字段。设置页版本号改为读取 Electron `app.getVersion()`，开发环境提示不会检查更新。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.17.0

- 结构化前台 turn progress：
  `ChannelEvent::ProgressFeedback` 现在携带结构化 `TurnProgress`，包括 `phase`、`model`、`activity`、`hint`、`plan` 和 `error`。Conversation 层不再提前生成 Telegram 风格 emoji 文案，Telegram/Web 各自按 channel 投影；WebSocket 新增 `type: "turn_progress"` 事件并保留旧 `progress_feedback` 兼容事件，兼容事件也带结构化 `plan`，但 `text` 不再夹带 plan 内容。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.16.8

- 修复模型选择后聊天面板仍停留在选择界面：
  Web status 快照当前不包含 `model_selection_pending` 字段，前端刷新 status 后侧栏可以显示模型，但聊天面板仍会回退到旧 conversation summary 的 pending 状态。现在 `/model <name>` 成功刷新 status 后会同步清除本地 conversation summary 的 `model_selection_pending` 并更新模型名，确保中间 ChatWorkspace 退出模型选择 gate。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.16.7

- 修复新建 Web conversation 选择模型后仍显示 pending：
  前端发送 `/model <name>` 后现在会短轮询当前 conversation status，避免 Web message API 的 202 投递响应早于 conversation 状态持久化时读到旧的 `model_selection_pending=true`；模型展示也改为在已有 status 快照时优先信任 status 的 pending 状态，不再被旧 conversation summary 覆盖。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.16.6

- 修复 Stellacode 控制命令的本地乐观消息残留：
  Web 前端发送 `/model <name>` 等控制命令成功后现在会移除本地 optimistic user 气泡，并主动刷新当前 conversation status，避免模型切换命令长期停留在最新用户消息位置，也避免在后端未额外推送 status 时模型选择界面继续显示。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.16.5

- 修复 Web conversation 列表卡死：
  Web channel 枚举 `conversations/` 时现在会跳过 `sshfs-*` workspace mount 目录，避免远程 FUSE mount 无响应时因为探测 `conversation.json` 阻塞整个请求；workdir upgrade 中扫描 conversation 目录的迁移步骤也同步跳过这些 mount。本次不改变 `config` schema，不改变 `workdir` schema。

- 兼容 Web token usage 字段展示：
  Web messages 和 message detail 的 `token_usage` 现在除保留 `uncache_input` 外，也返回等价的 `input` 与总量 `total`，避免前端或旧客户端只识别 `input` 时把切换模型后的大段非缓存输入误显示为 0。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.16.4

- 收窄 Web message skeleton 顶层文本语义：
  `MessageSkeleton.text`、`text_with_attachment_markers` 和 `preview` 现在只聚合普通 `Context` 文本，不再把 `ToolResult.context` 合并到顶层正文；tool result 的内容继续保留在对应 `items[].context` / `items[].context_with_attachment_markers` 中，避免前端 fallback 渲染顶层文本时把工具结果重复当正文展示。本次不改变 `config` schema，不改变 `workdir` schema。

- 美化 README 并突出 Stellacode + Remote mode 支撑的 Host -> Server -> Server 工作流：
  README 开头现在更明确说明 Stellacode/Telegram/Web 作为用户侧 host surface，Stellaclaw 作为 durable agent server，Remote mode 可把 conversation 绑定到第二台 server 的 workspace、terminal 和 execution root；同步补充 highlights、Web channel 当前能力、架构图中的 remote workspace server 边界，并新增 SSHFS workspace illusion 和 Web Channel 两跳远程接入的 SVG 讲解图，说明 agent 如何像在远端工作、用户如何随时开始/中断/继续。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.16.3

- sshfs watchdog 守护线程：
  FixedSsh remote mode 的 conversation 现在会启动一个后台守护线程，每 20 秒用带超时的子进程探活 sshfs mountpoint。如果 mountpoint 无响应，watchdog 设置 flag，主循环检测到后立即：关闭所有 agent_server 进程、向用户发送错误消息和 ChannelEvent::Error、lazy unmount 坏的 sshfs、退出 conversation 线程。后续用户发送任意消息时 dispatcher 会重新 spawn conversation 线程并自动重挂 sshfs。同时将 `is_mountpoint()` 和 `sshfs_workspace_is_usable()` 改为带超时子进程实现，避免在 FUSE 僵尸 mount 上永久阻塞。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.16.2

- 修复终端颜色和响应体验：
  终端 output 改为增量写入 xterm.js，不再每次 poll 全量 sync 累积 buffer，消除闪屏和重复渲染；poll 循环中直接把增量数据写入 xterm session，renderTerminalContext 不再干预已有 xterm session 的输出。添加 WebGL renderer addon 提升渲染性能，scrollback 从 5000 提升到 10000，字号从 12 调到 13，光标样式改为 bar。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.16.1

- 修复输入法回车误发消息：
  composer 和 rename modal 的 Enter keydown 现在检查 `event.isComposing`，IME 组合输入中的回车不再触发发送或确认。本次不改变 `config` schema，不改变 `workdir` schema。

- 优化面板宽度拖拽范围：
  侧栏拖拽范围从 220–420 放宽到 180–520，详情栏从 260–620 到 220–800，文件栏从 280–680 到 220–800，终端从 180–560 到 120–800，适配大屏显示器。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复终端 UTF-8 边界截断：
  terminal output 读取现在在 chunk 边界回退到 UTF-8 字符对齐位置，避免 `from_utf8_lossy` 把被切断的多字节序列替换为 `�`。本次不改变 `config` schema，不改变 `workdir` schema。

- 优化终端轮询响应速度：
  terminal poll 改为自适应间隔：有新数据时 32ms、空闲时 120ms、失焦时 700ms，改善交互延迟感。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.16.0

- 增加 Stellacode workspace 文件上传/下载功能：
  后端新增 `POST /api/conversations/{id}/workspace/upload?path=` 和 `GET /api/conversations/{id}/workspace/download?path=`，分别接收/返回 tar.gz 格式的压缩包，压缩后限制 10MB；上传时自动解包到目标目录，安全检查拒绝绝对路径和父目录穿越；下载支持单文件和整个目录。前端文件面板支持拖拽文件/文件夹上传到当前目录，以及右键菜单下载文件或目录（弹出原生保存对话框）。新增 `tar` 和 `flate2` Rust 依赖，HTTP body 上限从 2MB 提升到 11MB。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 Stellacode remote 模式切换后文件面板不刷新的问题：
  后端 `set_remote_mode` 和 `disable_remote_mode` 完成后现在会推送 `status` 事件；前端收到 status 更新时检测 `remote` 或 `workspace` 字段变化，如果变了就清除对应 conversation 的 workspace listing 和文件预览缓存并重新请求。本次不改变 `config` schema，不改变 `workdir` schema。

- 修复 Stellacode conversation 重命名在 Electron 中不可用的问题：
  `window.prompt()` 在 Electron BrowserWindow 中不支持，改为使用项目现有的 modal 弹窗基础设施，支持输入框、Enter 确认和 × 取消。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.15.13

- 优化 Web channel 图片附件展示：
  Web messages payload 保持原 `text` 干净文本不变，新增 `text_with_attachment_markers`，并在从 `<attachment>...</attachment>` 提取出的附件上返回对应 `marker`，例如 `[[attachment:0]]`，方便前端把图片按原文位置插回去做图文混排；结构化 `items[].text` 和 tool result context 也提供对应 marker 文本。本次不改变 `config` schema，不改变 `workdir` schema。

- 调整 Web 图片缩略图缓存策略：
  Web channel 图片 thumbnail 不再固定压到 360px，而是优先保留最长边 960px，并按目标 256KB 逐步降低 JPEG 质量和尺寸；旧缩略图缓存 key 已更新，避免继续复用过度压缩的旧缓存。本次不改变 `config` schema，不改变 `workdir` schema。

- 精简 Brave 图片搜索结果：
  Brave image search provider 不再额外拼接 `answer` 文本，只返回结构化 `query`、`citations` 和 `results`，减少工具结果 token 占用。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.15.12

- 增加 Conversation nickname：
  `ConversationState` 新增 `nickname` 持久化字段，新的 conversation 默认使用 `conversation_id` 作为 nickname；Web channel 的 conversation 列表和创建响应会返回该字段，并新增 `PATCH /api/conversations/:id` 支持只更新 nickname，不改变 `conversation_id` / `platform_chat_id` / session binding 等运行身份。旧 workdir 会在升级到 `0.10` 时为每个 `conversation.json` 写入默认 nickname。本次 `workdir` schema 更新到 `0.10`，`config` schema 不变。

- 扩展 Web messages token usage 响应：
  `GET /api/conversations/:id/messages`、`messages/after`、`messages/range` 和 WebSocket `type: "messages"` 推送现在会在每条 message skeleton 上直接返回完整 `token_usage`，同时保留 `has_token_usage` 兼容标记。本项不改变 `config` schema，不改变 `workdir` schema。

# 1.15.11

- 引入 Conversation 到 Channel 之间的统一 `ChannelEvent` 输出层：
  现有 delivery、processing、progress feedback 和 status 现在都通过 `OutgoingDispatch::Event` 分发，后续 Telegram/Web 可在同一个事件并集上分别投影。新增结构化 `Error` channel event，由 `ConversationRuntime` 把 turn failed、runtime crashed、control rejected、模型/remote/sandbox 切换失败和附件解析失败归一化为带 `scope`、`severity`、`code`、`message`、`detail`、`can_continue` 和 `suggested_action` 的事件；Telegram 继续渲染为短文本，WebSocket 会推送 `type: "error"` 的结构化 JSON。顺便缩短 incoming user metadata synthetic notice 文案。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.15.10

- 补齐 WebSocket foreground 订阅的实时非消息事件：
  `GET /api/ws` 的 foreground subscription 现在除了继续推送新增 `messages`，也会把 WebChannel 收到的 `processing`、`progress_feedback` 和 `status` 事件推给已订阅该 conversation 的连接；工具 batch 开始执行时 core 已发出 progress event，前端此前收不到是因为 WebChannel 只轮询持久化 ChatMessage，没有转发这些非持久化 outgoing events。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.15.9

- 扩展 Web channel 消息实时和历史读取能力：
  新增 `GET /api/ws` WebSocket 入口，客户端首条消息发送 `{"type":"subscribe_foreground","conversation_id":"..."}` 后会收到 foreground session 的 `subscription_ack`，其中包含握手时的 `current_message_id` / `next_message_id`；之后该连接会推送新增 foreground messages 的结构化 `messages` 事件。新增 `GET /api/conversations/:id/messages/range?anchor_id=<id>&direction=before|after&limit=<n>&include_anchor=true|false`，用于从某条消息 ID 往前或往后拉取窗口。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.15.8

- 重构 Web messages 结构化内容响应：
  `GET /api/conversations/:id/messages` 和单条消息详情现在返回 `items` 数组，用 JSON 区分 `text`、`file`、`tool_call` 和 `tool_result`；tool call 参数会按 JSON 解析后放入 `items[].arguments`，不再拼进 `preview` 字符串。`preview` / `text` 只保留 rendered text 内容，附件继续通过 `attachments` 返回。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.15.7

- 调整 Web messages 文本返回：
  `GET /api/conversations/:id/messages` 的 `preview` 字段现在保留完整 rendered text，不再按 240 字符截断；图片附件继续使用已有 thumbnail/cache 机制。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.15.6

- 优化 Web channel Remote 模式消息历史性能：
  sshfs mount 现在启用目录、属性、entry 和 FUSE auto cache，降低 remote workspace 上频繁 `stat` / `readdir` 的往返成本；Web messages 渲染不再为每条消息重复 ensure remote workspace，而是每次请求只计算一次 workspace/shared 路径，并避免在历史列表路径触发 ssh/sshfs/mountpoint 检查。新增按 conversation 分目录的 runtime cache manager，图片缩略图写入 `rundir/cache/conversations/<conversation_id>/thumbnails/`，后续命中直接读取本地缓存。本次 `workdir` schema 更新到 `0.9`，`config` schema 不变。

# 1.15.5

- 扩展 Web messages 附件响应：
  `GET /api/conversations/:id/messages` 的每条消息现在会直接返回附件 metadata，不再只给 `attachment_count`；图片附件会生成压缩 JPEG 缩略图并以内联 base64 返回，同时继续保留 workspace `path` / `url` 供前端读取完整文件。附件 metadata 继续用 `source` 区分 `<attachment>` 标签、结构化 `ChatMessageItem::File` 和 `ToolResult.result.file`。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.15.4

- 调整 tool result 媒体上下文行为：
  SessionActor 不再在 `ToolResult.result.file` 后自动追加 synthetic `role="user"` media context message；生成或加载出的文件只保留在原始 tool result 中，模型如需后续复用应显式调用 `image_load` 等工具。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.15.3

- 补全 Web channel 结构化附件展示：
  Web messages API 现在会直接遍历 `ChatMessage.data` 动态生成附件 response；除了继续兼容 `Context` 文本中的 `<attachment>...</attachment>` 标签，也会把结构化 `ChatMessageItem::File` 和 `ToolResult.result.file` 转成 `attachments` metadata。JSON-RPC 和 session JSONL 中的原始 `ChatMessage` 不需要迁移；只要历史消息本身保留了结构化 file item，Web response 会自动显示对应附件。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.15.2

- 修复 Web channel 的 `<attachment>` 消息展示：
  Web messages API 现在会在消息列表和详情中解析 assistant 输出里的 `<attachment>...</attachment>` 标签，返回去掉标签后的 `preview` / `rendered_text`，并在消息详情中附带可通过 workspace file API 读取的附件 metadata；附件解析失败不会让消息 API 整体失败，而是返回 `attachment_errors` 供前端展示。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.15.1

- 收紧 Web channel terminal API 语义：
  新建 PTY 时显式设置 `TERM=xterm-256color` 并继续支持 `cols` / `rows`；terminal 输入保持原样写入，不自动追加换行；输出继续按 offset 返回 PTY 输出的 UTF-8 文本视图和 `next_offset`，不清理 ANSI；resize 继续直连 PTY resize。Terminal 会话仍由 Web channel 在内存中按 conversation 管理，刷新或切换 conversation 不会重建 shell；当 conversation 的 Remote Mode 改变时，会清理该 conversation 下已有 terminal，让前端重新创建匹配新模式的 PTY。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.15.0

- 增加 Web channel 模型列表 API：
  新增 `GET /api/models`，返回当前 Web channel 可用于 foreground agent 选择的模型列表，字段包含 alias、模型名、provider 类型、能力和 token 上限；列表与 `available_agent_models` 白名单及 `POST /api/conversations` 的模型校验保持一致。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.14.1

- 修复 idle compaction 触发时间：
  SessionActor 现在用上一轮 provider 请求开始时间估算 Claude prompt cache 剩余窗口，避免按 turn 完全返回时间倒计时导致长输出后 idle compaction 晚于 5 分钟 cache TTL。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.14.0

- 允许模型配置控制 provider 输出上限：
  `ModelConfig` 新增 `max_tokens`，默认值为 `0`，表示请求时使用该模型配置的 `token_max_context` 作为输出上限；Claude provider 不再把 Messages 请求的 `max_tokens` 写死为 `4096`，而是始终发送 New API 原生 Claude 接口要求的必填 `max_tokens` 字段。`config` schema 更新到 `0.11`，`workdir` schema 不变。

# 1.13.1

- 调整 Stellacode 桌面体验：
  Stellacode 界面参考 Codex Desktop 的 Electron 视觉语言，重做侧栏、主聊天 surface、composer、消息气泡、详情栏、弹窗和深浅色 token；FileTree 拆成独立文件页，按 Codex 文件面板形态增加标题计数、筛选框、路径分组、SVG 图标、文件类型 badge 和真实层级缩进，并支持选中文件后在详情栏读取预览；新增右侧 Terminal 页，接入 Web channel terminal API 创建终端、轮询输出和发送 stdin；右侧面板开合、tab 切换、文件树展开和消息进入增加过渡动画。发送消息时新增本地乐观消息、会话切换过期响应丢弃、前台聚焦刷新、渐进式轮询，以及 Web API GET 请求的短退避重试。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.13.0

- 扩展 Brave 垂直搜索：
  `web_search` 工具新增 `image`、`video`、`news` 布尔参数，按当前 session 配置动态说明哪些垂直搜索可用；不再新增独立图片搜索工具或图片搜索能力，所有搜索 provider 统一使用 `web_search` 能力。新增 `brave_search_image`、`brave_search_video`、`brave_search_news` provider，分别调用 Brave `/res/v1/images/search`、`/res/v1/videos/search`、`/res/v1/news/search` 并返回对应结构化元数据。新增 `session_defaults.search_image_tool_model`、`search_video_tool_model` 和 `search_news_tool_model` 用于单独指定垂直搜索 provider。新增 `pricing/brave_search_image.json`、`pricing/brave_search_video.json`、`pricing/brave_search_news.json`，保持空字典，因为 Brave Search 垂直接口按请求计费，不按 token 归因。
- 增加 agent 模型选择白名单：
  根配置新增 `available_agent_models: string[]`，用于限制 `/model` 展示和 `/model <alias>` 可切换的 foreground agent 模型；为空时保持旧行为，所有 chat-capable 模型都可选。Web API 创建 conversation 时显式指定 `model` 也会使用同一白名单校验。`config` schema 更新到 `0.10`，`workdir` schema 不变。

- 增加长期记忆系统设计文档：
  新增 `MEMORY_DESIGN.md`，记录 MemoryStore 作为独立、无工具的长期记忆边界，并明确只有 ConversationActor 可以调用 memory JSON-RPC handle，普通 SessionActor 只能通过 SessionEvent 或 `memory_query` 工具桥接回所属 ConversationActor 后间接触发 memory RPC。文档采用独立 `memory_core`、同进程单 MemoryThread JSON-RPC、memory-specific provider 和可插拔 `MemoryAlgorithm`，默认算法为单一全局记忆池的 MemTree；同时覆盖 user/assistant 消息 copy、agent-to-agent 观察、`STELLACLAW.md` turn-boundary checkpoint/diff、Memory Node 三层结构、append-only entry event log、常规创建/替换/失效、容量溢出或每日整理时的维护合并、节点溢出下沉、由命中频率计算的重要性、稳定 `rendered_context`、provider/cache 复用和失败策略。本次不改变 `config` schema，不改变 `workdir` schema。
- 对齐 ClawParty 默认 recent 保留比例：
  自动上下文压缩在未显式配置 `compression_retain_recent_tokens` 时，默认保留最近区从阈值的 25% 调整为 18%；PartyClaw 兼容配置迁移在缺少 `recent_fidelity_target_ratio` 时也按 18% 生成 retain recent。显式配置的值不变。本次不改变 `config` schema，不改变 `workdir` schema。
- 恢复手动上下文压缩命令：
  Telegram/Web conversation 现在支持 `/compact`，会向 foreground session 发送 `CompactNow` 控制请求并主动压缩当前上下文；完成后返回压缩前后估算 token、阈值和消息保留/压缩数量。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.12.3

- 修复下载附件路径提示：
  本地 `file_download_start` 现在在工具结果中返回 workspace 相对路径，避免模型把机器相关的绝对路径写入 `<attachment>` 标签导致 Telegram 附件发送失败；附件解析同时兼容旧机器/旧 workdir 中同一 conversation 的绝对路径，只要对应文件已存在于当前 conversation workspace，就会自动重映射。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.12.2

- 修复 `ls` 跳过 shared 目录：
  本地 `ls` 工具现在会把 workspace 顶层 `shared` symlink 作为目录展示并递归列出其中内容，保持其它 symlink 仍默认跳过，避免重新引入循环链接或慢挂载风险。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.12.1

- 修复 `claude_code` prompt cache breakpoint：
  Claude Messages 请求现在恢复为优先在最后一个 message content block 上打 `cache_control`，只有没有可缓存 message block 时才回退到 system block。这样保留 Anthropic system block 格式的同时，避免长对话只缓存 system prompt，恢复旧 ClawParty 中大段历史前缀被 prompt cache 命中的行为。本次不改变 `config` schema，不改变 `workdir` schema。
- 对齐 OpenRouter Responses API 的缓存字段：
  `open_router_responses` 不再发送未出现在官方 Responses request schema 中的顶层 `cache_control`，改为在启用缓存时发送稳定的 `prompt_cache_key`，用于保持 OpenRouter/OpenResponses 缓存路由语义。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.12.0

- 增加 Web channel terminal 后端 API：
  Web channel 现在为每个 conversation 提供内存态多 terminal 管理能力，支持创建/list/get、轮询读取 PTY 输出、写入 stdin、resize 和 terminate。local conversation 会在本地 workspace 中启动 shell；fixed SSH Remote Mode 会通过 `ssh -tt <host>` 在远端 configured cwd 下启动 shell，避免把交互终端跑在 sshfs mount 上。本次不改变 `config` schema，不改变 `workdir` schema。
- 增加 Web channel workspace 文件读取 API：
  新增 `GET /api/conversations/{conversation_id}/workspace/file?path=<relative>&offset=<n>&limit_bytes=<n>`，用于读取当前 conversation workspace 内单个文件的一段内容；文本以 UTF-8 返回，二进制以 base64 返回，并带文件大小、offset、返回字节数和 truncated 标记。fixed SSH Remote Mode 继续复用当前 workspace 解析逻辑。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.11.0

- 增加统一工具结果上限：
  所有标准工具结果在进入 `ChatMessage::ToolResult` 前都会经过 100,000 字符的 model-visible `context.text` 上限；超过时返回截断提示和预览，并把完整未截断结果写入 workspace 的 `.output/tool_results/`。旧 session 加载时也会对已持久化的超大 tool result 做同样级别的安全截断，避免历史中已有的大输出继续撑爆 provider preflight。本次不改变 `config` schema，不改变 `workdir` schema。
- 扩展 Web channel workspace API：
  新增 `GET /api/conversations/{conversation_id}/workspace?path=<relative>&limit=<n>`，用于前端列出当前 conversation 实际 workspace 的一层目录内容；fixed SSH Remote Mode 下会解析到 sshfs workspace 并复用现有健康恢复逻辑，返回 local mount root、remote host/cwd、相对路径、parent 和目录项元数据。本次不改变 `config` schema，不改变 `workdir` schema。
- 拆出 remote workspace 查询边界：
  新增 `stellaclaw/src/remote_actor.rs` 承载 workspace 路径校验和目录 listing 响应结构，Web channel 只做鉴权、conversation ownership 校验和转发，给后续把 SSH/Remote 专用指令收敛到 Remote Actor 留出边界。本次不改变 `config` schema，不改变 `workdir` schema。
- 接入 Stellacode workspace 浏览：
  Stellacode 右侧概览栏现在会调用 Web channel workspace API 展示当前 conversation 的实际工作目录，支持目录进入、面包屑返回和刷新；Remote Mode 下显示对应 remote host/cwd 和本地 sshfs workspace root。本次不改变 `config` schema，不改变 `workdir` schema。
- 实装 cron script：
  cron task 现在支持 prompt 模式和 script 模式二选一。prompt 模式会按计划唤醒 background agent；script 模式会在当前 conversation sandbox/workspace 中以 `sh -lc` 运行 `script_command`，返回 `0` 时按 JSONL 解析 stdout 中最多三条消息并分发给 user、foreground agent 或 background agent，返回非零或输出格式错误时自动禁用该 cron 并通知用户和 foreground agent。旧 workdir 中的 `checker_*` cron 字段会迁移为 `script_*`。`workdir` schema 更新到 `0.8`，`config` schema 不变。
- 拆分 Conversation 辅助模块：
  将 attachment/rendering、cron script、skill sync 和 status usage snapshot 从 `stellaclaw/src/conversation.rs` 拆到 `stellaclaw/src/conversation/` 子模块，保留 `ConversationRuntime` 主状态机在原文件中，降低单文件复杂度。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.10.5

- 修复 Stellacode 轮询导致聊天区反复刷新的问题：
  当前 conversation 轮询刷新时会先比较消息骨架签名；只有消息 ID、角色、发送者、时间、预览文本或 usage 标记发生变化时才重绘中间消息列表，避免每轮 poll 都重建 DOM、重复播放进入动画并滚动到底部。本次不改变 `config` schema，不改变 `workdir` schema。
- 调整 Stellacode 详情栏折叠入口：
  详情栏现在默认收起，聊天头部保留固定的详情开关按钮；右侧栏内部按钮只负责收起，避免关闭详情后找不到重新打开入口。本次不改变 `config` schema，不改变 `workdir` schema。
- 增加 Stellacode 聊天内容 Markdown 渲染：
  中间聊天区和详情预览现在支持常用 Markdown，包括标题、段落、列表、引用、分隔线、链接、粗体、斜体、删除线、行内代码和 fenced code block；渲染前仍会转义文本，链接仅允许 `http(s)` 和 `mailto`。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.10.4

- 修复附件示例误触发发送：
  `<attachment>...</attachment>` 解析现在会跳过 Markdown fenced code block 内的示例文本，避免说明文档或代码块中的占位路径被当成真实附件发送；同时 shared 附件路径识别兼容 Windows 风格 `shared\\...` 分隔符。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.10.3

- 修复 shared 附件发送：
  conversation workspace 的 `shared/` 是指向 `rundir/shared` 的 symlink；附件路径校验现在允许 `<attachment>shared/...</attachment>` 解析到该共享目录，避免 canonicalize 后因落在 conversation root 外而拒绝发送。同时附件解析失败只会降级为可见错误提示并写 warning，不再让 conversation 线程直接退出。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.10.2

- 修正 Remote Mode 下 session 持久化根目录：
  fixed SSH Remote Mode 仍然让工具工作目录指向 sshfs workspace，但 agent session 的日志、`session.json` 和 `all_messages.jsonl` 现在固定写入本地 conversation 目录，避免 remote workspace/sshfs 路径承载会话状态导致历史、状态统计和前端轮询读不到反馈。本次不改变 `config` schema，不改变 `workdir` schema。
- 增强 sshfs workspace 恢复：
  fixed SSH Remote Mode 复用已有 sshfs mount 前会先做轻量探活；如果遇到 `Transport endpoint is not connected`，会自动卸载坏 mount 并重新挂载，避免 conversation 线程在重启后因为断开的 sshfs workspace 直接失败。本次不改变 `config` schema，不改变 `workdir` schema。
- 修正坏 sshfs mount 的恢复边界：
  卸载 sshfs workspace 时不再依赖 `Path::exists()`，避免断开的 FUSE mount 因 `exists()` 返回 false 而跳过卸载；同时增加 lazy unmount fallback，并兼容 mountpoint 目录已存在的恢复路径。本次不改变 `config` schema，不改变 `workdir` schema。
- 修正工具执行器 remote mode 注入：
  agent server 初始化 `LocalToolBatchExecutor` 时现在会传入 session initial 的 `tool_remote_mode`，避免 catalog 已切到 fixed SSH 但工具执行器仍按默认 selectable/local 执行，导致 `shell pwd` 等工具返回本地 sshfs mount 路径。本次不改变 `config` schema，不改变 `workdir` schema。
- 增强 dispatcher 对已停止 conversation 的恢复：
  向 conversation 线程投递消息时如果发现旧 sender 已失效，会移除缓存 sender、重新拉起 conversation 并重试一次；重试仍失败时只写 warning，不再让单个 conversation 初始化失败导致整个 Stellaclaw 服务退出。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.10.1

- 修复递归文件工具误入慢挂载：
  `glob`、`grep` 和递归 `ls` 现在会在本地和远端执行时识别并跳过 sshfs、NFS、CIFS/SMB、9p、rclone/s3fs/gcsfuse 等慢速远端挂载，避免从工作区父目录搜索时误扫 sshfs conversation 或网络目录导致工具长时间挂起。工具说明和系统提示同步说明该默认行为。本次不改变 `config` schema，不改变 `workdir` schema。
- 增强工具批次进度提示：
  `SessionEvent::Progress` 中的 tool batch 阶段现在会带上前几个工具的名称和关键参数摘要，例如 `grep core/src`、`file_read AGENTS.md`，方便前端/Telegram 判断当前具体卡在哪个工具或路径。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.10.0

- 调整 provider request isolation 生命周期：
  `ForkServerProvider` 现在为每个 provider 实例懒创建一个长期 provider worker child，真实 provider client 在 worker 内只创建一次并跨请求复用。Codex subscription 的 WebSocket、auth cache 与 `auth.json` refresh 状态因此可以跟随 Actor/provider 生命周期保留；provider drop 时会通知 forkserver 关闭对应 worker。provider-backed media tool 的 one-shot 请求也复用同一套 worker 转发机制，不再保留独立的 per-request child 实现。本次不改变 `config` schema，不改变 `workdir` schema。
- 内化 Provider 的模型配置：
  `Provider` 现在是 model-bound 实例，`ModelConfig` 在 provider 创建时注入，`send` 只接收 `ProviderRequest`，不再每次单独传入模型配置。这让 Codex subscription 这类 stateful provider 的持久 WebSocket/auth 状态和具体模型配置一一绑定，避免跨模型复用语义混乱。本次不改变 `config` schema，不改变 `workdir` schema。
- 规范化 provider isolation 错误返回：
  forkserver/worker IPC 不再把 provider error 简化成纯字符串，而是传递结构化 `ProviderErrorReport`，保留 HTTP status、provider failure kind、WebSocket、request-too-large 等语义后再在父进程还原成 `ProviderError`。这让 Actor 的 request-too-large 恢复路径不再依赖 provider child 字符串格式。本次不改变 `config` schema，不改变 `workdir` schema。
- 增强 Core 错误事件语义：
  `SessionEvent::TurnFailed` 和 `SessionEvent::RuntimeCrashed` 现在携带结构化 `SessionErrorDetail { module, kind, reason }`，用于说明错误来自哪个 core 模块、错误类别和具体原因；Telegram/Channel 展示层可以直接使用该结构化信息，而不是解析长串嵌套错误。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.9.0

- 新增 Stellacode Electron 客户端：
  在 `apps/stellacode` 下加入本地桌面聊天程序骨架，可配置多个 Stellaclaw Web channel 后端，支持直接连接或 SSH proxy 连接，展示 Web channel conversation 列表、状态、消息骨架/详情，并可创建 conversation 与发送消息。界面包含类聊天侧边栏、服务器状态弹窗、服务器设置、按需展开消息详情、浅色/深色系统主题适配。本次不改变 `config` schema，不改变 `workdir` schema。
- 修复 Codex subscription WebSocket 传输错误恢复：
  `IO error: Broken pipe`、connection reset、缺少 closing handshake 等 WebSocket 传输层错误现在会丢弃当前 socket 并立即新建连接重试一次，不依赖模型 `retry_mode`；provider 业务错误不会被当作传输错误重试。本次不改变 `config` schema，不改变 `workdir` schema。
- 拆分 provider 建连和请求超时：
  `open_router_completion`、`open_router_responses`、`claude_code`、`openai_image`、`codex_subscription` 和 `brave_search` 现在用 `conn_timeout` 控制建连，用 `request_timeout` 控制整体请求或 WebSocket 读取窗口，避免一个超时字段同时承担两种语义。
- 更新 `config` schema 到 `0.8`：
  模型配置新增 `request_timeout` 和 `maxRequestSize`；`request_timeout` 控制 provider 整体请求/读取超时，`maxRequestSize` 控制每次 provider 请求序列化后允许发送的最大 payload 字节数。默认值为 `conn_timeout=2` 秒、`request_timeout=600` 秒、`maxRequestSize=31457280` 字节。超过 `maxRequestSize` 会在发送前抛出 `request_too_large` provider failure，由 actor 走已有请求过大恢复流程。旧 `max_request_size` 作为兼容 alias 仍可读取；旧 `0.6`/`0.7` config 会自动升级；本次不改变 `workdir` schema。

# 1.8.1

- 修正 Remote Mode 启用前校验：
  `/remote <ssh-host> <path>` 现在会先使用 `ssh` 的 BatchMode 对目标 host 做免密码登录检查；只有确认可以免密码 SSH 登录后才继续创建 sshfs workspace 并开启 fixed Remote Mode。检查失败会直接返回远程 workspace 切换失败，不写入新的 remote 绑定。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.8.0

- 扩展 remote-aware 工具覆盖：
  `file_download_start` 现在在 selectable remote 下支持 `remote` 参数，并在 fixed remote 下直接通过 SSH 在远端发起下载，避免下载流量先回到本机再经 sshfs 写回远端。`image_load`、`pdf_load`、`audio_load`、`image_analysis`、`pdf_analysis`、`audio_analysis` 现在也会在 selectable remote 下暴露 `remote` 参数，用于从指定 SSH Host 读取媒体输入；fixed remote 继续通过 sshfs workspace 读取。`skill_load` 仍保持 workspace/sshfs 语义，不新增 remote 参数。本次不改变 `config` schema，不改变 `workdir` schema。

# 1.7.1

- 修正 Web channel 控制命令覆盖：
  Web channel 现在和 Telegram 一样识别 `/remote`、`/remote off`、`/remote <ssh-host> <path>` 控制命令；此前这些输入会被当作普通文本消息。当前 `config` schema 仍为 `0.6`，当前 Stellaclaw workdir schema 仍为 `0.6`。

# 1.7.0

- 扩展 Web channel API：
  新增 `GET /api/conversations?offset=0&limit=50`，用于分页列出当前 Web channel 创建或绑定的 conversations。列表只返回当前 Web channel 的 conversation 摘要，不混入 Telegram 等其他 channel 的会话；本次不改变 `config` schema，不改变 `workdir` schema。

# 1.6.1

- 修正 OpenRouter error envelope 的错误语义：
  OpenRouter 可能在 HTTP 200 的响应体中返回 `{"error":{"code":413,...}}`，此前会被 chat completion parser 当成正常响应解析并报 `missing field id`，导致 actor 无法识别 request-too-large 并触发剪历史重试。现在 provider 会把这类错误归一成语义化 `request_too_large` provider failure；本次不改变 `config` schema，不改变 `workdir` schema。

# 1.6.0

- 新增 Web channel 的第一版 JSON API：
  config channel 支持 `kind: "web"`，通过 `token_env` 指定 Authorization token，并在 `bind_addr` 监听 HTTP `/api/...`。当前 API 支持创建 conversation、分页获取消息骨架、按消息 ID 获取详情、向 conversation 投递消息、获取某条消息之后的增量骨架列表，以及获取 conversation 状态快照。前端网页暂未实现。
- 更新 `config` schema 到 `0.6`：
  新增 `WebChannelConfig`；旧 `0.5` config 会自动升级，未配置 web channel 的部署保持原行为。当前 Stellaclaw workdir schema 仍为 `0.6`。

# 1.5.6

- 启动时执行一次配置的 skill upstream 同步：
  Host 启动并完成 workdir upgrade 后，会遍历 `skill_sync.skill_name`，对已存在的 runtime skill 执行一次 best-effort git commit/push；同步时会把 skill 拷贝到 upstream repo 的 `<skill_name>/` 子目录，允许一个 upstream repo 保存多个 skill。skill 缺失或 push 失败只写 warning 日志，不阻断服务启动。
- 本次不改变 `config` schema，不改变 `workdir` schema：
  当前 config schema 仍为 `0.5`；当前 Stellaclaw workdir schema 仍为 `0.6`。

# 1.5.5

- 修正强制 context compression 阈值：
  新消息进入前的压缩阈值现在会按当前 session model 的 `token_max_context * 0.9` 计算上限；显式配置的 `compression_threshold_tokens` 仍可作为更低的提前压缩阈值，但不会再高于当前模型上下文的 90%。
- 修正多模态 token 估算默认值：
  未显式配置 `multimodal_estimator.image` 时，图片默认使用 GPT tile-grid 估算，不再按 0 token 忽略。
- 增加 request-too-large 自恢复：
  正常对话和压缩请求遇到 413 / `request_too_large` 时，会按 tool call/result 边界从旧到新丢弃约半数当前上下文、写 warning 日志、向 channel 通知发生数据丢失并自动重试；发送前本地估算已经超过 `token_max_context` 时也会直接进入同样的剪裁恢复流程，不先发送必然失败的请求。
- 本次不改变 `config` schema，不改变 `workdir` schema：
  当前 config schema 仍为 `0.5`；当前 Stellaclaw workdir schema 仍为 `0.6`。

# 1.5.4

- 修复 OpenRouter prompt cache write usage 解析：
  OpenRouter chat-completions 的 Anthropic prompt caching 会在 `usage.prompt_tokens_details.cache_write_tokens` 返回写缓存 token；`TokenUsage` 现在会把它计入 `cache_write`，并使用本地 pricing 计算对应美元开销。
- 本次不改变 `config` schema，不改变 `workdir` schema：
  当前 config schema 仍为 `0.5`；当前 Stellaclaw workdir schema 仍为 `0.6`。

# 1.5.3

- 调整 `/status` 的渲染边界：
  Conversation 现在只生成结构化 `OutgoingStatus` 状态快照，并通过 `OutgoingDispatch::Status` 交给 channel；Telegram channel 负责把状态渲染为当前文字消息。后续如果接入图表渲染或图片消息，只需要在 channel 层扩展，不再让 Conversation 依赖具体前端形态。
- 本次不改变 `config` schema，不改变 `workdir` schema：
  当前 config schema 仍为 `0.5`；当前 Stellaclaw workdir schema 仍为 `0.6`。

# 1.5.2

- 补齐 OpenRouter pricing 并增加更新脚本：
  新增 `scripts/update_openrouter_pricing.py`，从 OpenRouter `/api/v1/models` 拉取所有带 prompt/completion token 价格的模型，换算为 USD / 1M tokens 后写入 `open_router_completion.json` 和 `open_router_responses.json`。当前生成 350 个模型价格。
- 本次不改变 `config` schema，不改变 `workdir` schema：
  当前 config schema 仍为 `0.5`；当前 Stellaclaw workdir schema 仍为 `0.6`。

# 1.5.1

- 改为本地价格表计算 token cost：
  core 编译期嵌入根目录 `pricing/<provider_type>.json`，由 `PriceManager` 根据 `ModelConfig.provider_type`、`model_name` 和 `TokenUsage` 计算 `cost_usd`；provider 不再解析响应体里的 `usage.cost` 或 `cost_details`。
- 新增 provider pricing 维护约定：
  [AGENTS.md](AGENTS.md) 规定新增或修改 provider 时必须同步检查 pricing 文件，尽量补齐实际使用模型的 USD / 1M tokens 价格；订阅制或无法按 token 归因的 provider 可保持空字典。
- 本次不改变 `config` schema，不改变 `workdir` schema：
  当前 config schema 仍为 `0.5`；当前 Stellaclaw workdir schema 仍为 `0.6`。

# 1.5.0

- 更新 `/status` usage 统计：
  状态页会汇总当前 conversation 下 foreground、background、subagent 以及 provider-backed media tool 的 token usage，并按 cache read、cache write、uncached input、output 分桶显示 token 和美元开销，金额保留三位小数。
- 扩展 `TokenUsage`：
  provider 若返回成本字段，会记录可选 `cost_usd`，包含 cache read、cache write、uncached input、output 四类开销；没有明确成本字段的 provider 继续只记录 token 数，金额显示为 `$0.000`。
- 更新 `config` schema 到 `0.5`：
  root 新增 `skill_sync`，格式为 `[{ "skill_name": ["A", "B"], "upstream": ["C", "D"] }]`。`skill_update` 会把匹配 skill commit 后 push 到所有 upstream；每个 push 约 4 秒超时，失败只写 warning 并返回在工具结果中，不阻断 skill 更新。
- 移除模型可见的 `skill_set_upstream` 工具：
  skill upstream 同步目标现在由部署 config 管理，不再由会话运行时工具写入。
- 修复 Telegram 进度中的 `update_plan` 渲染：
  `update_plan` 现在会更新当前 turn 的内存态计划并刷新 progress 消息；每个 user 大轮结束、失败或进入 idle 后自动清空，不写入持久化 state。
- 更新 `workdir` schema 到 `0.6`：
  session/media usage 日志支持可选 `token_usage.cost_usd`。旧日志无需重写，本次迁移为兼容性 no-op。

# 1.4.7

- 修复 OpenRouter chat-completions 图片输出读取超时：
  `open_router_completion` 在模型具备 `image_out` 且本轮没有工具调用 schema 时，会使用 OpenRouter streaming chat-completions 协议读取图片输出。OpenRouter 会在长时间图片生成期间发送 keepalive 事件，避免非流式请求已经返回 `200 OK` 但完整 body 迟迟未结束而触发 body read timeout。
- Streaming 图片输出解析：
  支持 `choices[].delta.images[].image_url.url`，并继续解析最终 chunk 中的 `usage`，用于 provider-backed media tool usage 记录。
- 本次不改变 `config` schema，不改变 `workdir` schema：
  当前 config schema 仍为 `0.4`；当前 Stellaclaw workdir schema 仍为 `0.5`。

# 1.4.6

- 修复 OpenRouter 图片输出请求协议：
  `open_router_completion` 和 `open_router_responses` 在模型具备 `image_out` 时会按模型能力自动发送 `modalities`；同时保留 `chat` 能力时发送 `["image", "text"]`，纯图片输出模型发送 `["image"]`。
- 补齐 OpenRouter Responses 图片输出解析：
  支持 `image_generation_call.result` 的裸 base64 图片，以及 `openrouter:image_generation` server-tool 输出中的 `result`、`imageUrl`、`imageB64`。
- 改进 `open_router_responses` 响应诊断：
  请求会显式要求 `Accept-Encoding: identity`；响应体读取失败带 URL/status 和错误链，JSON 解析失败带 body preview。
- 本次不改变 `config` schema，不改变 `workdir` schema：
  当前 config schema 仍为 `0.4`；当前 Stellaclaw workdir schema 仍为 `0.5`。

# 1.4.5

- 改进 `open_router_completion` provider 响应诊断：
  OpenRouter chat completion 请求会显式要求 `Accept-Encoding: identity`；响应体读取失败会带上请求 URL 和状态码，成功状态下 JSON 解析失败会带上响应体预览，便于排查 OpenRouter-compatible 图片模型返回空响应、HTML、损坏压缩体或非 chat-completion JSON。
- 本次不改变 `config` schema，不改变 `workdir` schema：
  当前 config schema 仍为 `0.4`；当前 Stellaclaw workdir schema 仍为 `0.5`。

# 1.4.4

- 改进 `openai_image` provider 响应诊断：
  图片生成/编辑请求会显式要求 `Accept-Encoding: identity`；响应体读取失败会带上请求 URL 和状态码，JSON 解析失败会带上响应体预览，便于排查上游返回空响应、HTML、损坏压缩体或非 OpenAI-compatible JSON。
- 记录 provider-backed media tool token usage：
  图片生成、图片分析、PDF 分析、音频分析等工具模型调用完成后，如果 provider 返回 `token_usage`，会 append 到 workspace 下 `.log/stellaclaw/tool_usage.jsonl`，不混入对话历史。
- 本次不改变 `config` schema，不改变 `workdir` schema：
  当前 config schema 仍为 `0.4`；当前 Stellaclaw workdir schema 仍为 `0.5`。

# 1.4.3

- 修复 `codex_subscription` provider 复用缓存 WebSocket 时的断连恢复：
  如果缓存 socket 在请求读写时返回 WebSocket transport error，会丢弃旧连接、重新握手并重发一次，避免闲置连接被上游 reset 后直接让本轮失败。
- 更新 `ROAD_MAP.md`：
  增加 `SessionActor` 和 `Conversation` 当前内部构筑的 Mermaid 图，以及后续拆分候选边界。
- 本次不改变 `config` schema，不改变 `workdir` schema：
  当前 config schema 仍为 `0.4`；当前 Stellaclaw workdir schema 仍为 `0.5`。

# 1.4.2

- 修复 context compression 请求缺少 system prompt 的问题：
  主动压缩和 idle compaction 调用 provider 生成摘要时，现在会携带当前 session system prompt，避免 `codex_subscription` provider 因缺少 `instructions` 返回 400。
- 本次不改变 `config` schema，不改变 `workdir` schema：
  当前 config schema 仍为 `0.4`；当前 Stellaclaw workdir schema 仍为 `0.5`。

# 1.4.1

- 修复 HuggingFace chat template 兼容性：
  token estimation 改用 Jinja2 compatibility 环境，并在编译模板前把 Jinja 标签内部的 `foo.0` 数字属性访问归一化为 `foo[0]`，兼容 GLM-5.1 等 Transformers 模板，避免压缩时失败于 `unexpected float`。
- 本次不改变 `config` schema，不改变 `workdir` schema：
  当前 config schema 仍为 `0.4`；当前 Stellaclaw workdir schema 仍为 `0.5`。

# 1.4.0

- 更新 `config` schema 到 `0.4`：
  `sandbox` 新增可选 `software_dir` 和 `software_mount_path`。配置后，subprocess 模式会设置 `STELLACLAW_SOFTWARE_DIR` 指向该目录；bubblewrap 模式会把该目录挂载到 `software_mount_path`，默认 `/opt`，并设置同名环境变量。
- 明确 workspace 共享目录：
  每个 conversation workspace 的 `shared/` 指向同一个 `rundir/shared`，可用于跨 workspace 共享文件。
- 更新 system prompt：
  提示模型 `shared/` 是跨 conversation 共享目录，并提示通过 `STELLACLAW_SOFTWARE_DIR` 查找配置的软件目录位置。
- 更新 `workdir` schema 到 `0.5`：
  新增 `rundir/.skill_upstreams/` 保存共享 skill 的 upstream git repo 配置。
- 新增 `skill_set_upstream`：
  可为运行目录共享 skill 配置 `repo_url` 和可选 `branch`；之后 `skill_update` 会自动提交并 `git push -u origin <branch>`。仓库权限由部署环境自行保证。
- 本次改变 `config` schema 和 `workdir` schema：
  当前 config schema 更新为 `0.4`；当前 Stellaclaw workdir schema 更新为 `0.5`。

# 1.3.3

- 改进 provider 请求失败诊断：
  `request failed` 现在会保留 reqwest/source 错误链，便于区分连接中断、超时、TLS、DNS 或 body 读取失败等发送阶段错误。
- 本次不改变 `config` schema，不改变 `workdir` schema：
  当前 config schema 仍为 `0.3`；当前 Stellaclaw workdir schema 仍为 `0.4`。

# 1.3.2

- 修复 `openai_image` provider 对 OpenAI-compatible image adapters 的兼容性：
  不再强制发送 `response_format=b64_json`，避免不支持该参数的上游返回 `Unknown parameter: 'response_format'`。
- 修正 image provider HTTP 错误中的 URL：
  失败信息现在显示实际请求的 `/images/generations` 或 `/images/edits` endpoint，而不是配置中的 API base URL。
- 本次不改变 `config` schema，不改变 `workdir` schema：
  当前 config schema 仍为 `0.3`；当前 Stellaclaw workdir schema 仍为 `0.4`。

# 1.3.1

- 修正 `openai_image` provider 的 URL 语义：
  配置中的 `url` 规范上应指向 OpenAI-compatible API base，例如 `https://host/v1`；provider 会按功能自动路由到 `/images/generations` 或 `/images/edits`。
- 保留 endpoint URL 兼容：
  如果旧配置仍写成 `/v1/images/edits` 或 `/v1/images/generations`，provider 仍会推导到对应 sibling endpoint，避免已有部署立刻失效。
- 本次不改变 `config` schema，不改变 `workdir` schema：
  当前 config schema 仍为 `0.3`；当前 Stellaclaw workdir schema 仍为 `0.4`。

# 1.3.0

- 扩展 OpenAI-compatible image provider：
  新增 canonical provider type `openai_image`，同时保留 `openai_image_edit` 作为兼容 alias。
- 合并 Images Generations 和 Images Edits：
  provider request 带 user image 时使用 `multipart/form-data` 调用 `/v1/images/edits`；不带 user image 时使用 JSON 调用 `/v1/images/generations`。配置中的 `url` 可以指向 edits 或 generations，provider 会自动推导对应 sibling endpoint。
- 本次不改变 `config` schema，不改变 `workdir` schema：
  当前 config schema 仍为 `0.3`；当前 Stellaclaw workdir schema 仍为 `0.4`。

# 1.2.0

- 新增 `openai_image_edit` provider：
  支持 OpenAI Images Edits 兼容接口，使用 `multipart/form-data` POST 到配置中的 `/v1/images/edits` 端点，发送 `model`、`prompt`、第一张 user image 和 `response_format=b64_json`，并把返回的 `b64_json` 图片持久化为本地 `FileItem`。
- `openai_image_edit` 面向图片编辑：
  provider request 必须包含至少一张 user 图片；纯文本生图仍应使用支持 generation 协议的 provider。
- 本次不改变 `config` schema，不改变 `workdir` schema：
  当前 config schema 仍为 `0.3`；当前 Stellaclaw workdir schema 仍为 `0.4`。

# 1.1.4

- 修复 provider-backed media 工具的空 id 参数处理：
  `image_generation` 的空 `generation_id`、以及 image/pdf/audio analysis 的空白 job id 现在会按“未提供”处理并启动新任务，不再错误查询空字符串 job id。
- 本次不改变 `config` schema，不改变 `workdir` schema：
  当前 config schema 仍为 `0.3`；当前 Stellaclaw workdir schema 仍为 `0.4`。

# 1.1.3

- 更新 `workdir` schema 到 `0.4`：
  conversation 持久化的 `session_profile.main_model` 不再保存完整 `ModelConfig` 快照，而是保存当前选择的 model alias；后台和 subagent 的 `model_override` 也改为保存 alias。
- 新增旧 conversation 迁移：
  `0.3 -> 0.4` 会把旧的完整模型快照折叠成当前 config 中的 alias；优先精确匹配完整模型配置，其次按 provider `model_name` 匹配，因此全局 config 中的多模态尺寸、能力和工具模型配置更新会被已有 conversation 重新读取。
- 本次不改变 `config` schema，更新 `workdir` schema：
  当前 config schema 仍为 `0.3`；当前 Stellaclaw workdir schema 更新为 `0.4`。

# 1.1.2

- 修复 assistant/user 多模态角色降级：
  `image_in` 只表示 user 消息可以携带直接图片输入；assistant 历史里的图片只有在模型具备 `image_out` 时才保留为文件，否则降级为文本引用。
- 收紧 provider 编码：
  Claude 和 OpenRouter chat completions 不再把 assistant 历史图片误编码成 direct image input；OpenRouter chat completions 会把 assistant 图片保留为文本 URI，避免图片历史被静默丢弃。
- 本次不改变 `config` schema，不改变 `workdir` schema：
  当前 config schema 仍为 `0.3`；当前 Stellaclaw workdir schema 仍为 `0.3`。

# 1.1.1

- 打开前台运行日志：
  `run.sh` 默认设置 `STELLACLAW_LOG_STDOUT=1`，Host 日志仍写入文件，同时会镜像到 stdout，方便直接用前台进程排障。
- 本次不改变 `config` schema，不改变 `workdir` schema：
  当前 config schema 仍为 `0.3`；当前 Stellaclaw workdir schema 仍为 `0.3`。

# 1.1.0

- 更新 `config` schema 到 `0.3`：
  `session_defaults` 中的 `image_tool_model`、`pdf_tool_model`、`audio_tool_model`、`image_generation_tool_model` 和 `search_tool_model` 现在正式使用 `<model_alias>[:self]` 形式；旧的完整 `ModelConfig` 对象仍可读取，升级时会尽量折叠为匹配的 model alias。
- 更新 [example_config.json](example_config.json) 为原生 `0.3` 配置形态：
  顶层 `models` 使用当前 runtime 字段名，工具模型默认值使用 alias 字符串。
- 本次改变 `config` schema，不改变 `workdir` schema：
  当前 config schema 更新为 `0.3`；当前 Stellaclaw workdir schema 仍为 `0.3`。

# 1.0.3

- 修复 `claude_code` provider 的 prompt cache breakpoint：
  当请求包含 system prompt 时，`cache_control` 现在挂在 Anthropic Messages API 要求的 system text block 上，而不是把 system 作为普通字符串并只缓存最后一个 message block。
- 本次不改变 `config` schema，不改变 `workdir` schema：
  当前 config schema 仍为 `0.2`；当前 Stellaclaw workdir schema 仍为 `0.3`。

# 1.0.2

- 新增项目根 README：
  基于当前 `ROAD_MAP.md` 和已落地代码状态，重新说明 Stellaclaw 的定位、架构、Telegram 体验、工具系统、skills、多模态降级、provider、快速启动、版本文件和 CI/CD。
- 隐藏 provider reasoning 中不可展示的内容：
  provider 返回的 encrypted reasoning 不再被当作普通文本写入 assistant 消息；Host 输出渲染也会跳过 reasoning item，避免 Telegram 偶发显示大段 `[reasoning] {"encrypted_content": ...}`。
- 收紧 `subagent_start` 模型选择：
  subagent 不再暴露或接受模型覆盖，总是继承当前 conversation 的主模型，避免模型自行试错不存在的 named model。
- 强化 `ChatMessage::Reasoning`：
  新增 Codex 专用 `codex_summary` 和 `codex_encrypted_content` 字段，允许 Codex subscription provider 按官方 Responses reasoning item 形态把 encrypted reasoning continuation 带回下一轮，同时不把 raw reasoning text 放进普通文本上下文。
- 新增双层消息 normalization：
  `SessionActor` 先执行通用模型能力降级，再调用 provider 特化 normalization；Codex subscription provider 会净化并保留 encrypted reasoning，其他 provider 默认沿用通用结果。
- 扩展 token 估算：
  `codex_encrypted_content` 会按 Codex 的 encrypted reasoning 长度估算规则计入上下文成本，避免保留 continuation 后低估上下文长度。
- 修复新用户消息对运行中 tool batch 的打断语义：
  当 active tool batch 正在运行时，新的 foreground 用户消息会由 `SessionActor` 触发 cooperative interrupt；工具返回稳定结果后旧 turn 在安全边界 yield，新消息作为下一轮继续处理，而不是一直排队等工具自然结束。
- 修复 Telegram 前台进度面板的打断交接：
  新消息接管当前回合时不再发送固定“已被打断”文案，也不会新建第二个进度框；后续状态会复用并编辑当前 active progress message。
- 新增根目录 `run.sh`：
  支持不依赖 systemd 的前台启动路线，用户可直接传入 config 路径，由脚本构建 release binary、推导 workdir 并在当前终端运行。
- 本次不改变 `config` schema，更新 `workdir` schema：
  当前 config schema 仍为 `0.2`；当前 Stellaclaw workdir schema 更新为 `0.3`，用于记录 `ChatMessage::Reasoning` 的 Codex continuation 字段扩展。

# 1.0.1

- 修复 `skill_update` 对 `SKILL.md` YAML frontmatter 的误判：
  校验逻辑现在会正确扫描 `name` 后面的 `description` 字段，不会再因为 `description` 不是第一行而错误返回 `frontmatter must contain description`。
- 扩展 skill frontmatter description 解析：
  支持普通 scalar、quoted scalar 和简单 folded/block YAML 描述；runtime skill metadata 也会优先使用 frontmatter description，避免工具校验和运行时刷新显示不一致。
- 本次不改变 `config` schema，不改变 `workdir` schema：
  当前 config schema 仍为 `0.2`；当前 Stellaclaw workdir schema 仍为 `0.2`。

# 1.0.0

- 将当前 Stellaclaw / core / agent_server 架构作为新的稳定起点：
  Host 由 `stellaclaw` 承载，session 执行边界由独立 `agent_server` 承载，核心会话、provider、tool batch、Telegram channel 和 workdir upgrade 链路已经落地。
- 建立新的版本维护规则：
  根目录 `VERSION` 记录项目发布版本和 changelog；config schema 版本继续由 `stellaclaw/src/config/mod.rs` 管理；workdir schema 版本继续由 `stellaclaw/src/upgrade/mod.rs` 和运行目录 `STELLA_VERSION` 管理。
- 建立 GitHub Actions CI/CD：
  main / pull request 会运行 Rust 格式检查和 workspace 测试；main 上根 `VERSION` 变化且 tag 不存在时自动构建 release artifact 并发布 GitHub Release。
- 本次不改变 `config` schema，不改变 `workdir` schema：
  当前 config schema 仍为 `0.2`；当前 Stellaclaw workdir schema 仍为 `0.2`。

# Versioning

- 项目发布版本使用 SemVer，从 `1.0.0` 开始编号。
- 根 `VERSION` 的 `version:` 字段是当前项目发布版本。
- `config` schema version 和 `workdir` schema version 独立维护，不要求和项目发布版本相同。
