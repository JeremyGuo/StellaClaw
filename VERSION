version: 0.15.5
---

# 0.15.5

- 新增 conversation 级别的 `remote_workpaths`：
  foreground/background 等同一 conversation 下的 agent 共享远端 SSH 工作目录登记；`workpath_add` 会保存 host/path/description，并立即尝试读取远端 `AGENTS.md`，后续重建 system prompt 时会自动拼接登记信息并重新加载远端规则。
- 调整 compaction 提示：
  context compaction 不再要求摘要重复保存 remote workpath 的 host/path/description 或远端 `AGENTS.md` 内容，这些信息会由 conversation settings 重建。
- 改进 context token 估算：
  `agent_frame` 改用开源 `tiktoken-rs` 的 `o200k_base` BPE tokenizer 估算文本与工具 schema token 数，不再使用字符/字节比例粗估；内联图片、文件和音频 payload 仍按固定占位成本估算，避免大块 base64 被完整 tokenize。
- 升级 `workdir` 结构版本到 `0.22`：
  `0.21 -> 0.22` upgrade step 会为旧 conversation 的 `settings` 回填 `remote_workpaths: []`；本次不影响 `config` schema。
- 本次只影响 `workdir` schema，不影响 `config` schema：
  新增 workdir upgrade step；按 workdir-only 变更策略将仓库版本从 `0.15.4` 提升到 `0.15.5`。

# 0.15.4

- 修复 `agent_frame` integration tests 的 CI 回归：
  部分测试应保持单次 controlled run 或自动 resume 到结束，现已恢复各自原有语义，避免取消测试被错误地继续执行以及 usage 聚合测试只跑到 yielded 状态。
- 本次不影响 `config` 或 `workdir` schema：
  没有新增配置字段或 workdir migration；按普通 bug fix 将仓库版本从 `0.15.3` 提升到 `0.15.4`。
- 新增实时 interactive channel feedback：
  AgentFrame 会通过独立 progress API 向 AgentHost 推送当前执行状态；模型调用阶段显示“思考中...”，工具执行阶段显示当前工具列表以及每个工具的执行中/已完成/失败状态。
- Telegram 进度消息现在绑定到 foreground session 的 `session_state.progress_message`：
  首次发送进度消息后持久化 channel message id，正常完成时删除并清理；服务重启后会扫描旧的 progress message id 并尽力删除，避免“正在执行”消息长期残留。
- 升级 `workdir` 结构版本到 `0.21`：
  `0.20 -> 0.21` upgrade step 会为旧 session 的 `session_state` 回填 `progress_message: null`；本次不影响 `config` schema。
- 本次只影响 `workdir` schema，不影响 `config` schema：
  新增 workdir upgrade step；按 workdir-only 变更策略将仓库版本从 `0.15.2` 提升到 `0.15.3`。
- 新增 Per-Tool Remote SSH Execution：
  `exec_start`、`file_read`、`file_write`、`glob`、`grep`、`ls`、`edit` 与 `apply_patch` 支持可选 `remote="host"`，单次工具调用通过本机 `ssh host` 在远端执行；本地操作仍默认 local，prompt 明确要求省略 `remote` 以节省 Token。
- `exec_wait`、`exec_observe` 与 `exec_kill` 会通过 `exec_id` 复用进程 metadata 中记录的 `remote`，不要求模型重复填写远端 host。
- 升级 `workdir` 结构版本到 `0.20`：
  exec 进程 metadata/status 新增 `remote` 字段，exec worker job 也会携带远端信息；`0.19 -> 0.20` upgrade step 会为旧 metadata/status 回填 `"local"`，并为旧 exec worker job 回填 `null`；本次不影响 `config` schema。
- 本次只影响 `workdir` schema，不影响 `config` schema：
  新增 workdir upgrade step；按 workdir-only 变更策略将仓库版本从 `0.15.1` 提升到 `0.15.2`。

# 0.15.1

- 修复 `sandbox.map_docker_socket=false` 时 bubblewrap 仍通过 `--ro-bind /run /run` 暴露 `/run/docker.sock` 的问题：
  不再整体绑定 `/run`，仅保留 `/run/systemd/resolve` 只读绑定以兼容常见的 `systemd-resolved` DNS 配置；Docker socket 只在显式开启 `map_docker_socket` 时单独绑定。
- 本次不影响 `config` schema 和 `workdir` schema：
  仅修正 Linux bubblewrap 挂载行为，按普通 bug fix 将仓库版本从 `0.15.0` 提升到 `0.15.1`。

# 0.15.0

- 新增 `sandbox.map_docker_socket` 配置：
  Docker socket 映射改为显式开关，默认关闭；只有 Linux + `bubblewrap` + `map_docker_socket=true` + `/run/docker.sock` 存在时才会把 Docker socket bind 进沙盒，macOS、Windows 和 `subprocess` 模式会跳过该功能。
- 更新配置版本到 `0.15`：
  新增 `agent_host/src/config/v0_15.rs` loader，保留 `0.14` 及更早配置升级路径；TUI 配置编辑器、最新配置骨架和示例配置均补齐 `sandbox.map_docker_socket`。
- 更新部署文档：
  `docs/DEPLOY.md` 说明 Docker socket 映射的正确开启方式，并明确不要在 systemd user service 中手工加入 `SupplementaryGroups=docker`，避免 `status=216/GROUP`。
- 本次影响 `config` schema，不影响 `workdir` schema：
  没有新增 workdir upgrade step；按配置变更策略将仓库版本从 `0.14.12` 提升到 `0.15.0`。

# 0.14.12

- 调整 `image_load` 过量保护策略：
  不再把超过请求预算的图片静默降级为文本占位符；改为在 prompt 和工具描述中明确要求同一批最多并发 3 个 `image_load`，运行时也会让第 4 个及之后的同批 `image_load` 返回失败，避免模型误以为仍然看到了图片。
- 保留 Codex WebSocket 坏连接恢复改进：
  旧连接发送失败后，如果恢复请求仍失败，会尽力替换掉已关闭的 socket，避免后续 `/continue` 先复用坏连接并报 `Sending after closing`。
- 本次不影响 `config` 或 `workdir` schema：
  没有新增用户配置字段、config loader 或 workdir upgrade step；按 patch 发布将仓库版本从 `0.14.11` 提升到 `0.14.12`。

# 0.14.11

- 修复 Codex subscription 在大批量图片上下文下容易失败的问题：
  模型请求构造阶段会限制单次请求内联图片 data URL 总 payload，优先保留最近图片，超过预算的旧图片会以文本占位符替代，避免一次上传十余 MB base64 导致 WebSocket 在 `response.completed` 前被上游关闭。
- 改进 Codex WebSocket 恢复路径：
  旧连接发送失败后，如果恢复请求仍失败，会尽力替换掉已关闭的 socket，避免后续 `/continue` 先复用坏连接并报 `Sending after closing`。
- 本次不影响 `config` 或 `workdir` schema：
  没有新增用户配置字段、config loader 或 workdir upgrade step；按 patch 发布将仓库版本从 `0.14.10` 提升到 `0.14.11`。

# 0.14.10

- 修复 Codex subscription 前台 turn 可能无限卡住的问题：
  WebSocket 连接现在会把模型 `timeout_seconds` 应用到底层 TCP read/write timeout，避免上游不返回 `response.completed` 或 `response.failed` 时一直阻塞并持续发送 typing。
- 修复 Codex subscription 请求携带不兼容 prompt cache 字段的问题：
  ChatGPT Codex WebSocket 请求不再发送 `prompt_cache_key` / `prompt_cache_retention`，避免上游返回 `Unsupported parameter: prompt_cache_retention`。
- 本次不影响 `config` 或 `workdir` schema：
  没有新增用户配置字段、config loader 或 workdir upgrade step；按 patch 发布将仓库版本从 `0.14.9` 提升到 `0.14.10`。

# 0.14.9

- 修正 exec 工具结果的成功/失败标记：
  正常 exec 返回体里的 `"error": null` 不再被 session event 误判为 `errored=true`；显式错误或 `"failed": true` 仍会按失败记录。
- 修正 exec stdout/stderr 截断状态：
  完整短输出（例如 `echo hello`、`printf 123`）不再因为末尾换行计数差异被误标为 `stdout_truncated=true`，只有行窗口或字符窗口实际裁剪时才标记为截断。
- 本次不影响 `config` 或 `workdir` schema：
  没有新增用户配置字段、config loader 或 workdir upgrade step；按 patch 发布将仓库版本从 `0.14.8` 提升到 `0.14.9`。

# 0.14.8

- 修复 exec 输出包含非 UTF-8 字节时阻塞上下文压缩的问题：
  `exec_start`、`exec_wait`、`exec_observe` 与 active runtime summary 读取 stdout/stderr 时改为 bytes + lossy UTF-8 解码，避免 LaTeX、二进制片段或扩展编码输出导致 `stream did not contain valid UTF-8`。
- 本次不影响 `config` 或 `workdir` schema：
  没有新增用户配置字段、config loader 或 workdir upgrade step；按 patch 发布将仓库版本从 `0.14.7` 提升到 `0.14.8`。

# 0.14.7

- 修复阈值上下文压缩在包含工具调用历史时的兼容性问题：
  compaction 请求发送给上游前会把 `assistant.tool_calls` 与 `tool` 角色结果渲染为普通文本 transcript，不再把协议级 tool-call 消息发给不带 `tools` 定义的压缩模型，避免部分 Chat Completions 兼容服务返回 `NoneType object is not iterable`。
- 本次不影响 `config` 或 `workdir` schema：
  没有新增用户配置字段、config loader 或 workdir upgrade step；按 patch 发布将仓库版本从 `0.14.6` 提升到 `0.14.7`。

# 0.14.6

- 优化 exec 工具轮次开销：
  `exec_start` 默认等待命令完成，默认等待 270 秒；长任务可显式传 `return_immediate=true` 保持原来的后台启动行为。等待超时默认 `on_timeout=continue`，进程继续运行；也可传 `on_timeout=kill` 在超时时终止进程。
- 收紧 exec 输出返回体积：
  `exec_start`、`exec_wait` 与 `exec_observe` 的 `max_output_chars` 硬限制为 0 到 1000；超过限制时只返回前部与尾部片段，并提供完整 stdout/stderr 的 workspace 相对路径 `.agent_frame/exec/<exec_id>.stdout|stderr` 供后续定向读取。
- 更新 agent prompt：
  鼓励模型把无因果依赖的 exec 调用并发发起，优先使用 `exec_start` 默认等待完成模式，并在预期大输出时设置 `max_output_chars=0` 后读取保存的输出文件。
- 修复前台 user_tell 中断竞态：
  当用户插话刚好发生在旧 foreground turn 完成或 yield 的边界时，新的 foreground control 会立即消费 pending interrupt，避免 user_tell 打断信号丢失。
- 本次不影响 `config` 或 `workdir` schema：
  没有新增用户配置字段、config loader 或 workdir upgrade step；`.agent_frame/exec/` 是 exec 输出工件目录，不需要迁移历史 workdir；按 patch 发布将仓库版本从 `0.14.5` 提升到 `0.14.6`。

# 0.14.5

- 压缩上下文请求会在发送给上游前清理内联多模态 payload：
  图片、文件与音频内容会被替换为文本占位符，避免 context compression 阶段重复上传大体积 base64 数据；原始会话内容不变。
- OpenRouter Responses / Codex subscription 请求补齐 prompt cache 字段转发：
  `cache_control`、`prompt_cache_key` 与 `prompt_cache_retention` 现在会随 Responses payload 一起发送，OpenRouter Responses Claude 模型也会获得默认 5 分钟 ephemeral cache。
- 更新部署模板中的模型可用性：
  `deploy_telegram.json` 将 `gpt54` 加入 `agent_frame.available_models`，并收紧部分模型说明，避免默认被随意用作 subagent。
- 本次不影响 `config` 或 `workdir` schema：
  没有新增用户配置字段、config loader 或 workdir upgrade step；按 patch 发布将仓库版本从 `0.14.4` 提升到 `0.14.5`。

# 0.14.4

- 新增 Windows 原生运行兼容：
  `partyclaw` 现在可通过 Windows target 编译，运行工具命令时会使用 `%COMSPEC% /C`，后台任务进程检测与终止改用 `tasklist` / `taskkill`；Unix 平台继续保留原有 `sh`、PTY 与 `kill` 行为。
- 将 Unix-only 依赖收敛到目标平台依赖：
  `agent_frame` 的 `nix` / `libc` 和 `agent_host` 的 `libc` 只在 Unix target 下启用，避免 Windows 原生构建拉入不兼容 API。
- 更新部署文档：
  `docs/DEPLOY.md` 现在分别给出 Linux、macOS 与 Windows 原生命令，Windows 服务化示例改为 PowerShell + 任务计划程序。
- 本次不影响 `config` 或 `workdir` schema：
  不需要新增 config loader 或 workdir upgrade step；按显式 patch 发布要求将仓库版本从 `0.14.3` 提升到 `0.14.4`。

# 0.14.3

- 修复 `agent_frame` 在 musl 目标下交叉编译失败的问题：
  `libc::ioctl` 的 `request` 参数在 glibc 上是 `c_ulong`（`u64`），在 musl 上是 `c_int`（`i32`）；将 `TIOCSCTTY as libc::c_ulong` 改为 `TIOCSCTTY as _` 让编译器自动推导，兼容两种 libc。

# 0.14.2

- 修复 `cargo fmt` 格式问题导致 v0.14.1 CI 失败、Release 被跳过的问题。
- 无功能变更，仅重新触发 Release 流水线。

# 0.14.1

- 修复 bubblewrap 常驻子进程被 SIGKILL 的问题：
  `bwrap --die-with-parent` 使用 `PR_SET_PDEATHSIG(SIGKILL)` 跟踪的是**产生子进程的线程**而非进程；`PersistentChildRuntime::spawn()` 在 `tokio::task::spawn_blocking()` 内运行，当 tokio blocking pool 空闲线程被回收（~10s）后，Linux 会向 bwrap 子进程发送 SIGKILL，即使父进程仍然存活。修复方式：新增 `spawn_with_persistent_parent_thread()`，从一个专用的 `bwrap-parent-keeper` 线程产生子进程，该线程会一直 park 到子进程退出，避免线程回收触发误杀。
- 明确本次不影响 `config` 与 `workdir` schema：
  没有新增 config loader，也没有新增 workdir upgrade step；这是一次运行时 bug 修复。

# 0.14.0

- 升级 `config` 保存格式到 `0.14`：
  每个模型配置新增 `retry_mode` object，默认 `{"mode":"no"}`，失败时立即返回用户；`{"mode":"random","max_retries":N,"retry_random_mean":S}` 会在失败后按近似正态分布随机等待后重试。
- `agent_frame` 上游请求统一支持按模型重试：
  主对话、工具模型和压缩调用都会读取对应模型的 `retry_mode`；已有 `previous_response_id` fallback 的错误不会先消耗随机重试。
- TUI config editor 新增 `retry_mode.mode` 选择框，并在选择 `random` 时显示 `max_retries` 与 `retry_random_mean`；最新示例配置与部署模板均补齐到 `0.14`。
- `deploy_telegram.json` 的 `opus-4.6` 已启用 random retry：
  最大重试 2 次，随机等待均值 8 秒；其他模型保持默认 `no`。
- 本次同时包含 `workdir` `0.18 -> 0.19` 的 SessionState 冗余字段清理；最终仓库版本按 config schema 变更策略提升到 `0.14.0`，不再单独停留在 patch 版本。
- 升级 `workdir` 结构版本到 `0.19`：
  最新 `sessions/*/session.json` 不再持久化旧的 `idle_compaction_retry` 专用状态；新增 `0.18 -> 0.19` upgrade step 会移除该字段，idle compaction 失败继续只通过 `session_state.errno/errinfo` 表达。
- 继续收敛 SessionState 作为 durable 会话真相：
  移除 `SessionSnapshot.message_count` 这类旧 history 计数字段，并清理不再序列化的 `agent_messages` legacy 写入 clone；旧 snapshot / workdir 读取兼容仍保留在反序列化路径中。
- 明确本次只影响 `workdir`，不影响 `config`：
  新增 `0.18 -> 0.19` workdir upgrade step；按版本策略将仓库版本从 `0.13.0` 提升到 `0.13.1`。
- 升级 `config` 保存格式到 `0.13`，本次不影响 `workdir`：
  删除 `sandbox.mode = "disabled"` 这一用户可选模式，最新配置与 TUI 只保留 `subprocess` / `bubblewrap`；旧配置里残留的 `"disabled"` 会在加载时自动视为 `"subprocess"` 并在升级后写回最新版本。
- 前台 `agent_frame` 开始支持会话级常驻 child runtime：
  `MainForeground` 路径会按 session 复用同一个 child 进程；模型切换或 sandbox 切换时，该 runtime 会被显式销毁并重建，避免把 provider continuation 状态泄漏到不兼容的新环境里。
- `agent_frame` 内部重新收回 provider continuation 优化：
  `previous_response_id` / provider session 现在只由常驻 runtime 私有维护，不再穿过 `agent_host` 的 config 或持久化状态；host 继续只围绕 canonical `SessionState` 工作。
- 明确本次只影响 `config`，不影响 `workdir`：
  新增 `0.12 -> 0.13` config loader，并按版本策略将仓库版本从 `0.12.5` 提升到 `0.13.0`。
- 升级 `workdir` 结构版本到 `0.18`：
  最新 `sessions/*/session.json` 不再持久化旧的 `agent_messages` 字段；新增 `0.17 -> 0.18` upgrade step 会把历史 workdir 里残留的这个旧稳定 transcript 副本移除，让 durable 会话状态只围绕 `session_state.messages/pending_messages` 展开。
- 继续清理前台会话的双轨状态：
  `SessionSnapshot` 和内存态 `Session` 都不再单独维护 `agent_messages` 镜像，稳定 transcript 统一收敛到 `session_state.messages`，兼容旧磁盘格式的责任则留在 upgrade 和反序列化 fallback。
- 明确本次只影响 `workdir`，不影响 `config`：
  新增 `0.17 -> 0.18` workdir upgrade step；按版本策略将仓库版本从 `0.12.4` 提升到 `0.12.5`。
- 升级 `workdir` 结构版本到 `0.17`：
  新增 `0.16 -> 0.17` upgrade step，把历史 `session_state.errno = "legacy_pending_continue"` 统一重写为中性的 `runtime_failure`；这让旧恢复壳子的痕迹只停留在升级链里，不再暴露到最新运行时和最新持久化语义中。
- 继续清理前台恢复模型中的过渡概念：
  integration 与 upgrade 断言现在都只围绕 `session_state.phase`、`pending_messages` 和中性的 `runtime_failure` 工作，不再要求运行时代码理解 `LegacyPendingContinue` 这类兼容专用错误名。
- 明确本次只影响 `workdir`，不影响 `config`：
  新增 `0.16 -> 0.17` workdir upgrade step；按版本策略将仓库版本从 `0.12.3` 提升到 `0.12.4`。
- 升级 `workdir` 结构版本到 `0.16`：
  `sessions/*/session.json` 不再持久化旧的 `pending_continue` 字段；升级会保留已存在的 `session_state`，并把残留的 `pending_continue` 从持久化文件里移除，让运行时代码只围绕新的 durable `session_state` 恢复模型工作。
- 删除前台运行时对旧恢复壳子的依赖：
  foreground 普通消息、打断消息和 `/continue` 现在都直接使用 `session_state`、`pending_messages` 和 `errno/errinfo` 驱动恢复，不再把新的失败状态回写成 legacy `pending_continue`。
- 收紧前台用户消息的 durable staging 语义：
  用户消息在进入 `pending_messages` 时会同步写入可见历史，成功或 yielded 后不再重复补写 user history；这进一步减少了继续执行和失败恢复对旧 `original_user_text/original_attachments` 元数据的依赖。
- 明确本次只影响 `workdir`，不影响 `config`：
  新增 `0.15 -> 0.16` workdir upgrade step；按版本策略将仓库版本从 `0.12.2` 提升到 `0.12.3`。
- 升级 `workdir` 结构版本到 `0.15`：
  `sessions/*/session.json` 新增 durable `session_state`，集中持久化 `messages`、`pending_messages`、`phase` 与 `errno/errinfo`，为后续收敛 `/continue`、yield 与中断恢复语义打基础；升级会把旧的 `agent_messages` / `pending_continue.resume_messages` 自动投影成新的 `session_state`。
- 前台 session 持久化开始同步写入新的恢复视图：
  当前台 turn 正常结束、yield、idle compaction 失败或旧版 `pending_continue` 被恢复时，AgentHost 现在会同步维护 `session_state`，让 durable 状态不再只靠分散字段推断。
- 前台失败态开始写入结构化 errno：
  当前台 turn 因 threshold compaction、tool-wait compaction、上游 API 或其他 runtime 错误停在稳定点时，持久化的 `session_state.errno/errinfo` 会记录对应失败类型与摘要，而不再统一退化成“只有旧 `pending_continue`”。
- 明确本次只影响 `workdir`，不影响 `config`：
  新增 `0.14 -> 0.15` workdir upgrade step；按版本策略将仓库版本从 `0.12.1` 提升到 `0.12.2`。

# 0.12.1

- 升级 `workdir` 结构版本到 `0.14`：
  每个 workspace 的 `files/` 根目录新增隐藏的 `.context_attachments/` 目录，用来承接模型切换时从历史上下文中拆出的多模态引用文件；升级会为已有 workspace 补齐该目录。
- 在 `/agent` 切换模型时显式自愈历史多模态上下文：
  当前台会话已有历史 `agent_messages` 或 `pending_continue.resume_messages` 包含图片、文件或音频，而新选中的 backend/model 不再支持对应输入时，系统现在会把这些内联 payload 单向降级成普通文本引用，并把可落盘的原始内容保存到 workspace 的 `.context_attachments/<uuid>/` 下面，避免后续继续对话时把失配模态再次直接发给上游。
- `/agent` 切换同时会同步清理续跑状态：
  如果会话存在 `pending_continue`，其 `resume_messages`、`agent_backend`、`model_key` 与相关 checkpoint 会在切换时一起对齐到新的选择，避免用户重新选完模型后 `/continue` 仍偷偷沿用旧模型和旧断点。
- 明确本次只影响 `workdir`，不影响 `config`：
  新增 `0.13 -> 0.14` workdir upgrade step；按版本策略将仓库版本从 `0.12.0` 提升到 `0.12.1`。

# 0.12.0

- 升级 `config` 保存格式到 `0.12`，本次不影响 `workdir`：
  `main_agent` 新增 `time_awareness` 配置段，支持分别控制 `emit_system_date_on_user_message` 与 `emit_idle_time_gap_hint` 两个时间感知开关。
- 为每条普通用户消息增加可选的系统日期注入能力：
  当 `main_agent.time_awareness.emit_system_date_on_user_message = true` 时，运行时会在用户 turn 内容前注入一段 `[System Date: YYYY-MM-DD HH:MM:SS +TZ]`，给模型稳定提供当前本地日期与时间。
- 把“长时间静默后的时间提示”收敛成显式配置：
  现有的长静默提示逻辑改为受 `main_agent.time_awareness.emit_idle_time_gap_hint` 控制；默认继续保持开启，方便旧配置升级后延续原有行为。
- 同步更新配置编辑器与示例：
  TUI config editor 新增这两个开关字段；最新 skeleton、`agent_host/example_config.json`、`agent_host/example_telegram_config.json`、`agent_host/example_dingtalk_config.json` 以及仓库根的 `deploy_telegram.json` 都已补齐到新格式。
- 明确本次只影响 `config`，不影响 `workdir`：
  新增 `0.11 -> 0.12` config loader；按版本策略将仓库版本从 `0.11.0` 提升到 `0.12.0`。

# 0.11.0

- 升级 `config` 保存格式到 `0.11`，本次不影响 `workdir`：
  新增 `dingtalk` channel 配置类型，支持 `client_id` / `client_id_env`、`client_secret` / `client_secret_env` 与 `api_base_url` 字段，用于通过 DingTalk Stream 模式接入应用机器人。
- 新增 DingTalk Channel 最小可用接入：
  `agent_host` 现在可以建立 DingTalk Stream 长连接接收机器人消息，按群聊与单聊会话映射为独立 `conversation_id`，并使用回调里的 `sessionWebhook` 回复当前会话文本消息；`typing` 当前为 no-op。
- 同步更新配置编辑与示例：
  TUI config editor 现已支持创建和编辑 `dingtalk` channel，仓库新增 `agent_host/example_dingtalk_config.json`，现有示例配置文件也补齐到最新 `config` 版本号。
- 明确本次只影响 `config`，不影响 `workdir`：
  新增 `0.10 -> 0.11` config loader；按版本策略将仓库版本从 `0.10.1` 提升到 `0.11.0`。

# 0.10.1

- 升级 `workdir` 结构版本到 `0.13`：
  foreground / background session 持久化状态新增 `last_user_message_at`，用于在下一条普通用户消息进入前判断用户已经沉默了多久；升级会为已有 session 尽力回填该字段，没有可推断值时保持为空。
- 为长时间静默后的下一条用户消息增加时间感知提示：
  如果上一轮 agent 返回后，用户至少 5 分钟没有继续说话，那么下一条普通用户消息前会自动插入一条 `[System Tip: X.X hours since the last user message.]`，让模型在继续对话时对时间流逝有基本感知。
- 明确本次只影响 `workdir`，不影响 `config`：
  新增 `0.12 -> 0.13` workdir upgrade step；按版本策略将仓库版本从 `0.10.0` 提升到 `0.10.1`。

# 0.10.0

- 升级 `config` 保存格式到 `0.10`，并同时升级 `workdir` 结构版本到 `0.12`：
  `main_agent` 新增 `memory_system` 配置，可在现有分层 memory 与 Claude Code 风格 memory 之间切换；workdir 会为 `rundir` 和各 workspace root 自动补齐 `PARTCLAW.md` 模板文件。
- 新增 Claude Code 风格 memory 模式：
  当前台 agent 选择 `main_agent.memory_system = "claude_code"` 时，system prompt 会嵌入 workspace 根目录的 `PARTCLAW.md` 作为持久项目记忆，context compaction 改为输出 Claude 风格的压缩摘要，并额外明确要求保留仍在后台运行的 continuation-critical 标识，例如未结束的 `exec_id`、`download_id`、文件下载 id 和 subagent id，避免压缩后无法继续回写或观察。
- 调整 memory 能力暴露与持久化路径：
  Claude 风格模式下不再向前台 agent 暴露 `memory_search` / `rollout_search` / `rollout_read` 这类分层 memory 检索工具；旧的分层 compaction 产物仍保留在 `layered` 模式下使用。
- 收紧 system prompt 一致性恢复逻辑：
  当普通用户消息到来或 `/continue` 恢复前，如果持久化消息里的 canonical system prompt 与当前应构建出的 system prompt 不一致，AgentHost 现在会在保留后续消息历史的前提下直接重建 canonical system prompt；运行期动态插入的 shared profile / model catalog / skill update 等 system message 仍按临时消息处理，不会阻止重建。
- 明确本次同时影响 `config` 与 `workdir`：
  新增 `0.9 -> 0.10` config loader 与 `0.11 -> 0.12` workdir upgrade step；按版本策略将仓库版本从 `0.9.4` 提升到 `0.10.0`。

# 0.9.4

- 升级 `workdir` 结构版本到 `0.11`：
  foreground session 持久化新增模型目录观察状态，用来记录每个 session 上次看到的可用模型列表版本，以及下一条普通用户消息前是否还需要补发模型目录变化提示。
- 为每个 foreground session 增加模型目录变更检测：
  当当前可用 agent model 的数量、名称或 description 发生变化时，AgentHost 会像 `USER.md` / `IDENTITY.md` 变更提示那样，在下一条普通用户消息前插入一条 `[System Message]`，告知模型“当前可用模型列表和 description 已变化”，并附上新的完整列表。
- 收紧中断追问场景下的变更提示注入：
  `[Interrupted Follow-up]` 和 `[Queued User Updates]` 这类中途插话不会立刻消费模型目录 / shared profile 变更提示；这些提示会继续保留到下一条正常用户消息，避免在“进度如何”这类插话里平白膨胀上下文。
- 调整 compaction 提示词对临时 runtime system message 的处理：
  自动上下文压缩现在明确要求忽略“shared profile changed / runtime skill updates / available models changed”这类会在后续 turn 重新构造的临时 system message，避免把它们错误写回压缩摘要。
- 明确本次只影响 `workdir`，不影响 `config`：
  新增 `0.10 -> 0.11` workdir upgrade step；按版本策略将仓库版本从 `0.9.3` 提升到 `0.9.4`。

# 0.9.3

- 收紧中途追问进度时的默认前台 agent 行为：
  system prompt 现在明确把“进度如何”“还在跑吗”这类消息视为状态查询而不是默认停止信号，要求优先用 `user_tell` 发短进度并继续当前任务，只有在用户明确要求停止、改向或立即给最终答复时才中断执行。
- 扩大 bubblewrap 沙盒里的默认可写安装根：
  `main_agent.global_install_root` 现在会在进入 bubblewrap 前自动确保目录存在，并以可写 bind mount 暴露给沙盒内进程，方便把全局安装型依赖统一落在配置指定的位置。
- 更新默认 Telegram 部署模板：
  `deploy_telegram.json` 现在默认把 `opus-4.6` 纳入 `agent_frame.available_models`，开启 idle compaction，并补齐该模型在模板中的若干显式字段，减少部署后手工微调。
- 明确本次不影响 `config` 与 `workdir` schema：
  没有新增 config loader，也没有新增 workdir upgrade step；按版本策略将仓库版本从 `0.9.2` 提升到 `0.9.3`。

# 0.9.2

- 升级 `workdir` 结构版本到 `0.10`：
  `agent/runtime/*/agent_frame/processes/*.json` 的 exec 运行态元数据新增 `tty` 标记，用来区分普通 pipe 模式与 PTY 模式；升级时会清理旧版 exec 运行态与对应的 exec tool worker 文件，避免旧 metadata 与新 PTY 运行时混用。
- 为 `exec_start` 增加 `tty` 参数：
  当 `tty=true` 时，后台 exec worker 会为子进程分配 PTY，让安装器、REPL 等需要终端语义的命令能以终端模式运行，同时继续沿用 `exec_observe` / `exec_wait` / `exec_kill` 的同一套生命周期。
- 打通 PTY 模式下的输入与终止语义：
  `exec_wait(input=...)` 现在可以继续给 PTY 进程写入输入；当 PTY exec 被 kill 或 session destroy 时，运行时会按进程组终止前台命令，减少 shell 退出后子进程残留的风险。
- 明确本次只影响 `workdir`，不影响 `config`：
  新增 `0.9 -> 0.10` workdir upgrade step；按版本策略将仓库版本提升到 `0.9.2`。
- 收敛仓库探索工具命名与分工：
  `read_file` / `write_file` 统一重命名为 `file_read` / `file_write`，并新增 `glob`、`grep`、`ls`，主 prompt 也改为优先使用这些专用探索工具而不是默认走 shell 搜索。
- 增强只读探索体验：
  `file_read` 现支持 Claude 风格的 `file_path` / `offset` / `limit` 参数，并补充了截断与大文件保护；`glob` / `grep` / `ls` 也加入结果上限与更适合大仓库浏览的返回结构。
- 简化 subagent 生命周期：
  主 agent 现在只暴露 `subagent_start`、`subagent_join`、`subagent_kill`；旧的 `subagent_progress`、`subagent_charge`、`subagent_tell`、`subagent_list` 已移除，subagent 改为单次小任务 worker，完成或失败后由 `join` 立即回收。
- 调整 subagent 委派策略：
  主 / 子 agent prompt 现在更明确地鼓励把小搜索、小 side task 和并行任务尽早卸载给 subagent，以降低 main agent 的上下文开销；`subagent_join` 同时支持超时等待而不强制杀掉任务。
- 明确本次不影响 `config` 与 `workdir` schema：
  没有新增 config loader，也没有新增 workdir upgrade step；这是一次运行时工具与 prompt 行为更新。

# 0.9.0

- 升级 `config` 保存格式到 `0.9`，并同时升级 `workdir` 结构版本到 `0.9`：
  `models.<alias>` 不再在配置文件里声明 `backend`；新的根级 `agent` 配置改为按 backend 维护 `available_models`，conversation / snapshot / cron / subagent / pending_continue 等持久化状态也新增了 agent backend 选择字段。
- 将 agent backend 与 model 选择解耦：
  前台 conversation、background agent、cron 和 subagent 现在都会显式保存并校验各自的 agent backend，不再从 `model.backend` 反推，避免把某个 backend 不支持的 provider 误配进去后直接在启动或恢复时崩掉。
- 收紧 backend 内可见模型范围：
  `agent_frame` / `zgent` 只会暴露各自 `available_models` 里的候选模型；subagent 也只能启动当前 backend 下允许的模型，不再看到其他 backend 的 provider。
- 调整前端交互命令：
  用户侧主命令由 `/model` 切换为 `/agent`，交互顺序改为先选 backend 再选 model；对话缺少完整 agent 选择时，按钮与提示也会按这个两步流程引导。
- 修复未认证对话的首条消息流程：
  Telegram 等需要审批的 conversation 现在会先经过安全认证，再决定是否弹出 agent/backend 选择，不会再出现“第一句话先触发模型选择、跳过认证提示”的顺序错误。
- 明确本次同时影响 `config` 与 `workdir`：
  新增 `0.8 -> 0.9` workdir upgrade step，并把 root `VERSION` 按规则提升到 `0.9.0`。

# 0.8.4

- 升级 `workdir` 结构版本到 `0.8`：
  `agent/runtime/*/agent_frame/processes` 下的 `exec` 运行态元数据改为后台 worker 驱动的新格式，支持普通跨 turn 继续 `exec_wait(input=...)`，不再依赖启动该命令的同一个 child turn 进程仍然存活。
- 调整 `exec_*` 运行时模型：
  `exec_start` 现在会拉起独立的后台 exec worker，由它持有真实子进程的 stdin；后续 turn 会通过落盘请求文件把输入转发给同一个 worker，`exec_observe/exec_wait/exec_kill` 因而可以跨 turn 复用同一个 `exec_id`。
- 保持 session destroy 时的清理语义不变：
  前台 session 被销毁时，运行中的 `exec` 仍会被统一 kill，不会残留到 session 生命周期之外。
- 补强前台 turn 失败时的恢复点选择：
  `agent_frame` 现在会持续维护最近的 stable report；如果这一轮在 `tool-wait context compaction` 或后续模型调用阶段失败，但尚未 emit 正式 checkpoint，`pending_continue` 会回退到最近稳定前缀，而不是整轮 turn 开始前的 `previous_messages`。
- 改进 `/continue` 失败提示的可读性：
  `progress_summary` 现在会优先描述已保留的工具阶段、最近工具或部分助手输出；同时失败提示会附带压缩失败/上游失败的精简原因，减少只看到泛化英文提示的情况。
- 提高旧版 `exec` 运行态元数据的兼容性：
  自动上下文压缩和 runtime 清理在扫描 `agent_frame/processes/*.json` 时，遇到缺少 `worker_pid` 的旧格式 metadata 会跳过兼容处理，不再因为历史遗留文件直接让整轮 compaction 失败。
- 修复 sandbox 子进程模式下的稳定恢复点同步：
  `subprocess` / `bubblewrap` 前台 turn 现在会把 child 内存里的最近 stable report 通过 RPC 同步回 parent；因此即使正式 checkpoint 还没 emit，只要子进程已经进入稳定点，失败后的 `/continue` 也能继承这部分进度。
- 收紧 compaction 对活跃长任务的摘要约束：
  compaction prompt 现在明确要求只为仍在运行的 `*_start` 任务保留 continuation-critical ID（如 `exec_id` / `download_id` / subagent id）及必要引用；已结束任务不再强制保留其 ID，中间 tool 调用过程只需保留紧凑摘要。
- 明确本次只影响 `workdir`，不影响 `config`：
  新增 `0.7 -> 0.8` workdir upgrade step；升级时会清理旧版 `exec` runtime state，避免旧格式元数据与新版后台 worker 模式混用。

# 0.8.3

- 升级 `workdir` 结构版本到 `0.7`：
  `zgent_native` 持久化状态新增真实的 `context_window_current/context_window_size` 字段，用来记录 native `zgent` session 返回的上下文窗口占用信息。
- 接入 Native ZGent 的真实上下文窗口回灌：
  前台 native `zgent` turn 结束后，AgentHost 会从远端 `session/get` 拉回当前窗口用量并持久化到本地 session。
- 调整上下文阈值判断优先级：
  `/status`、idle compaction 和手动 compaction 现在会优先使用 native `zgent` 提供的真实 context window 占用；缺失时才回退到本地估算。
- 明确本次只影响 `workdir`，不影响 `config`：
  新增 `0.6 -> 0.7` workdir upgrade step，现有配置格式保持不变。

# 0.8.1-beta1

- 升级 `workdir` 结构版本到 `0.6`：
  前台 session 持久化新增 `zgent_native` 状态，用来记录 Native ZGent Kernel 绑定的远端 session 元数据，便于会话级复用与重连。
- 收敛 `zgent` 运行路径为 native-only：
  `agent_host` 不再走 zgent compat chat-completions 路径，前台会话直接复用 native kernel session，并把插话转发给 native `zgent-server` 处理。
- 明确本次只影响 `workdir`，不影响 `config`：
  新增 `0.5 -> 0.6` workdir upgrade step，现有配置格式保持不变。

# 0.8.0-beta1

- 升级 `config` 保存格式到 `0.8`：
  `models.<alias>` 新增 `agent_model_enabled` 开关，用来控制某个模型能否被当作前台/后台 agent model 选择；默认值为 `true`，老版本 config loader 会自动补成 `true` 保持兼容。
- 调整 agent model 可见性与校验：
  `/model` 可选列表、prompt 里的 `Available models`、以及 `main_agent.model` 校验现在都会忽略 `agent_model_enabled=false` 的 helper 模型；这类模型仍可保留 `chat` 能力供工具链内部调用。
- 更新默认部署配置：
  `sonar_pro` 和 `gemini3.1-flash-image` 现在被显式标记为 helper-only，不再出现在用户可选 agent model 列表中。
- 明确本次只影响 `config`，不影响 `workdir`：
  没有新增 `workdir` 结构版本，也不需要新的 workdir upgrade step。

# 0.7.0-beta1

- 升级 `config` 保存格式到 `0.7`：
  `main_agent.enabled_tools` 不再写入最新配置文件，工具改为默认可用并在运行时按 agent kind、模型能力和 tooling 配置自适应选择；老版本 config loader 继续兼容读取带 `enabled_tools` 的配置。
- 调整默认工具注册策略：
  `agent_frame` 不再依赖白名单构建 built-in tool registry，而是默认开放基础文件、exec、下载、web_fetch 等工具；`web_search` 会继续根据 native search / external tooling 自适应。
- 收敛图像工具语义：
  对支持 `image_in` 的模型默认暴露 `image_load`，工具会在 `function_call_output` 后自动拼接 synthetic 多模态 `user` 消息，把图片直接带回同一模型；如果 `tooling.image` 明确指定了外部图像模型且未使用 `:self`，则仍暴露旧的 `image_start/image_wait/image_cancel` 异步链路。
- 明确本次只影响 `config`，不影响 `workdir`：
  没有新增 `workdir` 结构版本，也不需要新的 workdir upgrade step。

# 0.6.0-beta1

- 升级 `config` 保存格式到 `0.6`：
  `models` 现在按 alias 扁平定义，并新增 `capabilities` 字段与根级 `tooling` 配置；老版本 config loader 继续保留兼容升级路径。
- 将 capability / tooling 接入运行时路由：
  `tooling.web_search` 与 `tooling.image` 已经真正影响 `agent_host` 的上游能力解析；直接多模态输入也开始读取 `image_in` capability，而不再只依赖旧的 `supports_vision_input`。
- 接入 Codex 订阅 websocket 连续对话：
  `agent_frame` 现在会在运行中使用 websocket 和 `previous_response_id` 做短期续接优化；这些状态不再持久化到 workdir，重启后的恢复仍然依赖本地 messages / rollout 重建，因此本次变更不影响 `workdir` 结构版本。
- 补充 `v0.6` 配置回归测试：
  覆盖 alias-keyed models、tooling target 解析，以及 tooling alias capability 校验。

# 0.5.0-beta1

- 修复 GitHub Release workflow 的 Zig 安装链路：
  将 `mlugg/setup-zig` 从过时的 `v1` 升级到 `v2.2.1`，解决 Zig 0.15.x 官方下载路径与 tarball 命名变化导致的 `404`，恢复自动打包发布。
- 修复 GitHub Release workflow 的交叉编译目标安装：
  Release 构建矩阵现在会随 `matrix.target` 一起安装对应 Rust target，避免 `cargo zigbuild` 在 musl 目标下因缺少标准库而报 `can't find crate for core`。
- 重构 `agent_host` 服务端主流程：
  将超长的 `server.rs` 拆分为运行时辅助、消息处理、命令处理和持久化子模块，并补充了 Mermaid 工作流图与伪代码文档。
- 调整 Background Agent 架构：
  Background Agent 现在与 Foreground Agent 共享 conversation 和 workspace，但不共享 session，记忆改为严格的 session 级隔离；cron 创建的后台任务也会挂到对应 conversation 下。
- 引入 workdir 结构版本升级链：
  workdir 根目录现在使用 `VERSION` 文件标识结构版本，缺失时按 `0.4` 处理，并可按顺序升级到 `0.5`；`0.5` 会把旧 session 中的 `workspace_id` 回填到 conversation 持久化状态。
- 升级配置保存格式到 `0.5`：
  保持老版本 config loader 兼容，同时最新保存格式不再写入 `main_agent.timeout_seconds`，并同步更新 `scripts/add_chat_model.sh` 对最新配置版本的支持。
- 改进 Telegram 交互：
  选项类消息改为 inline keyboard callback 按钮，减少聊天框输入干扰并改善模型选择、设置切换等交互体验。

# 0.4.0-beta3

- 修复 GitHub Release workflow 的远端 tag 检测逻辑：
  由本地 `refs/tags/*` 判断改为查询 `origin` 远端 tags，避免 `workflow_run` 场景下误判 tag 不存在并导致发布流程异常。
- 补齐自动发布链路验证：
  通过版本号递增重新触发 Release 流程，确保当前 CI/CD 与 GitHub Release 产物生成逻辑一致。

# 0.4.0-beta2

- 修复 `USER.md` / `IDENTITY.md` 共享资料流转：
  workspace 会自动带上副本，缺失时会在对话前自动补齐，并只在正常对话里注入更新提醒。
- 新增 `shared_profile_upload` 工具：
  Agent 修改 workspace 内的 `USER.md` / `IDENTITY.md` 后可回传到共享区，只有内容真的变化时才会更新共享文件并轮换 conversation 的 `chat_version_id`。
- 调整共享资料提示语义：
  当前前台运行期间不重建 system prompt，收到系统提示后需要用 `read_file` 读取 workspace 内的 `IDENTITY.md`，`USER.md` 则按需读取，从而避免中途改写 prompt 造成 KV cache 失效。
- 修复 `agent_frame` 集成测试与当前结构化 compaction / timeout observation 行为不一致的问题，恢复 CI 绿色并重新打通自动 Release 链路。

# 0.4.0-beta1

- 提供完整的 Rust 多代理运行时与长驻服务架构，分为 `agent_frame` 与 `agent_host` 两层。
- 支持长期运行的 conversation/session 模型，具备持久化、恢复、`/continue`、中断 follow-up 合流与多会话并发处理。
- 内置丰富工具链：
  `read_file`、`write_file`、`edit`、`apply_patch`、`exec_*`、`file_download_*`、`web_fetch`、`web_search`、`image_*`、subagent 等。
- 支持可中断工具与长任务状态保持，`exec`、下载、subagent 等运行态可跨压缩与跨 turn 延续。
- 支持技能系统：`SKILL.md` 驱动的 skills、共享 `.skill_memory`、skill create/update/load 与运行时变更感知。
- 支持 conversation 级记忆系统：
  `memory_summary`、`MEMORY`、`rollout_summary`、`rollout_transcript` 四层检索路径。
- 支持结构化上下文压缩：
  threshold compaction、idle compaction、timeout-observation compaction，以及高保真 recent context 保留。
- 支持共享状态更新注入：
  `USER.md`、`IDENTITY.md`、skills 更新通过 synthetic system message 提示并参与最新 system prompt 重组。
- 支持 Telegram 等 channel 集成，包含命令体系、长消息拆分、状态提示、恢复提示与会话级设置。
- 支持多模型配置、模型切换、reasoning effort、sandbox mode 切换与 bubblewrap 隔离。
- 支持 snapshots：
  `/snapsave`、`/snapload`、`/snaplist` 可完整保存与恢复 workspace、session、conversation memory。
- 提供结构化日志、状态查询、成本/usage 统计，以及用于生产部署的 systemd 运行方式。

# Versioning

- 版本号使用 SemVer，并允许 `-beta1`、`-rc1` 这类预发布后缀。
- 开发中的不稳定测试发布统一使用 `0.x.y-betaN`。
- 正式稳定发布使用 `MAJOR.MINOR.PATCH`。
